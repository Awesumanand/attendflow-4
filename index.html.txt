<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AttendFlow — Workforce Intelligence</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;800&family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<style>
:root {
  --bg:#f5f3ef; --bg2:#ede9e3; --sur:#fff; --sur2:#faf9f7; --sur3:#f0ede8;
  --bdr:#e8e3db; --bdr2:#d8d2c8;
  --pri:#4f46e5; --pril:#6366f1; --prix:#eef2ff; --priglow:rgba(79,70,229,.12);
  --vio:#7c3aed; --viol:#ede9fe;
  --grn:#059669; --grnl:#d1fae5; --grnm:#10b981;
  --amb:#d97706; --ambl:#fef3c7; --ambm:#f59e0b;
  --red:#e11d48; --redl:#ffe4e6; --redm:#f43f5e;
  --sky:#0284c7; --skyl:#e0f2fe;
  --txt:#1a1714; --txt2:#4a4540; --txt3:#8a8078; --txt4:#b8b0a5;
  --sh1:0 1px 3px rgba(0,0,0,.06),0 1px 2px rgba(0,0,0,.04);
  --sh2:0 4px 16px rgba(0,0,0,.07),0 2px 6px rgba(0,0,0,.04);
  --sh3:0 8px 32px rgba(0,0,0,.09),0 4px 12px rgba(0,0,0,.05);
  --sh4:0 20px 60px rgba(0,0,0,.12),0 8px 24px rgba(0,0,0,.06);
  --r:16px; --rsm:10px; --rxs:6px; --rpill:999px;
  --sw:252px; --th:66px;
}
*{margin:0;padding:0;box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{font-family:'Plus Jakarta Sans',sans-serif;background:var(--bg);color:var(--txt);min-height:100vh;overflow-x:hidden;}
::-webkit-scrollbar{width:5px;height:5px;}
::-webkit-scrollbar-track{background:transparent;}
::-webkit-scrollbar-thumb{background:var(--bdr2);border-radius:4px;}

/* SIDEBAR */
.sidebar{position:fixed;top:0;left:0;width:var(--sw);height:100vh;background:var(--sur);border-right:1px solid var(--bdr);display:flex;flex-direction:column;z-index:100;transition:transform .3s cubic-bezier(.4,0,.2,1);box-shadow:var(--sh2);}
.logo-wrap{padding:20px 18px 16px;border-bottom:1px solid var(--bdr);display:flex;align-items:center;gap:10px;}
.logo-icon{width:36px;height:36px;border-radius:11px;background:linear-gradient(135deg,var(--pri),var(--vio));display:flex;align-items:center;justify-content:center;font-size:17px;flex-shrink:0;box-shadow:0 4px 12px rgba(79,70,229,.28);}
.logo-txt{font-family:'Playfair Display',serif;font-weight:700;font-size:19px;color:var(--txt);letter-spacing:-.3px;}
.logo-txt span{color:var(--pri);}
.nav-wrap{padding:12px 10px;flex:1;overflow-y:auto;}
.nav-section{font-size:10px;font-weight:700;letter-spacing:1.4px;color:var(--txt4);text-transform:uppercase;padding:10px 10px 4px;}
.nav-item{display:flex;align-items:center;gap:9px;padding:9px 11px;border-radius:var(--rsm);cursor:pointer;color:var(--txt3);font-size:13.5px;font-weight:500;margin-bottom:1px;transition:all .16s;position:relative;overflow:hidden;user-select:none;}
.nav-item:hover{background:var(--sur3);color:var(--txt);}
.nav-item.active{background:var(--prix);color:var(--pri);font-weight:600;}
.nav-item.active::before{content:'';position:absolute;left:0;top:20%;bottom:20%;width:3px;border-radius:2px;background:var(--pri);}
.nav-item .ni{font-size:14px;width:18px;text-align:center;flex-shrink:0;}
.nav-badge{background:var(--red);color:#fff;font-size:10px;font-weight:700;padding:1px 6px;border-radius:var(--rpill);margin-left:auto;}
.sidebar-user{padding:13px 15px;border-top:1px solid var(--bdr);display:flex;align-items:center;gap:10px;cursor:pointer;transition:background .15s;}
.sidebar-user:hover{background:var(--sur2);}
.uav{width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,var(--pri),var(--vio));display:flex;align-items:center;justify-content:center;font-weight:700;font-size:12px;color:#fff;flex-shrink:0;box-shadow:0 2px 8px rgba(79,70,229,.22);}

/* MAIN */
.main{margin-left:var(--sw);min-height:100vh;}
.topbar{position:sticky;top:0;z-index:50;background:rgba(255,255,255,.9);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);border-bottom:1px solid var(--bdr);padding:0 26px;height:var(--th);display:flex;align-items:center;justify-content:space-between;box-shadow:0 1px 0 var(--bdr),0 2px 12px rgba(0,0,0,.03);}
.topbar-l{display:flex;align-items:center;gap:12px;}
.hamb{display:none;background:none;border:none;font-size:20px;cursor:pointer;color:var(--txt2);padding:4px;border-radius:var(--rxs);}
.pg-title{font-family:'Playfair Display',serif;font-size:19px;font-weight:700;color:var(--txt);letter-spacing:-.2px;}
.pg-date{font-size:11.5px;color:var(--txt3);margin-top:1px;}
.topbar-r{display:flex;align-items:center;gap:10px;}
#liveClock{font-family:'Playfair Display',serif;font-size:15px;font-weight:600;color:var(--pri);background:var(--prix);padding:5px 13px;border-radius:var(--rpill);letter-spacing:.3px;}

/* BUTTONS */
.btn{padding:8px 16px;border-radius:var(--rsm);border:none;cursor:pointer;font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;font-weight:600;transition:all .16s;white-space:nowrap;display:inline-flex;align-items:center;justify-content:center;gap:5px;}
.btn-primary{background:linear-gradient(135deg,var(--pri),var(--pril));color:#fff;box-shadow:0 4px 12px rgba(79,70,229,.26);}
.btn-primary:hover{transform:translateY(-1px);box-shadow:0 6px 20px rgba(79,70,229,.34);}
.btn-primary:active{transform:translateY(0);}
.btn-success{background:linear-gradient(135deg,var(--grn),var(--grnm));color:#fff;box-shadow:0 3px 10px rgba(5,150,105,.2);}
.btn-success:hover{transform:translateY(-1px);}
.btn-danger{background:linear-gradient(135deg,var(--red),var(--redm));color:#fff;box-shadow:0 3px 10px rgba(225,29,72,.2);}
.btn-danger:hover{transform:translateY(-1px);}
.btn-warning{background:linear-gradient(135deg,var(--amb),var(--ambm));color:#fff;}
.btn-secondary{background:var(--sur);color:var(--txt2);border:1.5px solid var(--bdr);}
.btn-secondary:hover{background:var(--sur3);color:var(--txt);border-color:var(--bdr2);}
.btn-ghost{background:transparent;color:var(--pri);padding:6px 12px;}
.btn-ghost:hover{background:var(--prix);}
.btn-sm{padding:6px 13px;font-size:12px;border-radius:var(--rxs);}
.btn-xs{padding:4px 9px;font-size:11px;border-radius:var(--rxs);}
.btn:disabled{opacity:.5;cursor:not-allowed;transform:none !important;}

/* PAGES */
.page{display:none;padding:24px 26px;animation:fadeUp .28s ease;}
.page.active{display:block;}
@keyframes fadeUp{from{opacity:0;transform:translateY(7px)}to{opacity:1;transform:none}}

/* STAT CARDS */
.stats-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:20px;}
.stat-card{background:var(--sur);border:1px solid var(--bdr);border-radius:var(--r);padding:18px;transition:all .2s;position:relative;overflow:hidden;box-shadow:var(--sh1);}
.stat-card:hover{transform:translateY(-2px);box-shadow:var(--sh3);}
.stat-card::after{content:'';position:absolute;top:0;left:0;right:0;height:3px;border-radius:var(--r) var(--r) 0 0;}
.sc-pri::after{background:linear-gradient(90deg,var(--pri),var(--pril));}
.sc-grn::after{background:linear-gradient(90deg,var(--grn),var(--grnm));}
.sc-amb::after{background:linear-gradient(90deg,var(--amb),var(--ambm));}
.sc-red::after{background:linear-gradient(90deg,var(--red),var(--redm));}
.sc-vio::after{background:linear-gradient(90deg,var(--vio),#9333ea);}
.sc-sky::after{background:linear-gradient(90deg,var(--sky),#0ea5e9);}
.stat-top{display:flex;align-items:flex-start;justify-content:space-between;}
.stat-label{font-size:10.5px;font-weight:700;color:var(--txt3);text-transform:uppercase;letter-spacing:.9px;margin-bottom:8px;}
.stat-val{font-family:'Playfair Display',serif;font-size:34px;font-weight:800;line-height:1;letter-spacing:-.5px;}
.v-pri{color:var(--pri);}.v-grn{color:var(--grn);}.v-amb{color:var(--amb);}.v-red{color:var(--red);}.v-vio{color:var(--vio);}.v-sky{color:var(--sky);}
.stat-ico{width:38px;height:38px;border-radius:11px;display:flex;align-items:center;justify-content:center;font-size:17px;flex-shrink:0;}
.ico-pri{background:var(--prix);}.ico-grn{background:var(--grnl);}.ico-amb{background:var(--ambl);}.ico-red{background:var(--redl);}.ico-vio{background:var(--viol);}.ico-sky{background:var(--skyl);}
.stat-chg{font-size:11.5px;margin-top:7px;display:flex;align-items:center;gap:3px;}
.chg-up{color:var(--grn);}.chg-dn{color:var(--red);}.chg-n{color:var(--txt3);}

/* CARDS */
.card{background:var(--sur);border:1px solid var(--bdr);border-radius:var(--r);padding:20px;box-shadow:var(--sh1);transition:box-shadow .2s;}
.card:hover{box-shadow:var(--sh2);}
.card-hd{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:16px;gap:8px;flex-wrap:wrap;}
.card-title{font-family:'Playfair Display',serif;font-size:15px;font-weight:700;color:var(--txt);}
.card-sub{font-size:11.5px;color:var(--txt3);margin-top:2px;}
.card-grad{background:linear-gradient(135deg,var(--pri) 0%,var(--vio) 100%);border:none;}

/* GRIDS */
.g2{display:grid;grid-template-columns:1fr 1fr;gap:16px;}
.g3{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;}
.g4{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;}
.mb12{margin-bottom:12px;}.mb16{margin-bottom:16px;}.mb20{margin-bottom:20px;}.mb24{margin-bottom:24px;}
.mt8{margin-top:8px;}.mt12{margin-top:12px;}.mt16{margin-top:16px;}

/* TABLE */
.tbl-wrap{overflow-x:auto;border-radius:var(--rsm);}
table{width:100%;border-collapse:collapse;font-size:13px;}
th{padding:10px 13px;text-align:left;font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;color:var(--txt3);background:var(--sur2);border-bottom:1.5px solid var(--bdr);}
th:first-child{border-radius:var(--rsm) 0 0 0;}
th:last-child{border-radius:0 var(--rsm) 0 0;}
td{padding:11px 13px;border-bottom:1px solid var(--bdr);vertical-align:middle;}
tr:last-child td{border-bottom:none;}
tbody tr{transition:background .1s;}
tbody tr:hover td{background:var(--sur2);}

/* BADGES */
.badge{display:inline-flex;align-items:center;gap:4px;padding:3px 9px;border-radius:var(--rpill);font-size:11px;font-weight:600;white-space:nowrap;}
.bp{background:var(--grnl);color:var(--grn);}
.ba{background:var(--redl);color:var(--red);}
.bl{background:var(--ambl);color:var(--amb);}
.bv{background:var(--viol);color:var(--vio);}
.bb{background:var(--prix);color:var(--pri);}
.bsky{background:var(--skyl);color:var(--sky);}
.b-live{background:var(--grnl);color:var(--grn);animation:pulse-b 2s infinite;}
@keyframes pulse-b{0%,100%{opacity:1}50%{opacity:.6}}

/* AVATAR */
.av{border-radius:50%;display:inline-flex;align-items:center;justify-content:center;font-weight:700;flex-shrink:0;}
.av-xs{width:26px;height:26px;font-size:10px;}
.av-sm{width:30px;height:30px;font-size:11px;}
.av-md{width:38px;height:38px;font-size:14px;}
.av-lg{width:48px;height:48px;font-size:17px;}
.av-xl{width:60px;height:60px;font-size:22px;}

/* FORMS */
.fg{margin-bottom:14px;}
.flabel{display:block;font-size:11px;font-weight:700;color:var(--txt3);margin-bottom:5px;text-transform:uppercase;letter-spacing:.6px;}
.fc{width:100%;padding:9px 12px;background:var(--sur2);border:1.5px solid var(--bdr);border-radius:var(--rsm);color:var(--txt);font-family:'Plus Jakarta Sans',sans-serif;font-size:13.5px;outline:none;transition:all .15s;}
.fc:focus{border-color:var(--pri);background:var(--sur);box-shadow:0 0 0 3px var(--priglow);}
.fc:disabled{opacity:.5;cursor:not-allowed;background:var(--bg2);}
select.fc{cursor:pointer;}
textarea.fc{resize:vertical;min-height:76px;}
.fc-error{border-color:var(--red) !important;}
.field-error{color:var(--red);font-size:11px;margin-top:4px;display:block;}

/* MODAL */
.overlay{display:none;position:fixed;inset:0;z-index:200;background:rgba(26,23,20,.42);backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px);align-items:center;justify-content:center;}
.overlay.open{display:flex;}
.modal{background:var(--sur);border:1px solid var(--bdr);border-radius:20px;padding:26px;width:520px;max-width:92vw;max-height:90vh;overflow-y:auto;animation:mIn .24s cubic-bezier(.34,1.56,.64,1);box-shadow:var(--sh4);}
.modal-lg{width:640px;}
.modal-xl{width:760px;}
@keyframes mIn{from{opacity:0;transform:scale(.94) translateY(10px)}to{opacity:1;transform:none}}
.mhd{display:flex;justify-content:space-between;align-items:center;margin-bottom:18px;}
.mtitle{font-family:'Playfair Display',serif;font-size:18px;font-weight:700;color:var(--txt);}
.mclose{background:var(--sur2);border:1px solid var(--bdr);color:var(--txt3);font-size:15px;cursor:pointer;width:28px;height:28px;border-radius:50%;display:flex;align-items:center;justify-content:center;transition:all .14s;}
.mclose:hover{background:var(--redl);color:var(--red);border-color:transparent;}
.mfooter{display:flex;gap:8px;justify-content:flex-end;margin-top:18px;padding-top:16px;border-top:1px solid var(--bdr);}

/* TOAST */
.toast-wrap{position:fixed;bottom:22px;right:22px;z-index:400;display:flex;flex-direction:column;gap:7px;pointer-events:none;}
.toast{background:var(--sur);border:1.5px solid var(--bdr);padding:12px 16px;border-radius:var(--rsm);font-size:13px;min-width:240px;max-width:320px;display:flex;align-items:center;gap:9px;box-shadow:var(--sh3);animation:tIn .28s cubic-bezier(.34,1.56,.64,1);transition:all .28s;pointer-events:auto;}
@keyframes tIn{from{opacity:0;transform:translateX(18px) scale(.96)}to{opacity:1;transform:none}}
.toast.success{border-left:4px solid var(--grn);}
.toast.error{border-left:4px solid var(--red);}
.toast.info{border-left:4px solid var(--pri);}
.toast.warning{border-left:4px solid var(--amb);}
.t-msg{flex:1;line-height:1.4;color:var(--txt2);font-weight:500;}

/* TABS */
.tab-row{display:flex;gap:2px;background:var(--sur2);padding:4px;border-radius:var(--rsm);width:fit-content;margin-bottom:16px;border:1px solid var(--bdr);}
.tab{padding:6px 15px;border-radius:var(--rxs);font-size:13px;font-weight:500;cursor:pointer;color:var(--txt3);transition:all .14s;user-select:none;}
.tab.active{background:var(--sur);color:var(--pri);font-weight:600;box-shadow:var(--sh1);}

/* PROGRESS */
.pbar{background:var(--bdr);border-radius:var(--rpill);height:6px;overflow:hidden;}
.pfill{height:100%;border-radius:var(--rpill);transition:width .8s cubic-bezier(.4,0,.2,1);}

/* SEARCH */
.search-box{position:relative;}
.search-box input{padding-left:35px;}
.search-ic{position:absolute;left:11px;top:50%;transform:translateY(-50%);color:var(--txt3);font-size:13px;pointer-events:none;}

/* PUNCH HERO */
.punch-hero{background:linear-gradient(135deg,#4f46e5 0%,#7c3aed 60%,#9333ea 100%);border:none;border-radius:20px;padding:30px;text-align:center;position:relative;overflow:hidden;box-shadow:0 12px 40px rgba(79,70,229,.28);}
.punch-hero::before{content:'';position:absolute;top:-50px;right:-50px;width:180px;height:180px;border-radius:50%;background:rgba(255,255,255,.07);}
.punch-hero::after{content:'';position:absolute;bottom:-40px;left:-30px;width:150px;height:150px;border-radius:50%;background:rgba(255,255,255,.05);}
.ph-label{font-size:10px;letter-spacing:2px;text-transform:uppercase;color:rgba(255,255,255,.6);margin-bottom:4px;position:relative;z-index:1;}
.clock-big{font-family:'Playfair Display',serif;font-size:50px;font-weight:800;color:#fff;letter-spacing:-1px;margin:4px 0;text-shadow:0 2px 20px rgba(0,0,0,.12);position:relative;z-index:1;}
.clock-sub{font-size:12px;color:rgba(255,255,255,.7);margin-bottom:18px;position:relative;z-index:1;}
.punch-status{font-size:12.5px;margin-bottom:16px;color:rgba(255,255,255,.8);position:relative;z-index:1;min-height:18px;}
.pbtn{width:96px;height:96px;border-radius:50%;border:3px solid rgba(255,255,255,.3);background:rgba(255,255,255,.18);backdrop-filter:blur(10px);color:#fff;font-family:'Plus Jakarta Sans',sans-serif;font-size:12px;font-weight:700;cursor:pointer;margin:0 auto 18px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;transition:all .2s;position:relative;z-index:1;}
.pbtn:hover{background:rgba(255,255,255,.28);transform:scale(1.05);border-color:rgba(255,255,255,.55);}
.pbtn:active{transform:scale(.97);}
.pbtn.out{background:rgba(225,29,72,.32);border-color:rgba(244,63,94,.5);}
.pbtn.out:hover{background:rgba(225,29,72,.45);}
.pbtn .pic{font-size:22px;}
.punch-stats-row{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:14px;position:relative;z-index:1;}
.ps-item{text-align:center;}
.ps-val{font-family:'Playfair Display',serif;font-size:17px;font-weight:700;color:#fff;}
.ps-lbl{font-size:10px;color:rgba(255,255,255,.6);margin-top:2px;}

/* CALENDAR */
.cal-hd{display:grid;grid-template-columns:repeat(7,1fr);gap:2px;margin-bottom:3px;}
.cal-hd span{text-align:center;font-size:10px;font-weight:700;color:var(--txt3);text-transform:uppercase;padding:3px;}
.cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:2px;}
.cal-day{text-align:center;padding:7px 2px;font-size:12px;border-radius:var(--rxs);cursor:pointer;transition:all .1s;font-weight:500;border:1px solid transparent;}
.cal-day:hover{background:var(--sur3);}
.cal-day.today{background:var(--pri);color:#fff;font-weight:700;box-shadow:0 2px 8px rgba(79,70,229,.3);}
.cal-day.present{background:var(--grnl);color:var(--grn);border-color:rgba(5,150,105,.2);}
.cal-day.absent{background:var(--redl);color:var(--red);border-color:rgba(225,29,72,.15);}
.cal-day.leave{background:var(--viol);color:var(--vio);border-color:rgba(124,58,237,.15);}
.cal-day.holiday{background:var(--ambl);color:var(--amb);border-color:rgba(217,119,6,.15);}
.cal-day.weekend{color:var(--txt4);}
.cal-day.empty{visibility:hidden;}

/* SHIFT PILL */
.spill{display:inline-flex;align-items:center;gap:4px;padding:3px 9px;border-radius:var(--rpill);font-size:11px;font-weight:600;background:var(--prix);color:var(--pri);}

/* EMPTY STATE */
.empty{text-align:center;padding:44px 16px;color:var(--txt3);}
.empty .ei{font-size:40px;margin-bottom:10px;opacity:.35;}
.empty p{font-size:13px;font-weight:500;}

/* TOGGLE */
.toggle{position:relative;display:inline-block;width:42px;height:22px;}
.toggle input{opacity:0;width:0;height:0;}
.tslider{position:absolute;cursor:pointer;inset:0;background:var(--bdr2);border-radius:22px;transition:.25s;}
.tslider::before{content:"";position:absolute;width:16px;height:16px;left:3px;bottom:3px;background:#fff;border-radius:50%;transition:.25s;box-shadow:0 1px 3px rgba(0,0,0,.14);}
.toggle input:checked+.tslider{background:var(--pri);}
.toggle input:checked+.tslider::before{transform:translateX(20px);}

/* CHART CONTAINERS */
.chart-box{position:relative;height:168px;}
.chart-box-sm{position:relative;height:130px;}

/* EMPLOYEE CARD */
.emp-card{background:var(--sur);border:1.5px solid var(--bdr);border-radius:var(--r);padding:16px;transition:all .2s;box-shadow:var(--sh1);}
.emp-card:hover{transform:translateY(-3px);box-shadow:var(--sh3);}

/* NOTIFICATION COUNT */
.notif-count{background:var(--red);color:#fff;font-size:10px;font-weight:700;min-width:18px;height:18px;padding:0 5px;border-radius:var(--rpill);display:inline-flex;align-items:center;justify-content:center;}

/* UTILS */
.flex{display:flex;}.aic{align-items:center;}.jb{justify-content:space-between;}.jc{justify-content:center;}
.gap4{gap:4px;}.gap6{gap:6px;}.gap8{gap:8px;}.gap12{gap:12px;}.gap16{gap:16px;}
.mla{margin-left:auto;}
.fw5{font-weight:500;}.fw6{font-weight:600;}.fw7{font-weight:700;}.fw8{font-weight:800;}
.tsm{font-size:13px;}.txs{font-size:11.5px;}.txxs{font-size:10.5px;}
.tmuted{color:var(--txt3);}.tpri{color:var(--pri);}.tgrn{color:var(--grn);}.tred{color:var(--red);}.tamb{color:var(--amb);}.tvio{color:var(--vio);}
.w100{width:100%;}.text-center{text-align:center;}
.divider{height:1px;background:var(--bdr);margin:14px 0;}

/* RESPONSIVE */
@media(max-width:1100px){.stats-grid{grid-template-columns:repeat(2,1fr);}.g3{grid-template-columns:1fr 1fr;}}
@media(max-width:768px){.sidebar{transform:translateX(-100%);}.sidebar.open{transform:none;}.main{margin-left:0;}.stats-grid{grid-template-columns:1fr 1fr;}.g2{grid-template-columns:1fr;}.g3{grid-template-columns:1fr;}.hamb{display:block;}.topbar{padding:0 14px;}.page{padding:14px;}}

/* INFO BOX */
.info-box{padding:12px 14px;border-radius:var(--rsm);border:1px solid;display:flex;align-items:flex-start;gap:8px;font-size:12.5px;}
.info-box.info{background:var(--prix);border-color:rgba(79,70,229,.2);color:var(--pri);}
.info-box.success{background:var(--grnl);border-color:rgba(5,150,105,.2);color:var(--grn);}
.info-box.warning{background:var(--ambl);border-color:rgba(217,119,6,.2);color:var(--amb);}
.info-box.error{background:var(--redl);border-color:rgba(225,29,72,.2);color:var(--red);}

/* DROPDOWN */
.dropdown{position:relative;}
.dropdown-menu{position:absolute;right:0;top:calc(100% + 6px);background:var(--sur);border:1px solid var(--bdr);border-radius:var(--rsm);padding:6px;min-width:160px;box-shadow:var(--sh3);z-index:50;display:none;animation:mIn .18s ease;}
.dropdown-menu.open{display:block;}
.dropdown-item{padding:8px 12px;border-radius:var(--rxs);font-size:13px;cursor:pointer;color:var(--txt2);transition:all .1s;display:flex;align-items:center;gap:8px;}
.dropdown-item:hover{background:var(--sur3);color:var(--txt);}
.dropdown-item.danger{color:var(--red);}
.dropdown-item.danger:hover{background:var(--redl);}

/* STEP INDICATOR */
.steps{display:flex;gap:8px;margin-bottom:20px;}
.step{flex:1;text-align:center;padding:10px;border-radius:var(--rsm);border:1.5px solid var(--bdr);font-size:12px;font-weight:600;color:var(--txt3);transition:all .2s;}
.step.active{border-color:var(--pri);background:var(--prix);color:var(--pri);}
.step.done{border-color:var(--grn);background:var(--grnl);color:var(--grn);}

/* QUICK ACTION PILLS */
.qa-wrap{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px;}
.qa-pill{padding:6px 14px;border-radius:var(--rpill);font-size:12px;font-weight:600;cursor:pointer;border:1.5px solid var(--bdr);background:var(--sur);color:var(--txt2);transition:all .15s;display:flex;align-items:center;gap:5px;}
.qa-pill:hover{border-color:var(--pri);color:var(--pri);background:var(--prix);}
.qa-pill.active{border-color:var(--pri);background:var(--prix);color:var(--pri);}

/* ══════════ LOGIN SCREEN ══════════ */
#loginScreen{position:fixed;inset:0;z-index:9999;background:linear-gradient(135deg,#f5f3ef 0%,#ede9e3 40%,#e8e4fd 100%);display:flex;align-items:center;justify-content:center;font-family:'Plus Jakarta Sans',sans-serif;}
#loginScreen::before{content:'';position:absolute;inset:0;background:url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%234f46e5' fill-opacity='0.03'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");pointer-events:none;}
.login-split{display:flex;width:960px;max-width:96vw;min-height:560px;border-radius:24px;overflow:hidden;box-shadow:0 32px 80px rgba(79,70,229,.18),0 12px 32px rgba(0,0,0,.08);position:relative;z-index:1;}
.login-left{flex:1;background:linear-gradient(135deg,#4f46e5 0%,#7c3aed 60%,#9333ea 100%);padding:48px 44px;display:flex;flex-direction:column;justify-content:space-between;position:relative;overflow:hidden;}
.login-left::before{content:'';position:absolute;top:-60px;right:-60px;width:240px;height:240px;border-radius:50%;background:rgba(255,255,255,.07);}
.login-left::after{content:'';position:absolute;bottom:-60px;left:-40px;width:200px;height:200px;border-radius:50%;background:rgba(255,255,255,.05);}
.ll-logo{display:flex;align-items:center;gap:12px;position:relative;z-index:1;}
.ll-logo-icon{width:44px;height:44px;border-radius:13px;background:rgba(255,255,255,.18);display:flex;align-items:center;justify-content:center;font-size:22px;backdrop-filter:blur(10px);}
.ll-logo-txt{font-family:'Playfair Display',serif;font-weight:700;font-size:22px;color:#fff;letter-spacing:-.3px;}
.ll-logo-txt span{color:rgba(255,255,255,.7);}
.ll-content{position:relative;z-index:1;}
.ll-heading{font-family:'Playfair Display',serif;font-size:30px;font-weight:800;color:#fff;line-height:1.2;margin-bottom:14px;}
.ll-sub{font-size:14px;color:rgba(255,255,255,.72);line-height:1.6;margin-bottom:32px;}
.ll-features{display:flex;flex-direction:column;gap:12px;}
.ll-feat{display:flex;align-items:center;gap:10px;color:rgba(255,255,255,.85);font-size:13px;}
.ll-feat-ic{width:28px;height:28px;border-radius:8px;background:rgba(255,255,255,.15);display:flex;align-items:center;justify-content:center;font-size:13px;flex-shrink:0;}
.ll-footer{font-size:11px;color:rgba(255,255,255,.45);position:relative;z-index:1;}
.login-right{width:420px;background:#fff;padding:48px 44px;display:flex;flex-direction:column;justify-content:center;}
.lr-title{font-family:'Playfair Display',serif;font-size:26px;font-weight:700;color:#1a1714;margin-bottom:6px;}
.lr-sub{font-size:13px;color:#8a8078;margin-bottom:32px;}
.login-form{display:flex;flex-direction:column;gap:0;}
.lf-group{margin-bottom:18px;position:relative;}
.lf-label{display:block;font-size:11px;font-weight:700;color:#8a8078;margin-bottom:6px;text-transform:uppercase;letter-spacing:.7px;}
.lf-input{width:100%;padding:12px 14px;background:#faf9f7;border:1.5px solid #e8e3db;border-radius:10px;color:#1a1714;font-family:'Plus Jakarta Sans',sans-serif;font-size:14px;outline:none;transition:all .16s;}
.lf-input:focus{border-color:#4f46e5;background:#fff;box-shadow:0 0 0 3px rgba(79,70,229,.1);}
.lf-input.error{border-color:#e11d48;background:#fff9f9;}
.lf-pw-wrap{position:relative;}
.lf-pw-wrap .lf-input{padding-right:44px;}
.pw-toggle{position:absolute;right:13px;top:50%;transform:translateY(-50%);background:none;border:none;cursor:pointer;font-size:16px;color:#8a8078;transition:color .15s;padding:2px;}
.pw-toggle:hover{color:#4f46e5;}
.login-btn{width:100%;padding:13px;background:linear-gradient(135deg,#4f46e5,#7c3aed);color:#fff;border:none;border-radius:10px;font-family:'Plus Jakarta Sans',sans-serif;font-size:14px;font-weight:700;cursor:pointer;transition:all .18s;letter-spacing:.3px;box-shadow:0 4px 14px rgba(79,70,229,.28);margin-bottom:20px;}
.login-btn:hover{transform:translateY(-1px);box-shadow:0 6px 22px rgba(79,70,229,.36);}
.login-btn:active{transform:translateY(0);}
.login-btn:disabled{opacity:.6;cursor:not-allowed;transform:none;}
.login-error{background:#fff2f4;border:1px solid rgba(225,29,72,.2);border-radius:8px;padding:11px 14px;font-size:13px;color:#e11d48;margin-bottom:16px;display:none;align-items:center;gap:8px;}
.login-error.show{display:flex;}
.login-divider{display:flex;align-items:center;gap:12px;margin:0 0 20px;color:#b8b0a5;font-size:12px;}
.login-divider::before,.login-divider::after{content:'';flex:1;height:1px;background:#e8e3db;}
.demo-accounts{display:grid;grid-template-columns:1fr 1fr;gap:8px;}
.demo-btn{padding:9px 10px;border:1.5px solid #e8e3db;border-radius:8px;background:#faf9f7;cursor:pointer;transition:all .15s;text-align:left;}
.demo-btn:hover{border-color:#4f46e5;background:#eef2ff;}
.demo-btn .db-role{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;color:#8a8078;margin-bottom:2px;}
.demo-btn .db-name{font-size:12.5px;font-weight:600;color:#1a1714;}
.demo-btn .db-cred{font-size:10.5px;color:#8a8078;font-family:monospace;}
.login-footer{margin-top:24px;text-align:center;font-size:12px;color:#b8b0a5;}

/* SESSION TOPBAR */
#sessionBar{display:none;align-items:center;gap:12px;}
.session-user{display:flex;align-items:center;gap:8px;padding:5px 12px;background:var(--sur2);border:1px solid var(--bdr);border-radius:var(--rpill);cursor:pointer;transition:all .15s;}
.session-user:hover{background:var(--sur3);}
.session-uav{width:26px;height:26px;border-radius:50%;background:linear-gradient(135deg,var(--pri),var(--vio));color:#fff;font-size:10px;font-weight:700;display:flex;align-items:center;justify-content:center;}
.logout-btn{padding:5px 12px;background:var(--redl);color:var(--red);border:1px solid rgba(225,29,72,.2);border-radius:var(--rpill);font-size:12px;font-weight:600;cursor:pointer;transition:all .15s;}
.logout-btn:hover{background:var(--red);color:#fff;}

/* ROLE BADGE */
.role-badge{display:inline-flex;align-items:center;gap:4px;padding:2px 8px;border-radius:var(--rpill);font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.5px;}
.rb-admin{background:rgba(79,70,229,.12);color:var(--pri);}
.rb-hr{background:rgba(5,150,105,.12);color:var(--grn);}
.rb-manager{background:rgba(217,119,6,.12);color:var(--amb);}
.rb-employee{background:rgba(124,58,237,.12);color:var(--vio);}

/* LOCK OVERLAY for restricted pages */
.page-locked{pointer-events:none;opacity:.45;filter:blur(1px);}
.lock-notice{position:absolute;inset:0;z-index:10;display:flex;flex-direction:column;align-items:center;justify-content:center;background:rgba(245,243,239,.7);backdrop-filter:blur(3px);border-radius:var(--r);}
.lock-notice-box{background:#fff;border:1px solid var(--bdr);border-radius:var(--r);padding:28px 36px;text-align:center;box-shadow:var(--sh3);}

/* LOGIN LOADING SPINNER */
.login-spinner{display:inline-block;width:16px;height:16px;border:2px solid rgba(255,255,255,.4);border-top-color:#fff;border-radius:50%;animation:spin .7s linear infinite;margin-right:6px;}
@keyframes spin{to{transform:rotate(360deg)}}

@media(max-width:768px){
  .login-split{flex-direction:column;width:100%;max-width:460px;min-height:auto;}
  .login-left{padding:32px 28px;min-height:200px;}
  .ll-features{display:none;}
  .login-right{width:100%;padding:32px 28px;}
  .demo-accounts{grid-template-columns:1fr;}
}

/* ═══════ EMPLOYEE PUNCH CARDS ═══════ */
.punch-emp-card {
  background: var(--sur);
  border: 2px solid var(--bdr);
  border-radius: var(--r);
  padding: 18px 16px;
  transition: all .2s cubic-bezier(.34,1.2,.64,1);
  position: relative;
  overflow: hidden;
  box-shadow: var(--sh1);
}
.punch-emp-card::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 3px;
  border-radius: var(--r) var(--r) 0 0;
  background: var(--bdr2);
  transition: background .2s;
}
.punch-emp-card.punched-in {
  border-color: rgba(5,150,105,.35);
  box-shadow: 0 4px 20px rgba(5,150,105,.12), var(--sh1);
}
.punch-emp-card.punched-in::before { background: linear-gradient(90deg,var(--grn),var(--grnm)); }
.punch-emp-card.punched-out {
  border-color: rgba(217,119,6,.25);
  background: var(--sur2);
}
.punch-emp-card.punched-out::before { background: linear-gradient(90deg,var(--amb),var(--ambm)); }
.punch-emp-card:hover { transform: translateY(-3px); box-shadow: var(--sh3); }

/* Pulse ring when actively punched in */
@keyframes ping {
  0% { transform: scale(1); opacity: .6; }
  100% { transform: scale(1.55); opacity: 0; }
}
.status-dot {
  width: 9px; height: 9px; border-radius: 50%;
  display: inline-block; flex-shrink: 0;
  position: relative;
}
.status-dot.active { background: var(--grn); }
.status-dot.active::after {
  content: ''; position: absolute; inset: 0;
  border-radius: 50%; background: var(--grn);
  animation: ping 1.4s ease-out infinite;
}
.status-dot.done { background: var(--amb); }
.status-dot.idle { background: var(--bdr2); }

/* Punch action button inside card */
.emp-punch-btn {
  width: 100%; padding: 9px 12px;
  border: none; border-radius: var(--rsm);
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12.5px; font-weight: 700;
  cursor: pointer;
  transition: all .18s;
  display: flex; align-items: center; justify-content: center; gap: 6px;
}
.emp-punch-btn.btn-in {
  background: linear-gradient(135deg,var(--grn),var(--grnm));
  color: #fff;
  box-shadow: 0 3px 10px rgba(5,150,105,.22);
}
.emp-punch-btn.btn-in:hover { transform: translateY(-1px); box-shadow: 0 5px 16px rgba(5,150,105,.32); }
.emp-punch-btn.btn-out {
  background: linear-gradient(135deg,var(--red),var(--redm));
  color: #fff;
  box-shadow: 0 3px 10px rgba(225,29,72,.2);
}
.emp-punch-btn.btn-out:hover { transform: translateY(-1px); box-shadow: 0 5px 16px rgba(225,29,72,.3); }
.emp-punch-btn.btn-break {
  background: var(--ambl);
  color: var(--amb);
  border: 1.5px solid rgba(217,119,6,.25);
}
.emp-punch-btn.btn-break:hover { background: var(--ambm); color: #fff; }

/* Timer display inside card */
.card-timer {
  font-family: 'Playfair Display', serif;
  font-size: 22px; font-weight: 800;
  letter-spacing: -0.5px;
  background: linear-gradient(135deg,var(--pri),var(--vio));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.card-timer.counting { color: var(--grn); }

/* Punch confirm overlay */
.punch-confirm-overlay {
  display: none;
  position: fixed; inset: 0; z-index: 500;
  background: rgba(26,23,20,.5);
  backdrop-filter: blur(6px);
  align-items: center; justify-content: center;
}
.punch-confirm-overlay.open { display: flex; }
.punch-confirm-box {
  background: var(--sur);
  border: 1px solid var(--bdr);
  border-radius: 20px;
  padding: 32px 36px;
  width: 440px; max-width: 92vw;
  text-align: center;
  box-shadow: var(--sh4);
  animation: mIn .24s cubic-bezier(.34,1.56,.64,1);
}
.punch-confirm-avatar {
  width: 72px; height: 72px; border-radius: 50%;
  margin: 0 auto 14px;
  display: flex; align-items: center; justify-content: center;
  font-weight: 800; font-size: 26px; color: #fff;
  box-shadow: 0 8px 24px rgba(0,0,0,.15);
}
.punch-confirm-name {
  font-family: 'Playfair Display', serif;
  font-size: 22px; font-weight: 700;
  margin-bottom: 4px;
}
.punch-confirm-time {
  font-family: 'Playfair Display', serif;
  font-size: 42px; font-weight: 800;
  letter-spacing: -1.5px;
  margin: 14px 0;
}
.punch-confirm-time.time-in { color: var(--grn); }
.punch-confirm-time.time-out { color: var(--red); }
.punch-confirm-actions { display: flex; gap: 10px; margin-top: 20px; }


@media print {
  body > * { display: none !important; }
  #payslipPrintWrap { display: block !important; position: fixed; inset: 0; z-index: 99999; background: #fff; padding: 20px; }
  #payslipPrintWrap * { -webkit-print-color-adjust: exact; print-color-adjust: exact; color-adjust: exact; }
}
#payslipPrintWrap { display: none; }
</style>
</head>
<body>
<div id="payslipPrintWrap"></div>

<!-- ══════════════════════════════════════════════════
     LOGIN SCREEN
══════════════════════════════════════════════════ -->
<div id="loginScreen">
  <div class="login-split">
    <div class="login-left">
      <div class="ll-logo">
        <div class="ll-logo-icon">⏱</div>
        <div class="ll-logo-txt">Attend<span>Flow</span></div>
      </div>
      <div class="ll-content">
        <div class="ll-heading">Workforce<br>Intelligence<br>Platform</div>
        <div class="ll-sub">Manage attendance, payroll, leaves and shifts — all in one beautiful dashboard built for modern teams.</div>
        <div class="ll-features">
          <div class="ll-feat"><div class="ll-feat-ic">📊</div><span>Real-time attendance tracking</span></div>
          <div class="ll-feat"><div class="ll-feat-ic">💰</div><span>Automated payroll processing</span></div>
          <div class="ll-feat"><div class="ll-feat-ic">🌴</div><span>Smart leave management</span></div>
          <div class="ll-feat"><div class="ll-feat-ic">🔄</div><span>Flexible shift scheduling</span></div>
          <div class="ll-feat"><div class="ll-feat-ic">📈</div><span>Analytics &amp; reports</span></div>
        </div>
      </div>
      <div class="ll-footer">© 2026 AttendFlow · Secure · GDPR Compliant</div>
    </div>
    <div class="login-right">
      <div class="lr-title">Welcome back 👋</div>
      <div class="lr-sub">Sign in to your AttendFlow account</div>
      <div class="login-error" id="loginError"><span>⚠️</span><span id="loginErrorMsg">Invalid username or password</span></div>
      <div class="login-form">
        <div class="lf-group">
          <label class="lf-label" for="loginUser">Username</label>
          <input class="lf-input" type="text" id="loginUser" placeholder="Enter your username" autocomplete="username" onkeydown="if(event.key==='Enter')doLogin()">
        </div>
        <div class="lf-group">
          <label class="lf-label" for="loginPass">Password</label>
          <div class="lf-pw-wrap">
            <input class="lf-input" type="password" id="loginPass" placeholder="Enter your password" autocomplete="current-password" onkeydown="if(event.key==='Enter')doLogin()">
            <button class="pw-toggle" onclick="togglePwVis()" type="button" id="pwToggleBtn">👁</button>
          </div>
        </div>
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:20px;">
          <label style="display:flex;align-items:center;gap:7px;cursor:pointer;font-size:13px;color:#4a4540;">
            <input type="checkbox" id="rememberMe" style="width:15px;height:15px;accent-color:#4f46e5;">Remember me
          </label>
          <a href="#" onclick="showForgotPw();return false;" style="font-size:12.5px;color:#4f46e5;text-decoration:none;font-weight:600;">Forgot password?</a>
        </div>
        <button class="login-btn" id="loginBtn" onclick="doLogin()">Sign In</button>
      </div>
      <div class="login-divider">Quick demo access</div>
      <div class="demo-accounts">
        <button class="demo-btn" onclick="fillDemo('admin','Admin@123')"><div class="db-role">Super Admin</div><div class="db-name">Admin User</div><div class="db-cred">admin / Admin@123</div></button>
        <button class="demo-btn" onclick="fillDemo('hrmanager','HR@2026')"><div class="db-role">HR Manager</div><div class="db-name">Ravi Kumar</div><div class="db-cred">hrmanager / HR@2026</div></button>
        <button class="demo-btn" onclick="fillDemo('manager','Mgr@2026')"><div class="db-role">Manager</div><div class="db-name">Kiran Rao</div><div class="db-cred">manager / Mgr@2026</div></button>
        <button class="demo-btn" onclick="fillDemo('employee','Emp@2026')"><div class="db-role">Employee</div><div class="db-name">Priya Patel</div><div class="db-cred">employee / Emp@2026</div></button>
      </div>
      <div class="login-footer">Protected by enterprise-grade security · Session expires in 8h</div>
    </div>
  </div>
</div>

<div id="appWrapper" style="display:none;">

<!-- ═══════════ SIDEBAR ═══════════ -->
<nav class="sidebar" id="sidebar">
  <div class="logo-wrap">
    <div class="logo-icon">⏱</div>
    <div class="logo-txt">Attend<span>Flow</span></div>
  </div>
  <div class="nav-wrap">
    <div class="nav-section">Overview</div>
    <div class="nav-item active" data-page="dashboard"><span class="ni">🏠</span>Dashboard</div>
    <div class="nav-item" data-page="attendance"><span class="ni">📋</span>Attendance</div>
    <div class="nav-item" data-page="punch"><span class="ni">⏱</span>Punch Clock</div>
    <div class="nav-item" data-page="timesheet"><span class="ni">🗓</span>Timesheet</div>
    <div class="nav-section">Management</div>
    <div class="nav-item" data-page="employees"><span class="ni">👥</span>Employees</div>
    <div class="nav-item" data-page="shifts"><span class="ni">🔄</span>Shifts</div>
    <div class="nav-item" data-page="leaves"><span class="ni">🌴</span>Leave Management <span class="nav-badge" id="leaveBadge">2</span></div>
    <div class="nav-item" data-page="salary"><span class="ni">💰</span>Payroll</div>
    <div class="nav-section">Analytics</div>
    <div class="nav-item" data-page="reports"><span class="ni">📊</span>Reports</div>
    <div class="nav-item" data-page="settings"><span class="ni">⚙️</span>Settings</div>
  </div>
  <div class="sidebar-user" onclick="openM('mProfile')">
    <div class="uav" id="topUserAv">AD</div>
    <div><div style="font-size:13px;font-weight:600;color:var(--txt)" id="topUserName">Admin User</div><div style="font-size:11px;color:var(--txt3)">Super Admin</div></div>
  </div>
</nav>

<div class="main">
<div class="topbar">
  <div class="topbar-l">
    <button class="hamb" id="hambBtn">☰</button>
    <div><div class="pg-title" id="pgTitle">Dashboard</div><div class="pg-date" id="pgDate"></div></div>
  </div>
  <div class="topbar-r">
    <span id="liveClock"></span>
    <button class="btn btn-primary btn-sm" onclick="openM('mAddEmp')">✦ Add Employee</button>
  </div>
</div>

<!-- ════════════════════ DASHBOARD ════════════════════ -->
<div class="page active" id="page-dashboard">
  <div class="card card-grad mb20" style="padding:22px 26px;position:relative;overflow:hidden;">
    <div style="position:absolute;right:24px;top:50%;transform:translateY(-50%);font-size:72px;opacity:.07;pointer-events:none">📊</div>
    <div style="max-width:480px;position:relative;z-index:1;">
      <div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:1.5px;color:rgba(255,255,255,.6);margin-bottom:5px;">Today's Overview</div>
      <div style="font-family:'Playfair Display',serif;font-size:21px;font-weight:700;color:#fff;margin-bottom:6px;" id="heroGreeting">Good morning, Admin! 👋</div>
      <div style="font-size:13px;color:rgba(255,255,255,.72);" id="heroSub">Your workforce is running smoothly today.</div>
    </div>
  </div>
  <div class="stats-grid">
    <div class="stat-card sc-grn"><div class="stat-top"><div><div class="stat-label">Present Today</div><div class="stat-val v-grn" id="ds-present">42</div></div><div class="stat-ico ico-grn">✅</div></div><div class="stat-chg chg-up">↑ 3 more than yesterday</div></div>
    <div class="stat-card sc-red"><div class="stat-top"><div><div class="stat-label">Absent Today</div><div class="stat-val v-red" id="ds-absent">8</div></div><div class="stat-ico ico-red">❌</div></div><div class="stat-chg chg-dn">↑ 2 more than yesterday</div></div>
    <div class="stat-card sc-amb"><div class="stat-top"><div><div class="stat-label">Late Arrivals</div><div class="stat-val v-amb" id="ds-late">5</div></div><div class="stat-ico ico-amb">⏰</div></div><div class="stat-chg chg-up">↓ 1 less than yesterday</div></div>
    <div class="stat-card sc-vio"><div class="stat-top"><div><div class="stat-label">On Leave</div><div class="stat-val v-vio" id="ds-leave">4</div></div><div class="stat-ico ico-vio">🌴</div></div><div class="stat-chg chg-n">Approved leaves</div></div>
  </div>
  <div class="g2 mb20">
    <div class="card">
      <div class="card-hd">
        <div><div class="card-title">Weekly Attendance</div><div class="card-sub">Present vs Absent this week</div></div>
        <select class="fc" style="width:115px;padding:5px 10px;font-size:12px;" id="weekMode" onchange="buildWeekChart()">
          <option value="both">Both</option><option value="present">Present</option><option value="absent">Absent</option>
        </select>
      </div>
      <div class="chart-box"><canvas id="weekChart"></canvas></div>
      <div class="flex gap16 mt12">
        <div class="flex aic gap4 txs tmuted"><div style="width:9px;height:9px;border-radius:3px;background:var(--pri)"></div>Present</div>
        <div class="flex aic gap4 txs tmuted"><div style="width:9px;height:9px;border-radius:3px;background:var(--red)"></div>Absent</div>
      </div>
    </div>
    <div class="card">
      <div class="card-hd"><div><div class="card-title">Today's Punch Log</div><div class="card-sub">Real-time clock-ins</div></div><span class="badge b-live">● Live</span></div>
      <div class="tbl-wrap"><table><thead><tr><th>Employee</th><th>In</th><th>Out</th><th>Status</th></tr></thead><tbody id="dashPunchLog"></tbody></table></div>
    </div>
  </div>
  <div class="g2">
    <div class="card"><div class="card-hd"><div><div class="card-title">Department Attendance</div><div class="card-sub">Today's breakdown</div></div></div><div id="dashDept"></div></div>
    <div class="card"><div class="card-hd"><div class="card-title">Recent Activity</div><span class="txs tmuted">Today</span></div><div id="dashActivity"></div></div>
  </div>
</div>

<!-- ════════════════════ ATTENDANCE ════════════════════ -->
<div class="page" id="page-attendance">
  <div class="flex aic gap12 mb16" style="flex-wrap:wrap;">
    <div class="search-box" style="flex:1;min-width:180px;"><span class="search-ic">🔍</span><input class="fc" id="attQ" placeholder="Search employee or department..." oninput="renderAtt()"></div>
    <select class="fc" style="width:145px;" id="attStatusF" onchange="renderAtt()"><option value="">All Status</option><option value="present">Present</option><option value="absent">Absent</option><option value="late">Late</option><option value="leave">On Leave</option></select>
    <select class="fc" style="width:155px;" id="attDeptF" onchange="renderAtt()"><option value="">All Departments</option><option>Engineering</option><option>Sales</option><option>HR</option><option>Finance</option><option>Design</option></select>
    <input class="fc" type="date" style="width:148px;" id="attDateF">
    <button class="btn btn-secondary btn-sm" onclick="exportAttCSV()">⬇ Export</button>
    <button class="btn btn-primary btn-sm" onclick="prepMarkAtt();openM('mMarkAtt')">+ Mark</button>
  </div>
  <div class="flex gap8 mb16" style="flex-wrap:wrap;" id="attSummaryRow"></div>
  <div class="card">
    <div class="card-hd"><div><div class="card-title">Daily Attendance Register</div><div class="card-sub" id="attSubtitle"></div></div></div>
    <div class="tbl-wrap"><table><thead><tr><th>#</th><th>Employee</th><th>Dept</th><th>Shift</th><th>Punch In</th><th>Punch Out</th><th>Hours</th><th>Status</th><th>Actions</th></tr></thead><tbody id="attTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ PUNCH CLOCK ════════════════════ -->
<div class="page" id="page-punch">

  <!-- ── TAB SWITCHER ── -->
  <div class="tab-row mb20" id="punchTabRow">
    <div class="tab active" onclick="switchPunchTab('my',this)">⏱ My Punch</div>
    <div class="tab" onclick="switchPunchTab('team',this)">👥 Team Station</div>
    <div class="tab" onclick="switchPunchTab('log',this)">📋 Today's Log</div>
  </div>

  <!-- ══ MY PUNCH TAB ══ -->
  <div id="punchTab-my">
    <div class="g2 mb20">
      <div class="punch-hero">
        <div class="ph-label">Your Work Timer</div>
        <div class="clock-big" id="punchClock">--:--:--</div>
        <div class="clock-sub" id="punchDateLbl"></div>
        <div class="punch-status" id="punchStatus">Ready to clock in</div>
        <div style="display:flex;justify-content:center;">
          <button class="pbtn" id="punchBtn" onclick="doPunch()">
            <span class="pic" id="punchIcon">▶</span>
            <span id="punchTxt" style="font-size:11px;letter-spacing:.5px">PUNCH IN</span>
          </button>
        </div>
        <div class="punch-stats-row">
          <div class="ps-item"><div class="ps-val" id="pTodayHrs">0h 0m</div><div class="ps-lbl">Today's Hours</div></div>
          <div class="ps-item"><div class="ps-val" id="pShiftStart">9:00 AM</div><div class="ps-lbl">Shift Start</div></div>
          <div class="ps-item"><div class="ps-val" id="pShiftEnd">6:00 PM</div><div class="ps-lbl">Shift End</div></div>
        </div>
      </div>
      <div class="card">
        <div class="card-hd"><div class="card-title">Today's Entries</div><span class="txs tmuted" id="punchEntryCnt">0 entries</span></div>
        <div id="punchLogEl"><div class="empty"><div class="ei">⏱</div><p>No punch entries yet today</p></div></div>
        <div id="punchSummaryBox" style="display:none;"></div>
      </div>
    </div>
    <div class="g2">
      <div class="card">
        <div class="card-hd"><div class="card-title">This Week</div><span class="fw6 tgrn txs" id="weekTotalLbl"></span></div>
        <table><thead><tr><th>Day</th><th>In</th><th>Out</th><th>Hours</th><th>Status</th></tr></thead><tbody id="weekTbl"></tbody></table>
      </div>
      <div class="card">
        <div class="card-hd"><div class="card-title">Attendance Calendar</div><span class="txs tmuted" id="calMonthLbl"></span></div>
        <div id="myCalendar"></div>
        <div class="flex gap12 mt12" style="flex-wrap:wrap;">
          <div class="flex aic gap4 txxs tmuted"><span style="display:inline-block;width:8px;height:8px;border-radius:2px;background:var(--grnl);border:1px solid var(--grn)"></span>Present</div>
          <div class="flex aic gap4 txxs tmuted"><span style="display:inline-block;width:8px;height:8px;border-radius:2px;background:var(--redl);border:1px solid var(--red)"></span>Absent</div>
          <div class="flex aic gap4 txxs tmuted"><span style="display:inline-block;width:8px;height:8px;border-radius:2px;background:var(--viol);border:1px solid var(--vio)"></span>Leave</div>
          <div class="flex aic gap4 txxs tmuted"><span style="display:inline-block;width:8px;height:8px;border-radius:2px;background:var(--ambl);border:1px solid var(--amb)"></span>Holiday</div>
        </div>
      </div>
    </div>
  </div>

  <!-- ══ TEAM PUNCH STATION TAB ══ -->
  <div id="punchTab-team" style="display:none;">
    <!-- Live clock banner -->
    <div style="background:linear-gradient(135deg,var(--pri) 0%,var(--vio) 100%);border-radius:var(--r);padding:20px 26px;margin-bottom:20px;display:flex;align-items:center;justify-content:space-between;box-shadow:0 8px 28px rgba(79,70,229,.22);">
      <div>
        <div style="font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:1.6px;color:rgba(255,255,255,.6);margin-bottom:4px;">Team Punch Station</div>
        <div style="font-family:'Playfair Display',serif;font-size:18px;font-weight:700;color:#fff;" id="teamStationTitle">Click any employee card to punch in or out</div>
      </div>
      <div style="text-align:right;">
        <div style="font-family:'Playfair Display',serif;font-size:28px;font-weight:800;color:#fff;letter-spacing:-1px;" id="teamStationClock">--:--:--</div>
        <div style="font-size:11px;color:rgba(255,255,255,.65);" id="teamStationDate"></div>
      </div>
    </div>

    <!-- Search + Filter bar -->
    <div class="flex aic gap12 mb20" style="flex-wrap:wrap;">
      <div class="search-box" style="flex:1;min-width:200px;">
        <span class="search-ic">🔍</span>
        <input class="fc" id="teamPunchQ" placeholder="Search employee..." oninput="renderTeamPunchCards()">
      </div>
      <select class="fc" style="width:160px;" id="teamPunchDept" onchange="renderTeamPunchCards()">
        <option value="">All Departments</option>
        <option>Engineering</option><option>Sales</option><option>HR</option><option>Finance</option><option>Design</option>
      </select>
      <select class="fc" style="width:150px;" id="teamPunchShift" onchange="renderTeamPunchCards()">
        <option value="">All Shifts</option>
        <option>Morning</option><option>Evening</option><option>Night</option><option>Flexible</option>
      </select>
      <div class="flex gap8">
        <button class="btn btn-secondary btn-sm" onclick="punchAllIn()" title="Punch In all visible employees">⬇ All In</button>
        <button class="btn btn-danger btn-sm" onclick="punchAllOut()" title="Punch Out all active employees">⬆ All Out</button>
      </div>
    </div>

    <!-- Stats strip -->
    <div class="flex gap12 mb20" style="flex-wrap:wrap;" id="teamPunchStats"></div>

    <!-- Employee Punch Cards Grid -->
    <div id="teamPunchGrid" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:14px;"></div>
  </div>

  <!-- ══ TODAY'S LOG TAB ══ -->
  <div id="punchTab-log" style="display:none;">
    <div class="flex aic gap12 mb16" style="flex-wrap:wrap;">
      <div class="search-box" style="flex:1;min-width:200px;">
        <span class="search-ic">🔍</span>
        <input class="fc" id="punchLogQ" placeholder="Search employee..." oninput="renderPunchLogTable()">
      </div>
      <select class="fc" style="width:150px;" id="punchLogStatus" onchange="renderPunchLogTable()">
        <option value="">All Status</option>
        <option value="in">Punched In</option>
        <option value="out">Punched Out</option>
        <option value="absent">Absent</option>
      </select>
      <button class="btn btn-secondary btn-sm" onclick="exportPunchLogCSV()">⬇ Export CSV</button>
      <button class="btn btn-primary btn-sm" onclick="refreshPunchLog()">↻ Refresh</button>
    </div>

    <!-- Summary cards -->
    <div class="stats-grid mb20" style="grid-template-columns:repeat(4,1fr);">
      <div class="stat-card sc-grn"><div class="stat-top"><div><div class="stat-label">Punched In</div><div class="stat-val v-grn" id="plPunchedIn">0</div></div><div class="stat-ico ico-grn">✅</div></div><div class="stat-chg chg-n">Currently working</div></div>
      <div class="stat-card sc-amb"><div class="stat-top"><div><div class="stat-label">Punched Out</div><div class="stat-val v-amb" id="plPunchedOut">0</div></div><div class="stat-ico ico-amb">🏁</div></div><div class="stat-chg chg-n">Completed today</div></div>
      <div class="stat-card sc-red"><div class="stat-top"><div><div class="stat-label">Not Punched</div><div class="stat-val v-red" id="plNotPunched">0</div></div><div class="stat-ico ico-red">⏳</div></div><div class="stat-chg chg-n">No activity yet</div></div>
      <div class="stat-card sc-pri"><div class="stat-top"><div><div class="stat-label">Total Hours</div><div class="stat-val v-pri" id="plTotalHrs">0h</div></div><div class="stat-ico ico-pri">⏱</div></div><div class="stat-chg chg-n">Team combined</div></div>
    </div>

    <div class="card">
      <div class="card-hd">
        <div><div class="card-title">Today's Punch Activity Log</div><div class="card-sub" id="punchLogSubtitle"></div></div>
        <div class="flex gap8">
          <span class="badge b-live">● Live</span>
        </div>
      </div>
      <div class="tbl-wrap">
        <table>
          <thead><tr><th>#</th><th>Employee</th><th>Dept</th><th>Shift</th><th>Punch In</th><th>Punch Out</th><th>Duration</th><th>Sessions</th><th>Status</th></tr></thead>
          <tbody id="punchLogTbl"></tbody>
        </table>
      </div>
    </div>
  </div>

</div>

<!-- ════════════════════ TIMESHEET ════════════════════ -->
<div class="page" id="page-timesheet">
  <div class="g2 mb20">
    <div class="card">
      <div class="card-title mb16">📝 Log Time Entry</div>
      <div class="fg"><label class="flabel">Project</label>
        <select class="fc" id="tsProj"><option>Website Redesign</option><option>Mobile App</option><option>API Integration</option><option>QA Testing</option><option>Data Migration</option><option>Admin Panel</option></select>
      </div>
      <div class="fg"><label class="flabel">Activity Description</label><input class="fc" id="tsAct" placeholder="What did you work on?"></div>
      <div class="g2" style="gap:10px;">
        <div class="fg"><label class="flabel">Date</label><input class="fc" id="tsDate" type="date"></div>
        <div class="fg"><label class="flabel">Billable?</label>
          <select class="fc" id="tsBillable"><option value="yes">Yes</option><option value="no">No</option></select>
        </div>
      </div>
      <div class="g2 mb12" style="gap:10px;">
        <div class="fg" style="margin:0"><label class="flabel">Start Time</label><input class="fc" id="tsStart" type="time" value="09:00"></div>
        <div class="fg" style="margin:0"><label class="flabel">End Time</label><input class="fc" id="tsEnd" type="time" value="17:00"></div>
      </div>
      <div id="tsDurPreview" class="txs tmuted mb12" style="min-height:17px;"></div>
      <button class="btn btn-primary w100" onclick="addTsEntry()">+ Log Entry</button>
    </div>
    <div class="card">
      <div class="card-title mb16">📊 Project Breakdown</div>
      <div id="tsProjBreak"><div class="empty" style="padding:20px 0"><div class="ei" style="font-size:28px">📋</div><p>No entries yet</p></div></div>
      <div class="divider"></div>
      <div class="flex jb mb12"><span class="tsm tmuted">Total hours this month</span><span class="fw7 tgrn" id="tsTotalHrs">—</span></div>
      <div class="flex jb mb12"><span class="tsm tmuted">Billable hours</span><span class="fw7 tpri" id="tsBillableHrs">—</span></div>
      <div class="flex jb"><span class="tsm tmuted">Days logged</span><span class="fw7" id="tsDaysLogged">—</span></div>
    </div>
  </div>
  <div class="card">
    <div class="card-hd">
      <div><div class="card-title">Monthly Timesheet</div><div class="card-sub" id="tsEntryCount"></div></div>
      <div class="flex gap8">
        <select class="fc" style="width:145px;padding:6px 10px;font-size:12px;" id="tsProjFilter" onchange="renderTimesheet()"><option value="">All Projects</option><option>Website Redesign</option><option>Mobile App</option><option>API Integration</option><option>QA Testing</option></select>
        <button class="btn btn-secondary btn-sm" onclick="exportTsCSV()">⬇ Export</button>
        <button class="btn btn-danger btn-sm" onclick="clearTs()">🗑 Clear</button>
      </div>
    </div>
    <div class="tbl-wrap"><table><thead><tr><th>Date</th><th>Project</th><th>Activity</th><th>Start</th><th>End</th><th>Duration</th><th>Billable</th><th>Action</th></tr></thead><tbody id="tsTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ EMPLOYEES ════════════════════ -->
<div class="page" id="page-employees">
  <div class="flex aic gap12 mb16" style="flex-wrap:wrap;">
    <div class="search-box" style="flex:1;min-width:180px;"><span class="search-ic">🔍</span><input class="fc" id="empQ" placeholder="Search name, ID, department, designation..." oninput="renderEmps()"></div>
    <select class="fc" style="width:160px;" id="empDeptF" onchange="renderEmps()"><option value="">All Departments</option><option>Engineering</option><option>Sales</option><option>HR</option><option>Finance</option><option>Design</option></select>
    <select class="fc" style="width:140px;" id="empStatusF" onchange="renderEmps()"><option value="">All Status</option><option>Active</option><option>Inactive</option></select>
    <select class="fc" style="width:140px;" id="empShiftF" onchange="renderEmps()"><option value="">All Shifts</option><option>Morning</option><option>Evening</option><option>Night</option><option>Flexible</option></select>
    <button class="btn btn-primary btn-sm" onclick="prepAddEmp();openM('mAddEmp')">✦ Add Employee</button>
  </div>
  <div class="flex aic mb16 gap12">
    <span class="tsm tmuted" id="empCountLbl"></span>
    <div class="flex gap8 mla">
      <button class="btn btn-secondary btn-xs" id="btnGrid" onclick="setEmpView('grid')">⊞ Grid</button>
      <button class="btn btn-secondary btn-xs" id="btnList" onclick="setEmpView('list')">☰ List</button>
      <button class="btn btn-secondary btn-xs" onclick="exportEmpCSV()">⬇ Export</button>
    </div>
  </div>
  <div id="empGrid" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(252px,1fr));gap:14px;"></div>
  <div id="empList" style="display:none;" class="card">
    <div class="tbl-wrap"><table><thead><tr><th>#</th><th>Employee</th><th>ID</th><th>Dept</th><th>Shift</th><th>Salary</th><th>Status</th><th>Actions</th></tr></thead><tbody id="empListTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ SHIFTS ════════════════════ -->
<div class="page" id="page-shifts">
  <div class="flex aic mb20 gap12">
    <div><div class="tsm" style="color:var(--txt2)">Manage work shifts and employee assignments</div></div>
    <div class="flex gap8 mla">
      <button class="btn btn-secondary btn-sm" onclick="openM('mAssignShift')">🔄 Bulk Assign</button>
      <button class="btn btn-primary btn-sm" onclick="prepAddShift();openM('mAddShift')">+ Create Shift</button>
    </div>
  </div>
  <div class="g3 mb20" id="shiftCards"></div>
  <div class="card">
    <div class="card-hd">
      <div><div class="card-title">Employee Shift Assignments</div><div class="card-sub">Current shift allocations</div></div>
      <div class="flex gap8">
        <div class="search-box"><span class="search-ic">🔍</span><input class="fc" id="shiftEmpQ" placeholder="Filter employees..." oninput="renderShiftAssign()" style="width:180px;"></div>
      </div>
    </div>
    <div class="tbl-wrap"><table><thead><tr><th>Employee</th><th>Department</th><th>Current Shift</th><th>Start</th><th>End</th><th>Actions</th></tr></thead><tbody id="shiftAssignTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ LEAVES ════════════════════ -->
<div class="page" id="page-leaves">
  <div class="stats-grid mb20">
    <div class="stat-card sc-pri"><div class="stat-top"><div><div class="stat-label">Total Requests</div><div class="stat-val v-pri" id="lv-total">0</div></div><div class="stat-ico ico-pri">📋</div></div><div class="stat-chg chg-n">This month</div></div>
    <div class="stat-card sc-amb"><div class="stat-top"><div><div class="stat-label">Pending</div><div class="stat-val v-amb" id="lv-pending">0</div></div><div class="stat-ico ico-amb">⏳</div></div><div class="stat-chg chg-n">Awaiting approval</div></div>
    <div class="stat-card sc-grn"><div class="stat-top"><div><div class="stat-label">Approved</div><div class="stat-val v-grn" id="lv-approved">0</div></div><div class="stat-ico ico-grn">✅</div></div><div class="stat-chg chg-up">This month</div></div>
    <div class="stat-card sc-red"><div class="stat-top"><div><div class="stat-label">Rejected</div><div class="stat-val v-red" id="lv-rejected">0</div></div><div class="stat-ico ico-red">❌</div></div><div class="stat-chg chg-n">This month</div></div>
  </div>
  <div class="flex aic mb16 gap12" style="flex-wrap:wrap;">
    <div class="tab-row" id="lvTabRow">
      <div class="tab active" onclick="switchLeaveTab('all',this)">All</div>
      <div class="tab" onclick="switchLeaveTab('pending',this)">Pending</div>
      <div class="tab" onclick="switchLeaveTab('approved',this)">Approved</div>
      <div class="tab" onclick="switchLeaveTab('rejected',this)">Rejected</div>
    </div>
    <div class="flex gap8 mla">
      <div class="search-box"><span class="search-ic">🔍</span><input class="fc" id="lvQ" placeholder="Search employee..." oninput="renderLeaves(lvFilter)" style="width:180px;"></div>
      <button class="btn btn-primary btn-sm" onclick="prepLeaveModal();openM('mApplyLeave')">+ Apply Leave</button>
    </div>
  </div>
  <div class="card">
    <div class="card-hd"><div class="card-title">Leave Requests</div><button class="btn btn-secondary btn-sm" onclick="exportLeavesCSV()">⬇ Export</button></div>
    <div class="tbl-wrap"><table><thead><tr><th>Employee</th><th>Type</th><th>From</th><th>To</th><th>Days</th><th>Reason</th><th>Status</th><th>Actions</th></tr></thead><tbody id="leaveTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ PAYROLL ════════════════════ -->
<div class="page" id="page-salary">
  <div class="stats-grid mb20">
    <div class="stat-card sc-grn"><div class="stat-top"><div><div class="stat-label">Total Payroll</div><div class="stat-val v-grn" id="pay-total">₹0</div></div><div class="stat-ico ico-grn">💰</div></div><div class="stat-chg chg-n" id="pay-month">Current Month</div></div>
    <div class="stat-card sc-pri"><div class="stat-top"><div><div class="stat-label">Processed</div><div class="stat-val v-pri" id="pay-done">0</div></div><div class="stat-ico ico-pri">✅</div></div><div class="stat-chg chg-up" id="pay-donePct"></div></div>
    <div class="stat-card sc-amb"><div class="stat-top"><div><div class="stat-label">Pending</div><div class="stat-val v-amb" id="pay-pend">0</div></div><div class="stat-ico ico-amb">⏳</div></div><div class="stat-chg chg-n">To process</div></div>
    <div class="stat-card sc-red"><div class="stat-top"><div><div class="stat-label">Deductions</div><div class="stat-val v-red" id="pay-deduct">₹0</div></div><div class="stat-ico ico-red">📉</div></div><div class="stat-chg chg-n">Absences & late</div></div>
  </div>
  <div class="card">
    <div class="card-hd">
      <div><div class="card-title">Salary Register</div></div>
      <div class="flex gap8">
        <select class="fc" style="width:150px;padding:7px 10px;font-size:12px;" id="payMonthSel" onchange="renderSalary()"><option>March 2026</option><option>February 2026</option><option>January 2026</option></select>
        <select class="fc" style="width:140px;padding:7px 10px;font-size:12px;" id="payDeptF" onchange="renderSalary()"><option value="">All Departments</option><option>Engineering</option><option>Sales</option><option>HR</option><option>Finance</option><option>Design</option></select>
        <button class="btn btn-secondary btn-sm" onclick="exportSalaryCSV()">⬇ Export</button>
        <button class="btn btn-primary btn-sm" onclick="processAllSalary()">⚡ Process All</button>
      </div>
    </div>
    <div class="tbl-wrap"><table><thead><tr><th>Employee</th><th>Dept</th><th>Base Salary</th><th>Present Days</th><th>Deductions</th><th>OT Bonus</th><th>Net Salary</th><th>Status</th><th>Actions</th></tr></thead><tbody id="salTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ REPORTS ════════════════════ -->
<div class="page" id="page-reports">
  <div class="g3 mb20">
    <div class="card" style="cursor:pointer;" onclick="genReport('Attendance')" onmouseenter="this.style.borderColor='var(--pri)';this.style.boxShadow='0 8px 24px rgba(79,70,229,.1)'" onmouseleave="this.style.borderColor='var(--bdr)';this.style.boxShadow=''">
      <div style="width:46px;height:46px;background:var(--prix);border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:21px;margin-bottom:13px;">📊</div>
      <div class="card-title">Attendance Report</div><div class="card-sub mb12">Daily, weekly, monthly breakdown</div>
      <button class="btn btn-secondary w100 btn-sm">Generate Report</button>
    </div>
    <div class="card" style="cursor:pointer;" onclick="genReport('Payroll')" onmouseenter="this.style.borderColor='var(--grn)';this.style.boxShadow='0 8px 24px rgba(5,150,105,.1)'" onmouseleave="this.style.borderColor='var(--bdr)';this.style.boxShadow=''">
      <div style="width:46px;height:46px;background:var(--grnl);border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:21px;margin-bottom:13px;">💰</div>
      <div class="card-title">Payroll Report</div><div class="card-sub mb12">Salary & deductions summary</div>
      <button class="btn btn-secondary w100 btn-sm">Generate Report</button>
    </div>
    <div class="card" style="cursor:pointer;" onclick="genReport('Overtime')" onmouseenter="this.style.borderColor='var(--amb)';this.style.boxShadow='0 8px 24px rgba(217,119,6,.1)'" onmouseleave="this.style.borderColor='var(--bdr)';this.style.boxShadow=''">
      <div style="width:46px;height:46px;background:var(--ambl);border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:21px;margin-bottom:13px;">⏱</div>
      <div class="card-title">Overtime Report</div><div class="card-sub mb12">OT hours & bonus calculation</div>
      <button class="btn btn-secondary w100 btn-sm">Generate Report</button>
    </div>
  </div>
  <div class="g2 mb20">
    <div class="card"><div class="card-hd"><div><div class="card-title">Attendance by Department</div><div class="card-sub">Employee distribution</div></div></div><div class="chart-box"><canvas id="rptDeptChart"></canvas></div></div>
    <div class="card"><div class="card-hd"><div><div class="card-title">Monthly Attendance Trend</div><div class="card-sub">Present vs Absent</div></div></div><div class="chart-box"><canvas id="rptTrendChart"></canvas></div></div>
  </div>
  <div class="card">
    <div class="card-hd">
      <div><div class="card-title">Monthly Attendance Overview</div></div>
      <div class="flex gap8">
        <select class="fc" style="width:140px;padding:6px 10px;font-size:12px;" id="rptMonth" onchange="renderReports()"><option>March 2026</option><option>February 2026</option><option>January 2026</option></select>
        <select class="fc" style="width:150px;padding:6px 10px;font-size:12px;" id="rptDeptF" onchange="renderReports()"><option value="">All Departments</option><option>Engineering</option><option>Sales</option><option>HR</option><option>Finance</option><option>Design</option></select>
        <button class="btn btn-secondary btn-sm" onclick="exportReportCSV()">⬇ Export</button>
      </div>
    </div>
    <div class="tbl-wrap"><table><thead><tr><th>Employee</th><th>Dept</th><th>Present</th><th>Absent</th><th>Late</th><th>OT Hours</th><th>Leave</th><th>Attendance %</th></tr></thead><tbody id="rptTbl"></tbody></table></div>
  </div>
</div>

<!-- ════════════════════ SETTINGS ════════════════════ -->
<div class="page" id="page-settings">
  <div class="tab-row mb20" id="settTabRow">
    <div class="tab active" onclick="switchSettTab('company',this)">🏢 Company</div>
    <div class="tab" onclick="switchSettTab('notifications',this)">🔔 Notifications</div>
    <div class="tab" onclick="switchSettTab('security',this)">🔑 Security</div>
    <div class="tab" onclick="switchSettTab('data',this)">📤 Data</div>
  </div>
  <div id="settSection-company">
    <div class="g2">
      <div class="card">
        <div class="card-title mb16">Company Information</div>
        <div class="fg"><label class="flabel">Company Name</label><input class="fc" id="sCompany" value="Acme Corp Pvt. Ltd."></div>
        <div class="fg"><label class="flabel">Industry</label><select class="fc" id="sIndustry"><option>Technology</option><option>Finance</option><option>Healthcare</option><option>Retail</option><option>Manufacturing</option></select></div>
        <div class="fg"><label class="flabel">Company Email</label><input class="fc" id="sCompEmail" type="email" value="hr@acmecorp.com"></div>
        <div class="fg"><label class="flabel">Phone</label><input class="fc" id="sCompPhone" value="+91 22 4567 8900"></div>
        <div class="fg"><label class="flabel">Address</label><textarea class="fc" id="sAddress" rows="2">123, Tech Park, Pune, Maharashtra 411001</textarea></div>
        <button class="btn btn-primary" onclick="saveCompanySettings()">💾 Save Company Info</button>
      </div>
      <div class="card">
        <div class="card-title mb16">Attendance Policy</div>
        <div class="fg"><label class="flabel">Working Days</label><select class="fc" id="sWorkDays"><option>Monday to Friday</option><option>Monday to Saturday</option><option>All 7 Days</option></select></div>
        <div class="g2" style="gap:10px;">
          <div class="fg"><label class="flabel">Work Hours / Day</label><input class="fc" id="sWorkHrs" type="number" value="8" min="1" max="24"></div>
          <div class="fg"><label class="flabel">Late Grace (mins)</label><input class="fc" id="sGrace" type="number" value="15" min="0" max="60"></div>
        </div>
        <div class="fg"><label class="flabel">OT Calculation After (hours)</label><input class="fc" id="sOT" type="number" value="8" min="1" max="24"></div>
        <div class="fg"><label class="flabel">Currency</label><select class="fc" id="sCurrency"><option value="INR">₹ INR — Indian Rupee</option><option value="USD">$ USD — US Dollar</option><option value="EUR">€ EUR — Euro</option></select></div>
        <div class="fg"><label class="flabel">Financial Year Start</label><select class="fc"><option>April (India)</option><option>January</option><option>July</option></select></div>
        <button class="btn btn-primary" onclick="saveAttendancePolicy()">💾 Save Policy</button>
      </div>
    </div>
  </div>
  <div id="settSection-notifications" style="display:none;">
    <div class="g2">
      <div class="card">
        <div class="card-title mb16">Email Notifications</div>
        <div style="display:flex;flex-direction:column;gap:14px;" id="notifSettings">
          <div class="flex jb aic"><div><div class="fw6 tsm">Punch Notifications</div><div class="txs tmuted">Alert on every employee punch</div></div><label class="toggle"><input type="checkbox" checked id="nPunch"><span class="tslider"></span></label></div>
          <div class="flex jb aic"><div><div class="fw6 tsm">Absence Alerts</div><div class="txs tmuted">Alert when employee is absent</div></div><label class="toggle"><input type="checkbox" checked id="nAbsence"><span class="tslider"></span></label></div>
          <div class="flex jb aic"><div><div class="fw6 tsm">Late Arrival Alerts</div><div class="txs tmuted">Alert for late arrivals</div></div><label class="toggle"><input type="checkbox" id="nLate"><span class="tslider"></span></label></div>
          <div class="flex jb aic"><div><div class="fw6 tsm">Weekly Report Email</div><div class="txs tmuted">Send weekly summary report</div></div><label class="toggle"><input type="checkbox" checked id="nWeekly"><span class="tslider"></span></label></div>
          <div class="flex jb aic"><div><div class="fw6 tsm">Leave Request Alerts</div><div class="txs tmuted">Notify on new leave requests</div></div><label class="toggle"><input type="checkbox" checked id="nLeave"><span class="tslider"></span></label></div>
          <div class="flex jb aic"><div><div class="fw6 tsm">Payroll Reminders</div><div class="txs tmuted">Monthly payroll processing reminder</div></div><label class="toggle"><input type="checkbox" checked id="nPayroll"><span class="tslider"></span></label></div>
        </div>
        <div class="mt16"><button class="btn btn-primary" onclick="saveNotifSettings()">💾 Save Preferences</button></div>
      </div>
      <div class="card">
        <div class="card-title mb16">Notification Schedule</div>
        <div class="fg"><label class="flabel">Daily Report Time</label><input class="fc" type="time" value="18:00"></div>
        <div class="fg"><label class="flabel">Weekly Report Day</label><select class="fc"><option>Monday</option><option>Friday</option><option>Sunday</option></select></div>
        <div class="fg"><label class="flabel">Absence Alert Time</label><input class="fc" type="time" value="10:30"></div>
        <div class="fg"><label class="flabel">Notify Email</label><input class="fc" type="email" value="admin@acmecorp.com"></div>
        <button class="btn btn-secondary w100" onclick="toast('Schedule saved','success')">Save Schedule</button>
      </div>
    </div>
  </div>
  <div id="settSection-security" style="display:none;">
    <div class="g2">
      <div class="card">
        <div class="card-title mb16">Admin Profile</div>
        <div class="fg"><label class="flabel">Full Name</label><input class="fc" id="sAdminName" value="Admin User"></div>
        <div class="fg"><label class="flabel">Email</label><input class="fc" id="sAdminEmail" type="email" value="admin@acmecorp.com"></div>
        <div class="fg"><label class="flabel">Phone</label><input class="fc" id="sAdminPhone" value="+91 9876543200"></div>
        <div class="fg"><label class="flabel">Role</label><input class="fc" value="Super Admin" disabled></div>
        <button class="btn btn-primary" onclick="saveProfile()">💾 Update Profile</button>
      </div>
      <div class="card">
        <div class="card-title mb16">Change Password</div>
        <div class="fg"><label class="flabel">Current Password</label><input class="fc" type="password" id="sPwd1" placeholder="Enter current password"></div>
        <div class="fg"><label class="flabel">New Password</label><input class="fc" type="password" id="sPwd2" placeholder="Min. 8 characters"></div>
        <div class="fg"><label class="flabel">Confirm New Password</label><input class="fc" type="password" id="sPwd3" placeholder="Repeat new password"></div>
        <div id="pwdStrength" class="txs mb12" style="min-height:16px;"></div>
        <button class="btn btn-primary" onclick="changePassword()">🔑 Change Password</button>
      </div>
    </div>
  </div>
  <div id="settSection-data" style="display:none;">
    <div class="g2">
      <div class="card">
        <div class="card-title mb16">Data Export</div>
        <div style="display:flex;flex-direction:column;gap:10px;">
          <div style="padding:14px;background:var(--sur2);border-radius:var(--rsm);border:1px solid var(--bdr);display:flex;justify-content:space-between;align-items:center;">
            <div><div class="fw6 tsm">Export Employees</div><div class="txs tmuted">All employee records as CSV</div></div>
            <button class="btn btn-secondary btn-sm" onclick="exportEmpCSV()">Export</button>
          </div>
          <div style="padding:14px;background:var(--sur2);border-radius:var(--rsm);border:1px solid var(--bdr);display:flex;justify-content:space-between;align-items:center;">
            <div><div class="fw6 tsm">Export Attendance</div><div class="txs tmuted">All attendance records</div></div>
            <button class="btn btn-secondary btn-sm" onclick="exportAttCSV()">Export</button>
          </div>
          <div style="padding:14px;background:var(--sur2);border-radius:var(--rsm);border:1px solid var(--bdr);display:flex;justify-content:space-between;align-items:center;">
            <div><div class="fw6 tsm">Export Payroll</div><div class="txs tmuted">All salary records</div></div>
            <button class="btn btn-secondary btn-sm" onclick="exportSalaryCSV()">Export</button>
          </div>
          <div style="padding:14px;background:var(--sur2);border-radius:var(--rsm);border:1px solid var(--bdr);display:flex;justify-content:space-between;align-items:center;">
            <div><div class="fw6 tsm">Full System Backup</div><div class="txs tmuted">Complete database backup</div></div>
            <button class="btn btn-primary btn-sm" onclick="toast('Backup created! ✅','success')">Backup Now</button>
          </div>
        </div>
      </div>
      <div class="card">
        <div class="card-title mb16">Danger Zone</div>
        <div class="info-box warning mb16">⚠️ These actions are irreversible. Please proceed with caution.</div>
        <div style="display:flex;flex-direction:column;gap:10px;">
          <div style="padding:14px;background:var(--redl);border-radius:var(--rsm);border:1px solid rgba(225,29,72,.2);display:flex;justify-content:space-between;align-items:center;">
            <div><div class="fw6 tsm tred">Clear Attendance Data</div><div class="txs tmuted">Remove all attendance records</div></div>
            <button class="btn btn-danger btn-sm" onclick="if(confirm('Clear ALL attendance data? Cannot be undone!'))ATT=[...ATT_BACKUP];toast('Attendance cleared','error')">Clear</button>
          </div>
          <div style="padding:14px;background:var(--redl);border-radius:var(--rsm);border:1px solid rgba(225,29,72,.2);display:flex;justify-content:space-between;align-items:center;">
            <div><div class="fw6 tsm tred">Reset All Data</div><div class="txs tmuted">Permanently delete everything</div></div>
            <button class="btn btn-danger btn-sm" onclick="if(confirm('Reset ALL data? This cannot be undone!'))toast('Data reset!','error')">Reset</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

</div><!-- /main -->
<!-- ══ MODAL: ADD / EDIT EMPLOYEE ══ -->
<div class="overlay" id="mAddEmp">
  <div class="modal">
    <div class="mhd"><div class="mtitle" id="mAddEmpTitle">Add New Employee</div><button class="mclose" onclick="closeM('mAddEmp')">✕</button></div>
    <div class="g2">
      <div class="fg"><label class="flabel">First Name *</label><input class="fc" id="eFirst" placeholder="John"><span class="field-error" id="eFirstErr"></span></div>
      <div class="fg"><label class="flabel">Last Name *</label><input class="fc" id="eLast" placeholder="Doe"><span class="field-error" id="eLastErr"></span></div>
      <div class="fg"><label class="flabel">Email *</label><input class="fc" id="eEmail" type="email" placeholder="john@company.com"><span class="field-error" id="eEmailErr"></span></div>
      <div class="fg"><label class="flabel">Employee ID *</label><input class="fc" id="eId" placeholder="EMP011"><span class="field-error" id="eIdErr"></span></div>
      <div class="fg"><label class="flabel">Department *</label><select class="fc" id="eDept"><option>Engineering</option><option>Sales</option><option>HR</option><option>Finance</option><option>Design</option></select></div>
      <div class="fg"><label class="flabel">Shift *</label><select class="fc" id="eShift"><option>Morning</option><option>Evening</option><option>Night</option><option>Flexible</option></select></div>
      <div class="fg"><label class="flabel">Designation</label><input class="fc" id="eDesig" placeholder="Software Engineer"></div>
      <div class="fg"><label class="flabel">Monthly Salary (₹) *</label><input class="fc" id="eSalary" type="number" placeholder="50000"><span class="field-error" id="eSalaryErr"></span></div>
      <div class="fg"><label class="flabel">Join Date</label><input class="fc" id="eJoin" type="date"></div>
      <div class="fg"><label class="flabel">Phone</label><input class="fc" id="ePhone" placeholder="+91 9876543210"></div>
    </div>
    <div class="fg"><label class="flabel">Status</label><select class="fc" id="eStatus"><option value="Active">Active</option><option value="Inactive">Inactive</option></select></div>
    <div class="mfooter">
      <button class="btn btn-secondary" onclick="closeM('mAddEmp')">Cancel</button>
      <button class="btn btn-primary" id="mAddEmpBtn" onclick="submitEmployee()">Add Employee</button>
    </div>
  </div>
</div>

<!-- ══ MODAL: VIEW EMPLOYEE ══ -->
<div class="overlay" id="mViewEmp">
  <div class="modal modal-lg"><div class="mhd"><div class="mtitle">Employee Details</div><button class="mclose" onclick="closeM('mViewEmp')">✕</button></div><div id="mViewEmpContent"></div></div>
</div>

<!-- ══ MODAL: MARK ATTENDANCE ══ -->
<div class="overlay" id="mMarkAtt">
  <div class="modal">
    <div class="mhd"><div class="mtitle">Mark Attendance</div><button class="mclose" onclick="closeM('mMarkAtt')">✕</button></div>
    <div class="fg"><label class="flabel">Employee *</label><select class="fc" id="maEmp"></select></div>
    <div class="fg"><label class="flabel">Date</label><input class="fc" id="maDate" type="date"></div>
    <div class="fg"><label class="flabel">Status *</label>
      <select class="fc" id="maStatus" onchange="togglePunchFields()"><option value="present">Present</option><option value="absent">Absent</option><option value="late">Late</option><option value="leave">On Leave</option><option value="halfday">Half Day</option></select>
    </div>
    <div id="punchFieldsWrap">
      <div class="g2"><div class="fg"><label class="flabel">Punch In</label><input class="fc" id="maIn" type="time" value="09:00"></div><div class="fg"><label class="flabel">Punch Out</label><input class="fc" id="maOut" type="time" value="18:00"></div></div>
    </div>
    <div class="fg"><label class="flabel">Note (optional)</label><input class="fc" id="maNote" placeholder="Any note..."></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mMarkAtt')">Cancel</button><button class="btn btn-primary" onclick="doMarkAtt()">Mark Attendance</button></div>
  </div>
</div>

<!-- ══ MODAL: EDIT ATTENDANCE ══ -->
<div class="overlay" id="mEditAtt">
  <div class="modal">
    <div class="mhd"><div class="mtitle">Edit Attendance</div><button class="mclose" onclick="closeM('mEditAtt')">✕</button></div>
    <div id="mEditAttContent"></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mEditAtt')">Cancel</button><button class="btn btn-primary" onclick="saveEditAtt()">Save Changes</button></div>
  </div>
</div>

<!-- ══ MODAL: ADD SHIFT ══ -->
<div class="overlay" id="mAddShift">
  <div class="modal">
    <div class="mhd"><div class="mtitle" id="mShiftTitle">Create Shift</div><button class="mclose" onclick="closeM('mAddShift')">✕</button></div>
    <div class="fg"><label class="flabel">Shift Name *</label><input class="fc" id="shName" placeholder="e.g. Morning Shift"><span class="field-error" id="shNameErr"></span></div>
    <div class="g2">
      <div class="fg"><label class="flabel">Start Time *</label><input class="fc" id="shStart" type="time" value="09:00"></div>
      <div class="fg"><label class="flabel">End Time *</label><input class="fc" id="shEnd" type="time" value="18:00"></div>
    </div>
    <div class="fg"><label class="flabel">Type</label><select class="fc" id="shType"><option>Day</option><option>Evening</option><option>Night</option><option>Flexible</option></select></div>
    <div class="fg"><label class="flabel">Accent Color</label><select class="fc" id="shColor"><option value="#4f46e5">Indigo</option><option value="#059669">Emerald</option><option value="#d97706">Amber</option><option value="#7c3aed">Violet</option><option value="#e11d48">Rose</option><option value="#0284c7">Sky</option></select></div>
    <div class="fg"><label class="flabel">Description (optional)</label><input class="fc" id="shDesc" placeholder="Brief description..."></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mAddShift')">Cancel</button><button class="btn btn-primary" id="mShiftBtn" onclick="doAddShift()">Create Shift</button></div>
  </div>
</div>

<!-- ══ MODAL: ASSIGN SHIFT ══ -->
<div class="overlay" id="mAssignShift">
  <div class="modal">
    <div class="mhd"><div class="mtitle">Assign Shift</div><button class="mclose" onclick="closeM('mAssignShift')">✕</button></div>
    <div class="fg"><label class="flabel">Select Employee(s)</label>
      <select class="fc" id="asEmpSelect" multiple style="height:120px;"></select>
      <div class="txs tmuted mt8">Hold Ctrl/Cmd to select multiple</div>
    </div>
    <div class="fg"><label class="flabel">Assign to Shift</label><select class="fc" id="asShiftSelect"></select></div>
    <div class="fg"><label class="flabel">Effective From</label><input class="fc" id="asDate" type="date"></div>
    <div class="fg"><label class="flabel">Note</label><input class="fc" id="asNote" placeholder="Optional note..."></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mAssignShift')">Cancel</button><button class="btn btn-primary" onclick="doAssignShift()">Assign Shift</button></div>
  </div>
</div>

<!-- ══ MODAL: APPLY LEAVE ══ -->
<div class="overlay" id="mApplyLeave">
  <div class="modal">
    <div class="mhd"><div class="mtitle" id="mLeaveTitle">Apply for Leave</div><button class="mclose" onclick="closeM('mApplyLeave')">✕</button></div>
    <div class="fg"><label class="flabel">Employee *</label><select class="fc" id="lvEmp"></select></div>
    <div class="fg"><label class="flabel">Leave Type *</label>
      <select class="fc" id="lvType"><option>Casual Leave</option><option>Sick Leave</option><option>Earned Leave</option><option>Half Day</option><option>Maternity Leave</option><option>Paternity Leave</option><option>Emergency Leave</option></select>
    </div>
    <div class="g2">
      <div class="fg"><label class="flabel">From Date *</label><input class="fc" id="lvFrom" type="date"><span class="field-error" id="lvFromErr"></span></div>
      <div class="fg"><label class="flabel">To Date *</label><input class="fc" id="lvTo" type="date"><span class="field-error" id="lvToErr"></span></div>
    </div>
    <div id="lvDaysPreview" class="txs tmuted mb12" style="min-height:17px;"></div>
    <div class="fg"><label class="flabel">Reason *</label><textarea class="fc" id="lvReason" rows="3" placeholder="Please provide reason for leave..."></textarea><span class="field-error" id="lvReasonErr"></span></div>
    <div class="fg"><label class="flabel">Supporting Document (optional)</label><input class="fc" type="file" id="lvDoc" accept=".pdf,.jpg,.png"></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mApplyLeave')">Cancel</button><button class="btn btn-primary" onclick="doApplyLeave()">Submit Request</button></div>
  </div>
</div>

<!-- ══ MODAL: PAYSLIP ══ -->
<div class="overlay" id="mPayslip">
  <div class="modal modal-lg">
    <div class="mhd"><div class="mtitle">Payslip</div><button class="mclose" onclick="closeM('mPayslip')">✕</button></div>
    <div id="mPayslipContent"></div>
    <div class="mfooter">
      <button class="btn btn-secondary" onclick="closeM('mPayslip')">✕ Close</button>
      <button class="btn btn-secondary" onclick="printPayslip()" style="gap:5px;">🖨️ Print</button>
      <button class="btn btn-primary" onclick="downloadPayslipPDF()" style="gap:5px;">⬇ Download PDF</button>
    </div>
  </div>
</div>

<!-- ══ MODAL: PROFILE ══ -->
<div class="overlay" id="mProfile">
  <div class="modal">
    <div class="mhd"><div class="mtitle">My Profile</div><button class="mclose" onclick="closeM('mProfile')">✕</button></div>
    <div style="text-align:center;padding:16px 0 20px;">
      <div class="uav" style="width:64px;height:64px;font-size:22px;margin:0 auto 10px;" id="profileAvatar">AD</div>
      <div style="font-family:'Playfair Display',serif;font-size:18px;font-weight:700;" id="profileName">Admin User</div>
      <div class="txs tmuted" id="profileRole">Super Admin</div>
    </div>
    <div class="fg"><label class="flabel">Full Name</label><input class="fc" id="pName" value="Admin User"></div>
    <div class="fg"><label class="flabel">Email</label><input class="fc" id="pEmail" type="email" value="admin@acmecorp.com"></div>
    <div class="fg"><label class="flabel">Phone</label><input class="fc" id="pPhone" value="+91 9876543200"></div>
    <div class="fg"><label class="flabel">Role</label><input class="fc" value="Super Admin" disabled></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mProfile')">Cancel</button><button class="btn btn-primary" onclick="saveProfileModal()">Save Profile</button></div>
  </div>
</div>

<!-- ══ MODAL: REPORT DETAIL ══ -->
<div class="overlay" id="mReportDetail">
  <div class="modal modal-xl">
    <div class="mhd"><div class="mtitle" id="mReportTitle">Report</div><button class="mclose" onclick="closeM('mReportDetail')">✕</button></div>
    <div id="mReportContent"></div>
    <div class="mfooter"><button class="btn btn-secondary" onclick="closeM('mReportDetail')">Close</button><button class="btn btn-primary" onclick="toast('Report exported!','success');closeM('mReportDetail')">⬇ Export PDF</button></div>
  </div>
</div>

<div class="toast-wrap" id="toastWrap"></div>
<script>
// ══════════════════════════════════════════════════
// DATA STORE
// ══════════════════════════════════════════════════
let EMPS = [
  {id:'EMP001',name:'Arjun Sharma',dept:'Engineering',shift:'Morning',salary:65000,color:'#6366f1',desig:'Senior Engineer',status:'Active',phone:'+91 9876543210',email:'arjun@acme.com',join:'2022-03-15'},
  {id:'EMP002',name:'Priya Patel',dept:'Sales',shift:'Morning',salary:45000,color:'#ec4899',desig:'Sales Executive',status:'Active',phone:'+91 9876543211',email:'priya@acme.com',join:'2023-01-10'},
  {id:'EMP003',name:'Ravi Kumar',dept:'HR',shift:'Morning',salary:40000,color:'#f59e0b',desig:'HR Manager',status:'Active',phone:'+91 9876543212',email:'ravi@acme.com',join:'2021-06-20'},
  {id:'EMP004',name:'Sneha Verma',dept:'Finance',shift:'Evening',salary:55000,color:'#059669',desig:'Finance Analyst',status:'Active',phone:'+91 9876543213',email:'sneha@acme.com',join:'2022-09-01'},
  {id:'EMP005',name:'Kiran Rao',dept:'Engineering',shift:'Morning',salary:70000,color:'#3b82f6',desig:'Lead Developer',status:'Active',phone:'+91 9876543214',email:'kiran@acme.com',join:'2020-11-15'},
  {id:'EMP006',name:'Nidhi Gupta',dept:'Design',shift:'Flexible',salary:48000,color:'#7c3aed',desig:'UI/UX Designer',status:'Active',phone:'+91 9876543215',email:'nidhi@acme.com',join:'2023-04-22'},
  {id:'EMP007',name:'Rohit Mishra',dept:'Sales',shift:'Morning',salary:42000,color:'#ea580c',desig:'Sales Manager',status:'Active',phone:'+91 9876543216',email:'rohit@acme.com',join:'2022-07-08'},
  {id:'EMP008',name:'Anjali Singh',dept:'Engineering',shift:'Night',salary:68000,color:'#0d9488',desig:'Backend Developer',status:'Active',phone:'+91 9876543217',email:'anjali@acme.com',join:'2021-12-01'},
  {id:'EMP009',name:'Vikram Joshi',dept:'Finance',shift:'Morning',salary:52000,color:'#db2777',desig:'Accounts Lead',status:'Active',phone:'+91 9876543218',email:'vikram@acme.com',join:'2022-02-14'},
  {id:'EMP010',name:'Meera Nair',dept:'HR',shift:'Morning',salary:38000,color:'#0284c7',desig:'HR Executive',status:'Active',phone:'+91 9876543219',email:'meera@acme.com',join:'2023-08-30'},
];

let ATT = [
  {emp:'Arjun Sharma',dept:'Engineering',shift:'Morning',in:'09:02 AM',out:'06:15 PM',hours:'9h 13m',status:'present',note:''},
  {emp:'Priya Patel',dept:'Sales',shift:'Morning',in:'09:45 AM',out:'06:00 PM',hours:'8h 15m',status:'late',note:'Traffic delay'},
  {emp:'Ravi Kumar',dept:'HR',shift:'Morning',in:'—',out:'—',hours:'—',status:'absent',note:''},
  {emp:'Sneha Verma',dept:'Finance',shift:'Evening',in:'02:00 PM',out:'10:58 PM',hours:'8h 58m',status:'present',note:''},
  {emp:'Kiran Rao',dept:'Engineering',shift:'Morning',in:'08:55 AM',out:'06:05 PM',hours:'9h 10m',status:'present',note:''},
  {emp:'Nidhi Gupta',dept:'Design',shift:'Flexible',in:'10:30 AM',out:'—',hours:'In Progress',status:'present',note:''},
  {emp:'Rohit Mishra',dept:'Sales',shift:'Morning',in:'—',out:'—',hours:'—',status:'leave',note:'Casual leave approved'},
  {emp:'Anjali Singh',dept:'Engineering',shift:'Night',in:'10:00 PM',out:'—',hours:'In Progress',status:'present',note:''},
  {emp:'Vikram Joshi',dept:'Finance',shift:'Morning',in:'09:10 AM',out:'06:00 PM',hours:'8h 50m',status:'present',note:''},
  {emp:'Meera Nair',dept:'HR',shift:'Morning',in:'09:00 AM',out:'06:00 PM',hours:'9h 00m',status:'present',note:''},
];
const ATT_BACKUP = JSON.parse(JSON.stringify(ATT));

let LEAVES = [
  {id:1,emp:'Rohit Mishra',type:'Casual Leave',from:'2026-03-17',to:'2026-03-19',days:3,reason:'Personal work',status:'pending'},
  {id:2,emp:'Priya Patel',type:'Sick Leave',from:'2026-03-10',to:'2026-03-11',days:2,reason:'Fever',status:'approved'},
  {id:3,emp:'Ravi Kumar',type:'Earned Leave',from:'2026-03-18',to:'2026-03-18',days:1,reason:'Family function',status:'pending'},
  {id:4,emp:'Nidhi Gupta',type:'Half Day',from:'2026-03-15',to:'2026-03-15',days:0.5,reason:'Doctor appointment',status:'approved'},
  {id:5,emp:'Meera Nair',type:'Sick Leave',from:'2026-02-20',to:'2026-02-21',days:2,reason:'Migraine',status:'rejected'},
];

let SHIFTS = [
  {id:1,name:'Morning Shift',start:'09:00 AM',end:'06:00 PM',type:'Day',color:'#4f46e5',startRaw:'09:00',endRaw:'18:00',desc:'Standard day shift'},
  {id:2,name:'Evening Shift',start:'02:00 PM',end:'11:00 PM',type:'Evening',color:'#d97706',startRaw:'14:00',endRaw:'23:00',desc:'Afternoon to late evening'},
  {id:3,name:'Night Shift',start:'10:00 PM',end:'07:00 AM',type:'Night',color:'#7c3aed',startRaw:'22:00',endRaw:'07:00',desc:'Overnight shift'},
  {id:4,name:'Flexible Shift',start:'Flexible',end:'8h / day',type:'Flexible',color:'#059669',startRaw:'--:--',endRaw:'--:--',desc:'Work any 8 hours'},
];

let TS_ENTRIES = [
  {id:1,date:'Mar 19',project:'Website Redesign',activity:'UI Component Development',start:'09:00',end:'12:30',dur:'3h 30m',mins:210,billable:'yes'},
  {id:2,date:'Mar 19',project:'Mobile App',activity:'API Integration Testing',start:'13:30',end:'17:00',dur:'3h 30m',mins:210,billable:'yes'},
  {id:3,date:'Mar 18',project:'Website Redesign',activity:'Code Review',start:'09:00',end:'11:00',dur:'2h 00m',mins:120,billable:'no'},
  {id:4,date:'Mar 18',project:'QA Testing',activity:'Bug Fixing',start:'14:00',end:'18:00',dur:'4h 00m',mins:240,billable:'yes'},
  {id:5,date:'Mar 17',project:'API Integration',activity:'Documentation',start:'10:00',end:'13:00',dur:'3h 00m',mins:180,billable:'no'},
];

let WEEK_DATA = [
  {day:'Monday',in:'09:02 AM',out:'06:10 PM',hours:'9h 8m',status:'present',mins:548},
  {day:'Tuesday',in:'09:15 AM',out:'06:00 PM',hours:'8h 45m',status:'late',mins:525},
  {day:'Wednesday',in:'09:00 AM',out:'07:00 PM',hours:'10h 0m',status:'present',mins:600},
  {day:'Thursday',in:'—',out:'—',hours:'—',status:'absent',mins:0},
  {day:'Friday',in:'09:05 AM',out:'06:05 PM',hours:'9h 0m',status:'present',mins:540},
];

let salCache=[], lvFilter='all', empView='grid', editingEmpId=null, editShiftId=null;
const SAL_PROCESSED = {}; // persistent: { empId: true/false } — survives re-renders
let punchedIn=false,punchStart=null,punchTimer=null,punchSecs=0,punchLog=[];
let editingAttEmp=null;
let weekChartObj=null,rptDeptChartObj=null,rptTrendChartObj=null;

// ══════════════════════════════════════════════════
// CLOCK + INIT
// ══════════════════════════════════════════════════
function tick(){
  const n=new Date(),p=v=>String(v).padStart(2,'0');
  document.getElementById('liveClock').textContent=`${p(n.getHours())}:${p(n.getMinutes())}:${p(n.getSeconds())}`;
  const ds=n.toLocaleDateString('en-IN',{weekday:'long',year:'numeric',month:'long',day:'numeric'});
  document.getElementById('pgDate').textContent=ds;
  document.getElementById('punchDateLbl').textContent=ds;
  if(!punchedIn) document.getElementById('punchClock').textContent=`${p(n.getHours())}:${p(n.getMinutes())}:${p(n.getSeconds())}`;
  const h=n.getHours();
  document.getElementById('heroGreeting').textContent=(h<12?'Good morning':h<17?'Good afternoon':'Good evening')+', Admin! '+(h<12?'☀️':h<17?'👋':'🌙');
}
setInterval(tick,1000); tick();

// ══════════════════════════════════════════════════
// NAVIGATION
// ══════════════════════════════════════════════════
const PTITLES={dashboard:'Dashboard',attendance:'Attendance Register',punch:'Punch Clock',timesheet:'Timesheet',employees:'Employees',shifts:'Shift Management',leaves:'Leave Management',salary:'Payroll & Salary',reports:'Reports & Analytics',settings:'Settings'};

document.querySelectorAll('.nav-item').forEach(el=>{
  el.addEventListener('click',function(){
    goPage(this.dataset.page);
    document.getElementById('sidebar').classList.remove('open');
  });
});
document.getElementById('hambBtn').addEventListener('click',()=>document.getElementById('sidebar').classList.toggle('open'));

function goPage(p){
  document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));
  document.getElementById('page-'+p).classList.add('active');
  const ni=document.querySelector(`.nav-item[data-page="${p}"]`);
  if(ni) ni.classList.add('active');
  document.getElementById('pgTitle').textContent=PTITLES[p]||p;
  if(p==='attendance'){initAttPage();renderAtt();}
  if(p==='employees'){renderEmps();renderEmpList();}
  if(p==='leaves'){renderLeaveStats();renderLeaves(lvFilter);prepLeaveSelects();}
  if(p==='salary') renderSalary();
  if(p==='timesheet'){initTs();renderTimesheet();}
  if(p==='reports') renderReports();
  if(p==='shifts'){renderShiftCards();renderShiftAssign();prepAssignModal();}
  if(p==='punch'){switchPunchTab('my',document.querySelector('#punchTabRow .tab'));renderWeekTbl();buildCalendar();}
}

// ══════════════════════════════════════════════════
// MODAL
// ══════════════════════════════════════════════════
function openM(id){document.getElementById(id).classList.add('open');}
function closeM(id){document.getElementById(id).classList.remove('open');}
document.querySelectorAll('.overlay').forEach(o=>o.addEventListener('click',function(e){if(e.target===this)this.classList.remove('open');}));
document.getElementById('punchConfirmOverlay')?.addEventListener('click', function(e) { if(e.target===this) cancelPunchConfirm(); });

// ══════════════════════════════════════════════════
// TOAST
// ══════════════════════════════════════════════════
const TICO={success:'✅',error:'❌',info:'ℹ️',warning:'⚠️'};
function toast(msg,type='info',dur=3000){
  const el=document.createElement('div');
  el.className=`toast ${type}`;
  el.innerHTML=`<span>${TICO[type]||'ℹ️'}</span><span class="t-msg">${msg}</span>`;
  document.getElementById('toastWrap').appendChild(el);
  setTimeout(()=>{el.style.opacity='0';el.style.transform='translateX(14px)';},dur-300);
  setTimeout(()=>el.remove(),dur);
}

// ══════════════════════════════════════════════════
// HELPERS
// ══════════════════════════════════════════════════
function initials(n){return n.split(' ').map(x=>x[0]).join('').toUpperCase();}
function empColor(n){return EMPS.find(e=>e.name===n)?.color||'#6366f1';}
function avEl(n,sz='sm'){return `<div class="av av-${sz}" style="background:${empColor(n)};color:#fff">${initials(n)}</div>`;}
function fmt12(t){if(!t||t==='--:--')return '—';const[h,m]=t.split(':').map(Number);return `${String(h%12||12).padStart(2,'0')}:${String(m).padStart(2,'0')} ${h>=12?'PM':'AM'}`;}
function calcDur(s,e){const[sh,sm]=s.split(':').map(Number),[eh,em]=e.split(':').map(Number);let mn=(eh*60+em)-(sh*60+sm);if(mn<=0)return null;return{h:Math.floor(mn/60),m:mn%60,mins:mn,str:`${Math.floor(mn/60)}h ${mn%60}m`};}
function todayStr(){return new Date().toISOString().split('T')[0];}
function fmtDate(d){try{return new Date(d).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'});}catch{return d;}}
function exportCSV(rows,fname,headers){
  if(!rows.length){toast('No data to export','warning');return;}
  const csv=[headers.join(','),...rows.map(r=>headers.map(h=>`"${(Object.values(r)[headers.indexOf(h)]||'').toString().replace(/"/g,'""')}"`).join(','))].join('\n');
  const a=document.createElement('a');a.href='data:text/csv,'+encodeURIComponent(csv);a.download=fname+'.csv';a.click();
  toast(`${fname}.csv exported!`,'success');
}

// ══════════════════════════════════════════════════
// DASHBOARD
// ══════════════════════════════════════════════════
function renderDashboard(){
  buildWeekChart();
  document.getElementById('dashPunchLog').innerHTML=ATT.slice(0,7).map(r=>`
    <tr><td><div class="flex aic gap8">${avEl(r.emp,'sm')}<span class="fw6">${r.emp}</span></div></td>
    <td style="color:var(--grn);font-weight:600">${r.in}</td>
    <td style="color:var(--txt3)">${r.out}</td>
    <td><span class="badge ${statusBadge(r.status)}">${r.status}</span></td></tr>`).join('');
  const d={};
  ATT.forEach(r=>{if(!d[r.dept])d[r.dept]={p:0,t:0};d[r.dept].t++;if(r.status==='present'||r.status==='late')d[r.dept].p++;});
  const dclr={'Engineering':'var(--pri)','Sales':'var(--red)','HR':'var(--amb)','Finance':'var(--grn)','Design':'var(--vio)'};
  document.getElementById('dashDept').innerHTML=Object.entries(d).map(([nm,v])=>{
    const pct=Math.round(v.p/v.t*100);
    return `<div class="mb16"><div class="flex jb mb12" style="margin-bottom:5px;"><span class="tsm fw6">${nm}</span><span class="txs tmuted">${v.p}/${v.t} · ${pct}%</span></div><div class="pbar"><div class="pfill" style="width:${pct}%;background:${dclr[nm]||'var(--pri)'}"></div></div></div>`;
  }).join('');
  const acts=[
    {ic:'🟢',txt:'Arjun Sharma clocked in',t:'9:02 AM'},
    {ic:'🔴',txt:'Ravi Kumar is absent today',t:'9:00 AM'},
    {ic:'🟡',txt:'Priya Patel arrived late (9:45 AM)',t:'9:45 AM'},
    {ic:'🌴',txt:'Rohit Mishra on approved leave',t:'Yesterday'},
    {ic:'✅',txt:'Vikram Joshi clocked out',t:'6:00 PM'},
    {ic:'📋',txt:'New leave request from Ravi Kumar',t:'8:30 AM'},
  ];
  document.getElementById('dashActivity').innerHTML=acts.map(a=>`
    <div class="flex aic gap10" style="padding:9px 0;border-bottom:1px solid var(--bdr);">
      <div style="width:30px;height:30px;border-radius:50%;background:var(--sur2);display:flex;align-items:center;justify-content:center;font-size:14px;flex-shrink:0">${a.ic}</div>
      <div style="flex:1;font-size:13px;font-weight:500;color:var(--txt2)">${a.txt}</div>
      <span class="txxs tmuted fw6">${a.t}</span>
    </div>`).join('');
  // Update stat cards
  document.getElementById('ds-present').textContent=ATT.filter(r=>r.status==='present').length;
  document.getElementById('ds-absent').textContent=ATT.filter(r=>r.status==='absent').length;
  document.getElementById('ds-late').textContent=ATT.filter(r=>r.status==='late').length;
  document.getElementById('ds-leave').textContent=ATT.filter(r=>r.status==='leave').length;
}

function buildWeekChart(){
  const present=[42,38,45,31,44,0,0];
  const absent=[8,12,5,19,6,0,0];
  const labels=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'];
  const mode=document.getElementById('weekMode')?.value||'both';
  const ctx=document.getElementById('weekChart').getContext('2d');
  if(weekChartObj) weekChartObj.destroy();
  const ds=[];
  if(mode==='both'||mode==='present') ds.push({label:'Present',data:present,backgroundColor:'rgba(79,70,229,.72)',borderColor:'#4f46e5',borderWidth:0,borderRadius:6,borderSkipped:false});
  if(mode==='both'||mode==='absent') ds.push({label:'Absent',data:absent,backgroundColor:'rgba(225,29,72,.68)',borderColor:'#e11d48',borderWidth:0,borderRadius:6,borderSkipped:false});
  weekChartObj=new Chart(ctx,{type:'bar',data:{labels,datasets:ds},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false},tooltip:{backgroundColor:'#fff',titleColor:'#1a1714',bodyColor:'#4a4540',borderColor:'#e8e3db',borderWidth:1,cornerRadius:8,padding:10}},scales:{x:{ticks:{color:'#8a8078',font:{size:11}},grid:{color:'rgba(0,0,0,.04)'}},y:{ticks:{color:'#8a8078',font:{size:11}},grid:{color:'rgba(0,0,0,.05)'},beginAtZero:true}}}});
}

// ══════════════════════════════════════════════════
// ATTENDANCE
// ══════════════════════════════════════════════════
function statusBadge(s){const m={present:'bp',absent:'ba',late:'bl',leave:'bv',halfday:'bsky'};return m[s]||'bb';}

function initAttPage(){
  document.getElementById('attDateF').value=todayStr();
  document.getElementById('maDate').value=todayStr();
  const sel=document.getElementById('maEmp');
  sel.innerHTML=EMPS.map(e=>`<option value="${e.name}">${e.name} (${e.dept})</option>`).join('');
}

function prepMarkAtt(){
  document.getElementById('maDate').value=todayStr();
}

function getAttRows(){
  const q=(document.getElementById('attQ')?.value||'').toLowerCase();
  const sf=document.getElementById('attStatusF')?.value||'';
  const df=document.getElementById('attDeptF')?.value||'';
  return ATT.filter(r=>
    (!q||r.emp.toLowerCase().includes(q)||r.dept.toLowerCase().includes(q)) &&
    (!sf||r.status===sf) && (!df||r.dept===df)
  );
}

function renderAtt(){
  const rows=getAttRows();
  const counts={present:0,absent:0,late:0,leave:0};
  rows.forEach(r=>{if(counts[r.status]!==undefined)counts[r.status]++;});
  document.getElementById('attSubtitle').textContent=`Showing ${rows.length} of ${ATT.length} employees`;
  document.getElementById('attSummaryRow').innerHTML=
    `<span class="badge bp" style="font-size:12px;padding:5px 13px">✅ ${counts.present} Present</span>`+
    `<span class="badge ba" style="font-size:12px;padding:5px 13px">❌ ${counts.absent} Absent</span>`+
    `<span class="badge bl" style="font-size:12px;padding:5px 13px">⏰ ${counts.late} Late</span>`+
    `<span class="badge bv" style="font-size:12px;padding:5px 13px">🌴 ${counts.leave} On Leave</span>`;
  document.getElementById('attTbl').innerHTML=rows.length?rows.map((r,i)=>`
    <tr>
      <td class="tmuted txs">${i+1}</td>
      <td><div class="flex aic gap8">${avEl(r.emp,'sm')}<div><div class="fw6">${r.emp}</div>${r.note?`<div class="txxs tmuted">${r.note}</div>`:''}</div></div></td>
      <td><span class="txs tmuted">${r.dept}</span></td>
      <td><span class="spill">${r.shift}</span></td>
      <td style="color:var(--grn);font-weight:600">${r.in}</td>
      <td class="tmuted">${r.out}</td>
      <td class="fw6">${r.hours}</td>
      <td><span class="badge ${statusBadge(r.status)}">${r.status}</span></td>
      <td><div class="flex gap4">
        <button class="btn btn-secondary btn-xs" onclick="openEditAtt('${r.emp}')">✏️ Edit</button>
        <button class="btn btn-ghost btn-xs" onclick="viewEmp('${EMPS.find(e=>e.name===r.emp)?.id||''}')">👁</button>
      </div></td>
    </tr>`).join(''):`<tr><td colspan="9"><div class="empty"><div class="ei">📋</div><p>No records found</p></div></td></tr>`;
}

function togglePunchFields(){
  const s=document.getElementById('maStatus').value;
  const hide=s==='absent'||s==='leave';
  document.getElementById('punchFieldsWrap').style.display=hide?'none':'block';
}

function doMarkAtt(){
  const emp=document.getElementById('maEmp').value;
  const status=document.getElementById('maStatus').value;
  const inT=document.getElementById('maIn').value;
  const outT=document.getElementById('maOut').value;
  const note=document.getElementById('maNote').value;
  const isNA=status==='absent'||status==='leave';
  const inStr=isNA?'—':fmt12(inT);
  const outStr=isNA?'—':fmt12(outT);
  const hrs=isNA?'—':(calcDur(inT,outT)?.str||'—');
  const eObj=EMPS.find(e=>e.name===emp);
  const existing=ATT.find(r=>r.emp===emp);
  if(existing){existing.status=status;existing.in=inStr;existing.out=outStr;existing.hours=hrs;existing.note=note;}
  else ATT.push({emp,dept:eObj?.dept||'—',shift:eObj?.shift||'Morning',in:inStr,out:outStr,hours:hrs,status,note});
  closeM('mMarkAtt');renderAtt();renderDashboard();
  toast(`Attendance marked: ${emp} → ${status}`,'success');
}

function openEditAtt(empName){
  const r=ATT.find(x=>x.emp===empName);
  if(!r)return;
  editingAttEmp=empName;
  document.getElementById('mEditAttContent').innerHTML=`
    <div class="flex aic gap10 mb16"><div class="av av-md" style="background:${empColor(empName)};color:#fff">${initials(empName)}</div><div><div class="fw6">${empName}</div><div class="txs tmuted">${r.dept}</div></div></div>
    <div class="fg"><label class="flabel">Status</label>
      <select class="fc" id="editAttStatus">
        <option value="present" ${r.status==='present'?'selected':''}>Present</option>
        <option value="absent" ${r.status==='absent'?'selected':''}>Absent</option>
        <option value="late" ${r.status==='late'?'selected':''}>Late</option>
        <option value="leave" ${r.status==='leave'?'selected':''}>On Leave</option>
        <option value="halfday" ${r.status==='halfday'?'selected':''}>Half Day</option>
      </select>
    </div>
    <div class="g2">
      <div class="fg"><label class="flabel">Punch In</label><input class="fc" id="editAttIn" type="time" value="${r.in==='—'?'09:00':r.in.replace(' AM','').replace(' PM','').trim()}"></div>
      <div class="fg"><label class="flabel">Punch Out</label><input class="fc" id="editAttOut" type="time" value="${r.out==='—'?'18:00':r.out.replace(' AM','').replace(' PM','').trim()}"></div>
    </div>
    <div class="fg"><label class="flabel">Note</label><input class="fc" id="editAttNote" value="${r.note||''}" placeholder="Optional note..."></div>`;
  openM('mEditAtt');
}

function saveEditAtt(){
  if(!editingAttEmp)return;
  const r=ATT.find(x=>x.emp===editingAttEmp);
  if(!r)return;
  const status=document.getElementById('editAttStatus').value;
  const inT=document.getElementById('editAttIn').value;
  const outT=document.getElementById('editAttOut').value;
  const isNA=status==='absent'||status==='leave';
  r.status=status;
  r.in=isNA?'—':fmt12(inT);
  r.out=isNA?'—':fmt12(outT);
  r.hours=isNA?'—':(calcDur(inT,outT)?.str||'—');
  r.note=document.getElementById('editAttNote').value;
  closeM('mEditAtt');renderAtt();renderDashboard();
  toast(`Attendance updated for ${editingAttEmp}`,'success');
}

function exportAttCSV(){
  exportCSV(getAttRows().map(r=>({Employee:r.emp,Department:r.dept,Shift:r.shift,'Punch In':r.in,'Punch Out':r.out,Hours:r.hours,Status:r.status,Note:r.note})),'attendance',['Employee','Department','Shift','Punch In','Punch Out','Hours','Status','Note']);
}

// ══════════════════════════════════════════════════
// PUNCH CLOCK
// ══════════════════════════════════════════════════
function doPunch(){
  const btn=document.getElementById('punchBtn');
  const n=new Date();
  const tstr=n.toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'});
  if(!punchedIn){
    punchedIn=true;punchStart=Date.now();
    btn.classList.add('out');
    document.getElementById('punchIcon').textContent='⏹';
    document.getElementById('punchTxt').textContent='PUNCH OUT';
    document.getElementById('punchStatus').textContent=`Clocked in at ${tstr}`;
    punchLog.push({type:'In',time:tstr,ts:Date.now()});
    punchTimer=setInterval(updatePunchTimer,1000);
    toast(`Punched IN at ${tstr}`,'success');
  }else{
    punchedIn=false;
    const elapsed=Math.floor((Date.now()-punchStart)/1000);
    punchSecs+=elapsed;
    clearInterval(punchTimer);
    btn.classList.remove('out');
    document.getElementById('punchIcon').textContent='▶';
    document.getElementById('punchTxt').textContent='PUNCH IN';
    document.getElementById('punchStatus').textContent=`Punched out at ${tstr}`;
    punchLog.push({type:'Out',time:tstr,ts:Date.now()});
    showPunchSummary();
    toast(`Punched OUT at ${tstr}`,'info');
  }
  renderPunchLog();
  document.getElementById('punchEntryCnt').textContent=`${punchLog.length} entries`;
}

function updatePunchTimer(){
  const total=punchSecs+Math.floor((Date.now()-punchStart)/1000);
  const h=Math.floor(total/3600),m=Math.floor((total%3600)/60),s=total%60;
  const p=v=>String(v).padStart(2,'0');
  document.getElementById('punchClock').textContent=`${p(h)}:${p(m)}:${p(s)}`;
  document.getElementById('pTodayHrs').textContent=`${h}h ${m}m`;
}

function renderPunchLog(){
  const el=document.getElementById('punchLogEl');
  if(!punchLog.length){el.innerHTML='<div class="empty"><div class="ei">⏱</div><p>No punch entries yet today</p></div>';return;}
  el.innerHTML=punchLog.map((p,i)=>`
    <div class="flex jb aic" style="padding:10px 0;border-bottom:1px solid var(--bdr);">
      <div class="flex aic gap10">
        <div style="width:32px;height:32px;border-radius:50%;background:${p.type==='In'?'var(--grnl)':'var(--redl)'};display:flex;align-items:center;justify-content:center;font-size:14px;">${p.type==='In'?'🟢':'🔴'}</div>
        <div><div class="fw6 tsm">Clock ${p.type}</div><div class="txxs tmuted">Entry ${i+1}</div></div>
      </div>
      <span style="font-family:'Playfair Display',serif;font-weight:700;font-size:15px;color:var(--pri)">${p.time}</span>
    </div>`).join('');
}

function showPunchSummary(){
  const el=document.getElementById('punchSummaryBox');
  const h=Math.floor(punchSecs/3600),m=Math.floor((punchSecs%3600)/60);
  el.style.display='block';
  el.innerHTML=`<div class="info-box success mt12">✅ Session summary: <strong>${h}h ${m}m</strong> worked today across ${punchLog.filter(x=>x.type==='In').length} session(s).</div>`;
}

function renderWeekTbl(){
  let total=0;
  document.getElementById('weekTbl').innerHTML=WEEK_DATA.map(r=>{
    total+=r.mins;
    return `<tr><td class="fw6">${r.day}</td><td style="color:var(--grn)">${r.in}</td><td class="tmuted">${r.out}</td><td class="fw7">${r.hours}</td><td><span class="badge ${statusBadge(r.status)}">${r.status}</span></td></tr>`;
  }).join('');
  const h=Math.floor(total/60),m=total%60;
  document.getElementById('weekTotalLbl').textContent=`Total: ${h}h ${m}m`;
}

function buildCalendar(){
  const now=new Date(),y=now.getFullYear(),mo=now.getMonth();
  const days=new Date(y,mo+1,0).getDate(),first=new Date(y,mo,1).getDay();
  document.getElementById('calMonthLbl').textContent=now.toLocaleDateString('en-IN',{month:'long',year:'numeric'});
  const st={};
  for(let i=1;i<=days;i++){const d=new Date(y,mo,i).getDay();if(d===0||d===6)st[i]='weekend';else{const r=Math.random();st[i]=r>.65?'present':r>.5?'absent':r>.42?'leave':r>.35?'holiday':'';}}
  st[now.getDate()]='today';
  let h=`<div class="cal-hd">${['S','M','T','W','T','F','S'].map(d=>`<span>${d}</span>`).join('')}</div><div class="cal-grid">`;
  for(let i=0;i<first;i++) h+=`<div class="cal-day empty"></div>`;
  for(let i=1;i<=days;i++) h+=`<div class="cal-day ${st[i]||''}" title="${st[i]||'—'}">${i}</div>`;
  h+='</div>';
  document.getElementById('myCalendar').innerHTML=h;
}


// ══════════════════════════════════════════════════
// TEAM PUNCH STATION — INDIVIDUAL EMPLOYEE PUNCHING
// ══════════════════════════════════════════════════

/* Per-employee punch state store */
const EMP_PUNCH = {}; // { empId: { punchedIn, punchStart, totalSecs, sessions:[{in,out,dur}], lastIn, lastOut } }

function getEmpPunch(id) {
  if (!EMP_PUNCH[id]) {
    EMP_PUNCH[id] = { punchedIn: false, punchStart: null, totalSecs: 0, sessions: [], lastIn: null, lastOut: null };
  }
  return EMP_PUNCH[id];
}

/* Running timers for each card */
const cardTimers = {};

function switchPunchTab(tab, el) {
  ['my','team','log'].forEach(t => {
    const el2 = document.getElementById('punchTab-'+t);
    if (el2) el2.style.display = t === tab ? 'block' : 'none';
  });
  document.querySelectorAll('#punchTabRow .tab').forEach(t => t.classList.remove('active'));
  if (el) el.classList.add('active');
  if (tab === 'team') {
    renderTeamPunchCards();
    startTeamClock();
  }
  if (tab === 'log') {
    renderPunchLogTable();
  }
  if (tab === 'my') {
    renderWeekTbl();
    buildCalendar();
  }
}

/* Team station clock */
let teamClockInterval = null;
function startTeamClock() {
  if (teamClockInterval) clearInterval(teamClockInterval);
  function updateTeamClock() {
    const n = new Date(), p = v => String(v).padStart(2,'0');
    const el = document.getElementById('teamStationClock');
    if (el) el.textContent = `${p(n.getHours())}:${p(n.getMinutes())}:${p(n.getSeconds())}`;
    const del = document.getElementById('teamStationDate');
    if (del) del.textContent = n.toLocaleDateString('en-IN',{weekday:'long',day:'numeric',month:'long',year:'numeric'});
  }
  updateTeamClock();
  teamClockInterval = setInterval(updateTeamClock, 1000);
}

/* Render employee punch cards */
function renderTeamPunchCards() {
  const q = (document.getElementById('teamPunchQ')?.value || '').toLowerCase();
  const dept = document.getElementById('teamPunchDept')?.value || '';
  const shift = document.getElementById('teamPunchShift')?.value || '';

  const filtered = EMPS.filter(e =>
    e.status === 'Active' &&
    (!q || e.name.toLowerCase().includes(q) || e.dept.toLowerCase().includes(q) || e.id.toLowerCase().includes(q)) &&
    (!dept || e.dept === dept) &&
    (!shift || e.shift === shift)
  );

  // Update stats strip
  let inCount = 0, outCount = 0, idleCount = 0;
  filtered.forEach(e => {
    const ps = getEmpPunch(e.id);
    if (ps.punchedIn) inCount++;
    else if (ps.sessions.length > 0) outCount++;
    else idleCount++;
  });
  const statsEl = document.getElementById('teamPunchStats');
  if (statsEl) {
    statsEl.innerHTML = `
      <span class="badge bp" style="font-size:13px;padding:7px 16px;gap:6px;"><span class="status-dot active" style="width:8px;height:8px;display:inline-block;flex-shrink:0;"></span>🟢 ${inCount} Currently In</span>
      <span class="badge bl" style="font-size:13px;padding:7px 16px;">🏁 ${outCount} Punched Out</span>
      <span class="badge" style="font-size:13px;padding:7px 16px;background:var(--sur2);color:var(--txt3);border:1px solid var(--bdr);">⏳ ${idleCount} Not Yet</span>
      <span class="badge bb" style="font-size:13px;padding:7px 16px;">👥 ${filtered.length} Showing</span>
    `;
  }

  const grid = document.getElementById('teamPunchGrid');
  if (!grid) return;

  if (!filtered.length) {
    grid.innerHTML = '<div class="empty" style="grid-column:1/-1"><div class="ei">👥</div><p>No employees match your filter</p></div>';
    return;
  }

  // Clear old timers
  Object.keys(cardTimers).forEach(id => clearInterval(cardTimers[id]));

  grid.innerHTML = filtered.map(e => {
    const ps = getEmpPunch(e.id);
    const attR = ATT.find(a => a.emp === e.name);
    const cardClass = ps.punchedIn ? 'punched-in' : (ps.sessions.length ? 'punched-out' : '');
    const dotClass = ps.punchedIn ? 'active' : (ps.sessions.length ? 'done' : 'idle');
    const statusLabel = ps.punchedIn ? 'Working now' : (ps.sessions.length ? 'Punched out' : 'Not punched in');
    const totalH = Math.floor(ps.totalSecs / 3600), totalM = Math.floor((ps.totalSecs % 3600) / 60);
    const sessCount = ps.sessions.length;
    const lastAction = ps.lastOut ? `Out: ${ps.lastOut}` : (ps.lastIn ? `In: ${ps.lastIn}` : '—');

    return `<div class="punch-emp-card ${cardClass}" id="empcard-${e.id}">
      <!-- Employee info row -->
      <div style="display:flex;align-items:center;gap:10px;margin-bottom:12px;">
        <div class="av av-md" style="background:${e.color};color:#fff;flex-shrink:0;">${initials(e.name)}</div>
        <div style="flex:1;min-width:0;">
          <div class="fw7 tsm" style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${e.name}</div>
          <div class="txxs tmuted">${e.dept} · ${e.shift}</div>
        </div>
        <div style="display:flex;align-items:center;gap:5px;">
          <span class="status-dot ${dotClass}"></span>
        </div>
      </div>

      <!-- Timer display -->
      <div style="text-align:center;padding:10px 0;border-top:1px solid var(--bdr);border-bottom:1px solid var(--bdr);margin-bottom:12px;">
        <div class="card-timer" id="cardtimer-${e.id}">${formatCardTime(ps)}</div>
        <div class="txxs tmuted" style="margin-top:3px;">${statusLabel}</div>
      </div>

      <!-- Stats row -->
      <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:6px;margin-bottom:12px;font-size:11px;text-align:center;">
        <div style="padding:6px 4px;background:var(--sur2);border-radius:var(--rxs);">
          <div class="fw7" style="color:var(--grn);font-family:'Playfair Display',serif;font-size:15px;">${totalH > 0 || totalM > 0 ? totalH+'h '+totalM+'m' : '—'}</div>
          <div class="tmuted">Total</div>
        </div>
        <div style="padding:6px 4px;background:var(--sur2);border-radius:var(--rxs);">
          <div class="fw7" style="color:var(--pri);font-family:'Playfair Display',serif;font-size:15px;">${sessCount}</div>
          <div class="tmuted">Sessions</div>
        </div>
        <div style="padding:6px 4px;background:var(--sur2);border-radius:var(--rxs);">
          <div class="fw7 txxs" style="color:var(--txt2);">${lastAction}</div>
          <div class="tmuted">Last</div>
        </div>
      </div>

      <!-- Punch action buttons -->
      ${ps.punchedIn ? `
        <div style="display:flex;gap:8px;">
          <button class="emp-punch-btn btn-out" style="flex:2" onclick="openPunchConfirm('${e.id}','out')">⏹ Punch Out</button>
          <button class="emp-punch-btn btn-break" style="flex:1;font-size:11px;" onclick="openPunchConfirm('${e.id}','break')">☕</button>
        </div>
      ` : `
        <button class="emp-punch-btn btn-in" onclick="openPunchConfirm('${e.id}','in')">▶ Punch In</button>
      `}
    </div>`;
  }).join('');

  // Start running timers for punched-in employees
  filtered.forEach(e => {
    const ps = getEmpPunch(e.id);
    if (ps.punchedIn) {
      cardTimers[e.id] = setInterval(() => updateCardTimer(e.id), 1000);
    }
  });
}

function formatCardTime(ps) {
  const live = ps.punchedIn ? Math.floor((Date.now() - ps.punchStart) / 1000) : 0;
  const total = ps.totalSecs + live;
  if (total === 0) return '0h 00m 00s';
  const h = Math.floor(total / 3600), m = Math.floor((total % 3600) / 60), s = total % 60;
  return `${h}h ${String(m).padStart(2,'0')}m ${String(s).padStart(2,'0')}s`;
}

function updateCardTimer(empId) {
  const ps = getEmpPunch(empId);
  if (!ps.punchedIn) return;
  const el = document.getElementById(`cardtimer-${empId}`);
  if (el) el.textContent = formatCardTime(ps);
}

/* ── PUNCH CONFIRM FLOW ── */
let pendingPunch = null;

function openPunchConfirm(empId, action) {
  const emp = EMPS.find(e => e.id === empId);
  if (!emp) return;
  const ps = getEmpPunch(empId);
  const now = new Date();
  const timeStr = now.toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
  const dateStr = now.toLocaleDateString('en-IN', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' });

  pendingPunch = { empId, action, emp, time: timeStr, date: now };

  // Fill confirm overlay
  const av = document.getElementById('pcAvatar');
  av.textContent = initials(emp.name);
  av.style.background = `linear-gradient(135deg,${emp.color},${emp.color}cc)`;

  document.getElementById('pcName').textContent = emp.name;
  document.getElementById('pcRole').textContent = `${emp.desig} · ${emp.dept}`;

  const dot = document.getElementById('pcStatusDot');
  const actionEl = document.getElementById('pcAction');
  const timeEl = document.getElementById('pcTime');
  const btn = document.getElementById('pcConfirmBtn');
  const durEl = document.getElementById('pcDuration');

  timeEl.textContent = timeStr;
  document.getElementById('pcDate').textContent = dateStr;

  if (action === 'in') {
    dot.className = 'status-dot active';
    actionEl.textContent = 'Punch In';
    timeEl.className = 'punch-confirm-time time-in';
    btn.textContent = '✅ Confirm Punch In';
    btn.className = 'btn btn-success';
    btn.style.flex = '2';
    durEl.textContent = '';
  } else if (action === 'out') {
    dot.className = 'status-dot done';
    actionEl.textContent = 'Punch Out';
    timeEl.className = 'punch-confirm-time time-out';
    btn.textContent = '⏹ Confirm Punch Out';
    btn.className = 'btn btn-danger';
    btn.style.flex = '2';
    // Show session duration
    if (ps.punchStart) {
      const secs = Math.floor((now - ps.punchStart) / 1000);
      const h = Math.floor(secs / 3600), m = Math.floor((secs % 3600) / 60);
      durEl.textContent = `Session duration: ${h}h ${m}m`;
      durEl.style.color = 'var(--amb)';
    }
  } else if (action === 'break') {
    dot.className = 'status-dot done';
    actionEl.textContent = 'Break (Temporary Out)';
    timeEl.className = 'punch-confirm-time time-out';
    btn.textContent = '☕ Start Break';
    btn.className = 'btn btn-warning';
    btn.style.flex = '2';
    durEl.textContent = 'Employee can punch back in after break';
    durEl.style.color = 'var(--amb)';
  }

  document.getElementById('punchConfirmOverlay').classList.add('open');
}

function cancelPunchConfirm() {
  document.getElementById('punchConfirmOverlay').classList.remove('open');
  pendingPunch = null;
}

function confirmEmployeePunch() {
  if (!pendingPunch) return;
  const { empId, action, emp, time, date } = pendingPunch;
  const ps = getEmpPunch(empId);
  const now = date;
  const timeStr = time;

  if (action === 'in') {
    ps.punchedIn = true;
    ps.punchStart = now.getTime();
    ps.lastIn = timeStr;
    // Update ATT record
    const attR = ATT.find(a => a.emp === emp.name);
    if (attR) {
      if (attR.in === '—' || attR.in === '') attR.in = timeStr;
      attR.status = now.getHours() > 9 ? 'late' : 'present';
    }
    // Start card timer
    clearInterval(cardTimers[empId]);
    cardTimers[empId] = setInterval(() => updateCardTimer(empId), 1000);
    toast(`✅ ${emp.name} punched IN at ${timeStr}`, 'success');

  } else if (action === 'out' || action === 'break') {
    if (!ps.punchedIn) { toast('Not currently punched in', 'error'); return; }
    const elapsed = Math.floor((now.getTime() - ps.punchStart) / 1000);
    ps.totalSecs += elapsed;
    ps.punchedIn = false;
    ps.punchStart = null;
    ps.lastOut = timeStr;
    const h = Math.floor(elapsed / 3600), m = Math.floor((elapsed % 3600) / 60);
    ps.sessions.push({ in: ps.lastIn, out: timeStr, dur: `${h}h ${m}m`, secs: elapsed });
    clearInterval(cardTimers[empId]);
    // Update ATT record
    const attR = ATT.find(a => a.emp === emp.name);
    const th = Math.floor(ps.totalSecs / 3600), tm = Math.floor((ps.totalSecs % 3600) / 60);
    if (attR) {
      attR.out = timeStr;
      attR.hours = `${th}h ${tm}m`;
    }
    const label = action === 'break' ? 'started break' : 'punched OUT';
    toast(`${emp.name} ${label} at ${timeStr} · ${h}h ${m}m session`, 'info', 4000);
  }

  document.getElementById('punchConfirmOverlay').classList.remove('open');
  pendingPunch = null;
  renderTeamPunchCards();
  renderDashboard();
  if (document.getElementById('punchTab-log')?.style.display !== 'none') {
    renderPunchLogTable();
  }
}

/* ── BULK PUNCH ACTIONS ── */
function punchAllIn() {
  const q = (document.getElementById('teamPunchQ')?.value || '').toLowerCase();
  const dept = document.getElementById('teamPunchDept')?.value || '';
  const filtered = EMPS.filter(e =>
    e.status === 'Active' &&
    (!q || e.name.toLowerCase().includes(q)) &&
    (!dept || e.dept === dept)
  );
  const notIn = filtered.filter(e => !getEmpPunch(e.id).punchedIn);
  if (!notIn.length) { toast('All visible employees already punched in', 'warning'); return; }
  if (!confirm(`Punch IN ${notIn.length} employee(s) now?`)) return;
  const now = new Date();
  const timeStr = now.toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit' });
  notIn.forEach(e => {
    const ps = getEmpPunch(e.id);
    ps.punchedIn = true;
    ps.punchStart = now.getTime();
    ps.lastIn = timeStr;
    const attR = ATT.find(a => a.emp === e.name);
    if (attR) { if (!attR.in || attR.in === '—') attR.in = timeStr; attR.status = 'present'; }
    clearInterval(cardTimers[e.id]);
    cardTimers[e.id] = setInterval(() => updateCardTimer(e.id), 1000);
  });
  toast(`✅ ${notIn.length} employees punched IN at ${timeStr}`, 'success');
  renderTeamPunchCards();
  renderDashboard();
}

function punchAllOut() {
  const active = EMPS.filter(e => getEmpPunch(e.id).punchedIn);
  if (!active.length) { toast('No employees currently punched in', 'warning'); return; }
  if (!confirm(`Punch OUT ${active.length} active employee(s)?`)) return;
  const now = new Date();
  const timeStr = now.toLocaleTimeString('en-IN', { hour: '2-digit', minute: '2-digit' });
  active.forEach(e => {
    const ps = getEmpPunch(e.id);
    const elapsed = Math.floor((now.getTime() - ps.punchStart) / 1000);
    ps.totalSecs += elapsed;
    ps.punchedIn = false; ps.punchStart = null; ps.lastOut = timeStr;
    const h = Math.floor(elapsed / 3600), m = Math.floor((elapsed % 3600) / 60);
    ps.sessions.push({ in: ps.lastIn, out: timeStr, dur: `${h}h ${m}m`, secs: elapsed });
    clearInterval(cardTimers[e.id]);
    const attR = ATT.find(a => a.emp === e.name);
    const th = Math.floor(ps.totalSecs / 3600), tm = Math.floor((ps.totalSecs % 3600) / 60);
    if (attR) { attR.out = timeStr; attR.hours = `${th}h ${tm}m`; }
  });
  toast(`⏹ ${active.length} employees punched OUT at ${timeStr}`, 'info', 4000);
  renderTeamPunchCards();
  renderDashboard();
}

/* ── TODAY'S PUNCH LOG TABLE ── */
function renderPunchLogTable() {
  const q = (document.getElementById('punchLogQ')?.value || '').toLowerCase();
  const statusF = document.getElementById('punchLogStatus')?.value || '';

  let rows = EMPS.map(e => {
    const ps = getEmpPunch(e.id);
    const attR = ATT.find(a => a.emp === e.name);
    const th = Math.floor(ps.totalSecs / 3600), tm = Math.floor((ps.totalSecs % 3600) / 60);
    let pStatus = ps.punchedIn ? 'in' : (ps.sessions.length ? 'out' : 'absent');
    return { e, ps, attR, th, tm, pStatus };
  }).filter(r =>
    (!q || r.e.name.toLowerCase().includes(q) || r.e.dept.toLowerCase().includes(q)) &&
    (!statusF || r.pStatus === statusF)
  );

  const inCount = rows.filter(r => r.pStatus === 'in').length;
  const outCount = rows.filter(r => r.pStatus === 'out').length;
  const absentCount = rows.filter(r => r.pStatus === 'absent').length;
  const totalTeamSecs = rows.reduce((s, r) => {
    let live = r.ps.punchedIn ? Math.floor((Date.now() - r.ps.punchStart) / 1000) : 0;
    return s + r.ps.totalSecs + live;
  }, 0);

  if (document.getElementById('plPunchedIn')) document.getElementById('plPunchedIn').textContent = inCount;
  if (document.getElementById('plPunchedOut')) document.getElementById('plPunchedOut').textContent = outCount;
  if (document.getElementById('plNotPunched')) document.getElementById('plNotPunched').textContent = absentCount;
  if (document.getElementById('plTotalHrs')) document.getElementById('plTotalHrs').textContent = Math.floor(totalTeamSecs / 3600) + 'h';

  const subtitle = document.getElementById('punchLogSubtitle');
  if (subtitle) subtitle.textContent = `${rows.length} of ${EMPS.length} employees · ${new Date().toLocaleDateString('en-IN')}`;

  const tbody = document.getElementById('punchLogTbl');
  if (!tbody) return;

  tbody.innerHTML = rows.length ? rows.map((r, i) => {
    const { e, ps, attR, th, tm, pStatus } = r;
    const live = ps.punchedIn ? Math.floor((Date.now() - ps.punchStart) / 1000) : 0;
    const totalSecs = ps.totalSecs + live;
    const lh = Math.floor(totalSecs / 3600), lm = Math.floor((totalSecs % 3600) / 60);
    const dotClass = pStatus === 'in' ? 'active' : (pStatus === 'out' ? 'done' : 'idle');
    const badgeClass = pStatus === 'in' ? 'bp' : (pStatus === 'out' ? 'bl' : 'ba');
    const badgeLabel = pStatus === 'in' ? 'Working' : (pStatus === 'out' ? 'Done' : 'Absent');
    const firstIn = ps.sessions.length ? ps.sessions[0].in : (ps.lastIn || attR?.in || '—');
    const lastOut = ps.lastOut || (ps.sessions.length ? ps.sessions[ps.sessions.length-1].out : attR?.out || '—');
    return `<tr>
      <td class="tmuted txs">${i+1}</td>
      <td><div class="flex aic gap8">${avEl(e.name,'sm')}<div><div class="fw6">${e.name}</div><div class="txxs tmuted">${e.id}</div></div></div></td>
      <td class="tmuted txs">${e.dept}</td>
      <td><span class="spill">${e.shift}</span></td>
      <td style="color:var(--grn);font-weight:600">${firstIn}</td>
      <td class="tmuted">${lastOut}</td>
      <td style="font-family:'Playfair Display',serif;font-weight:700;color:var(--pri)">${lh}h ${String(lm).padStart(2,'0')}m</td>
      <td style="text-align:center;">${ps.sessions.length}<span class="txxs tmuted"> sess</span></td>
      <td><div class="flex aic gap6"><span class="status-dot ${dotClass}" style="flex-shrink:0;"></span><span class="badge ${badgeClass}">${badgeLabel}</span></div></td>
    </tr>`;
  }).join('') : `<tr><td colspan="9"><div class="empty"><div class="ei">📋</div><p>No records match your filter</p></div></td></tr>`;
}

function refreshPunchLog() {
  renderPunchLogTable();
  toast('Log refreshed', 'info', 1500);
}

function exportPunchLogCSV() {
  const rows = EMPS.map(e => {
    const ps = getEmpPunch(e.id);
    const live = ps.punchedIn ? Math.floor((Date.now() - ps.punchStart) / 1000) : 0;
    const totalSecs = ps.totalSecs + live;
    const h = Math.floor(totalSecs / 3600), m = Math.floor((totalSecs % 3600) / 60);
    const pStatus = ps.punchedIn ? 'Working' : (ps.sessions.length ? 'Done' : 'Absent');
    return {
      Employee: e.name, Department: e.dept, Shift: e.shift,
      'First In': ps.sessions.length ? ps.sessions[0].in : (ps.lastIn || '—'),
      'Last Out': ps.lastOut || '—',
      'Total Hours': `${h}h ${m}m`,
      Sessions: ps.sessions.length,
      Status: pStatus
    };
  });
  exportCSV(rows, 'punch_log_' + new Date().toISOString().split('T')[0],
    ['Employee','Department','Shift','First In','Last Out','Total Hours','Sessions','Status']);
}

// ══════════════════════════════════════════════════
// TIMESHEET
// ══════════════════════════════════════════════════
function initTs(){
  document.getElementById('tsDate').value=todayStr();
  document.getElementById('tsDurPreview').textContent='';
}

['tsStart','tsEnd'].forEach(id=>document.getElementById(id).addEventListener('change',()=>{
  const s=document.getElementById('tsStart').value,e=document.getElementById('tsEnd').value;
  if(s&&e){const d=calcDur(s,e);document.getElementById('tsDurPreview').textContent=d?`⏱ Duration: ${d.str}`:'⚠️ End time must be after start time';}
}));

document.getElementById('lvFrom').addEventListener('change',updateLvDaysPreview);
document.getElementById('lvTo').addEventListener('change',updateLvDaysPreview);

function updateLvDaysPreview(){
  const f=document.getElementById('lvFrom').value,t=document.getElementById('lvTo').value;
  if(f&&t){const days=Math.ceil((new Date(t)-new Date(f))/(864e5))+1;document.getElementById('lvDaysPreview').textContent=days>0?`📅 ${days} day(s) of leave`:'⚠️ To date must be after From date';}
}

function addTsEntry(){
  const proj=document.getElementById('tsProj').value;
  const act=document.getElementById('tsAct').value.trim();
  const date=document.getElementById('tsDate').value;
  const s=document.getElementById('tsStart').value,e=document.getElementById('tsEnd').value;
  const bill=document.getElementById('tsBillable').value;
  if(!act){toast('Please enter activity description','error');document.getElementById('tsAct').classList.add('fc-error');return;}
  document.getElementById('tsAct').classList.remove('fc-error');
  if(!s||!e){toast('Please set start and end time','error');return;}
  const d=calcDur(s,e);
  if(!d){toast('End time must be after start time','error');return;}
  const dateStr=date?new Date(date).toLocaleDateString('en-US',{month:'short',day:'numeric'}):new Date().toLocaleDateString('en-US',{month:'short',day:'numeric'});
  TS_ENTRIES.unshift({id:Math.max(0,...TS_ENTRIES.map(x=>x.id))+1,date:dateStr,project:proj,activity:act,start:s,end:e,dur:d.str,mins:d.mins,billable:bill});
  document.getElementById('tsAct').value='';
  document.getElementById('tsDurPreview').textContent='';
  renderTimesheet();toast('Time entry logged! ✅','success');
}

function deleteTs(id){
  if(!confirm('Delete this time entry?'))return;
  TS_ENTRIES.splice(TS_ENTRIES.findIndex(e=>e.id===id),1);
  renderTimesheet();toast('Entry removed','warning');
}

function clearTs(){if(!confirm('Clear ALL timesheet entries?'))return;TS_ENTRIES.length=0;renderTimesheet();toast('Timesheet cleared','warning');}

function renderTimesheet(){
  const pf=document.getElementById('tsProjFilter')?.value||'';
  const filtered=pf?TS_ENTRIES.filter(e=>e.project===pf):TS_ENTRIES;
  document.getElementById('tsEntryCount').textContent=`${filtered.length} entr${filtered.length!==1?'ies':'y'}`;
  document.getElementById('tsTbl').innerHTML=filtered.length?filtered.map(e=>`
    <tr>
      <td style="color:var(--pri);font-weight:600">${e.date}</td>
      <td class="fw6">${e.project}</td>
      <td class="tmuted">${e.activity}</td>
      <td>${e.start}</td><td>${e.end}</td>
      <td style="color:var(--grn);font-weight:700">${e.dur}</td>
      <td><span class="badge ${e.billable==='yes'?'bp':'ba'}">${e.billable==='yes'?'Billable':'Non-bill'}</span></td>
      <td><button class="btn btn-danger btn-xs" onclick="deleteTs(${e.id})">🗑</button></td>
    </tr>`).join(''):`<tr><td colspan="8"><div class="empty"><div class="ei">🗓</div><p>No entries yet</p></div></td></tr>`;
  const projs={};
  TS_ENTRIES.forEach(e=>{projs[e.project]=(projs[e.project]||0)+e.mins;});
  const total=Object.values(projs).reduce((s,v)=>s+v,0)||1;
  const clrs=['var(--pri)','var(--grn)','var(--amb)','var(--vio)','var(--red)','var(--sky)'];
  document.getElementById('tsProjBreak').innerHTML=Object.entries(projs).length?
    Object.entries(projs).map(([p,m],i)=>{const h=Math.floor(m/60),mn=m%60,pct=Math.round(m/total*100);return `<div class="mb16"><div class="flex jb mb12" style="margin-bottom:5px;"><span class="tsm fw6">${p}</span><span class="txs tmuted">${h}h ${mn}m · ${pct}%</span></div><div class="pbar"><div class="pfill" style="width:${pct}%;background:${clrs[i%clrs.length]}"></div></div></div>`;}).join('')
    :'<div class="empty" style="padding:16px 0"><div class="ei" style="font-size:28px">📋</div><p>No entries yet</p></div>';
  const totalMins=TS_ENTRIES.reduce((s,e)=>s+e.mins,0);
  const billMins=TS_ENTRIES.filter(e=>e.billable==='yes').reduce((s,e)=>s+e.mins,0);
  document.getElementById('tsTotalHrs').textContent=`${Math.floor(totalMins/60)}h ${totalMins%60}m`;
  document.getElementById('tsBillableHrs').textContent=`${Math.floor(billMins/60)}h ${billMins%60}m`;
  document.getElementById('tsDaysLogged').textContent=[...new Set(TS_ENTRIES.map(e=>e.date))].length+' days';
}

function exportTsCSV(){
  exportCSV(TS_ENTRIES.map(e=>({Date:e.date,Project:e.project,Activity:e.activity,Start:e.start,End:e.end,Duration:e.dur,Billable:e.billable})),'timesheet',['Date','Project','Activity','Start','End','Duration','Billable']);
}

// ══════════════════════════════════════════════════
// EMPLOYEES
// ══════════════════════════════════════════════════
function filteredEmps(){
  const q=(document.getElementById('empQ')?.value||'').toLowerCase();
  const d=document.getElementById('empDeptF')?.value||'';
  const s=document.getElementById('empStatusF')?.value||'';
  const sh=document.getElementById('empShiftF')?.value||'';
  return EMPS.filter(e=>
    (!q||e.name.toLowerCase().includes(q)||e.id.toLowerCase().includes(q)||e.dept.toLowerCase().includes(q)||e.desig.toLowerCase().includes(q))&&
    (!d||e.dept===d)&&(!s||e.status===s)&&(!sh||e.shift===sh));
}

function setEmpView(v){
  empView=v;
  document.getElementById('empGrid').style.display=v==='grid'?'grid':'none';
  document.getElementById('empList').style.display=v==='list'?'block':'none';
  const bg='background:var(--prix);color:var(--pri);border-color:var(--pri);';
  document.getElementById('btnGrid').setAttribute('style',v==='grid'?bg:'');
  document.getElementById('btnList').setAttribute('style',v==='list'?bg:'');
}

function renderEmps(){
  const emps=filteredEmps();
  document.getElementById('empCountLbl').textContent=`${emps.length} employee${emps.length!==1?'s':''} found`;
  document.getElementById('empGrid').innerHTML=emps.length?emps.map(e=>`
    <div class="emp-card" style="border-color:transparent" onmouseenter="this.style.borderColor='${e.color}'" onmouseleave="this.style.borderColor='transparent'">
      <div class="flex aic gap12 mb16">
        <div class="av av-lg" style="background:${e.color};color:#fff">${initials(e.name)}</div>
        <div style="flex:1;min-width:0;">
          <div class="fw7 tsm">${e.name}</div>
          <div class="txxs tmuted">${e.id} · ${e.desig}</div>
        </div>
        <span class="badge ${e.status==='Active'?'bp':'ba'}">${e.status}</span>
      </div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:12px;margin-bottom:13px;">
        <div style="padding:8px;background:var(--sur2);border-radius:8px;"><div class="txxs tmuted mb12" style="margin-bottom:2px;">DEPT</div><div class="fw6">${e.dept}</div></div>
        <div style="padding:8px;background:var(--sur2);border-radius:8px;"><div class="txxs tmuted mb12" style="margin-bottom:2px;">SHIFT</div><div class="fw6">${e.shift}</div></div>
        <div style="padding:8px;background:var(--sur2);border-radius:8px;"><div class="txxs tmuted mb12" style="margin-bottom:2px;">SALARY</div><div class="fw6 tgrn">₹${e.salary.toLocaleString()}</div></div>
        <div style="padding:8px;background:var(--sur2);border-radius:8px;"><div class="txxs tmuted mb12" style="margin-bottom:2px;">JOINED</div><div class="fw6">${e.join}</div></div>
      </div>
      <div class="flex gap6">
        <button class="btn btn-secondary btn-xs" style="flex:1" onclick="viewEmp('${e.id}')">👁 View</button>
        <button class="btn btn-ghost btn-xs" style="flex:1" onclick="editEmp('${e.id}')">✏️ Edit</button>
        <button class="btn btn-danger btn-xs" onclick="delEmp('${e.id}')">🗑</button>
      </div>
    </div>`).join(''):`<div class="empty" style="grid-column:1/-1"><div class="ei">👥</div><p>No employees found</p></div>`;
}

function renderEmpList(){
  const emps=filteredEmps();
  document.getElementById('empListTbl').innerHTML=emps.map((e,i)=>`
    <tr>
      <td class="tmuted txs">${i+1}</td>
      <td><div class="flex aic gap8">${avEl(e.name,'sm')}<div><div class="fw6">${e.name}</div><div class="txxs tmuted">${e.desig}</div></div></div></td>
      <td class="tmuted txs">${e.id}</td>
      <td>${e.dept}</td>
      <td><span class="spill">${e.shift}</span></td>
      <td style="color:var(--grn);font-weight:600">₹${e.salary.toLocaleString()}</td>
      <td><span class="badge ${e.status==='Active'?'bp':'ba'}">${e.status}</span></td>
      <td><div class="flex gap4">
        <button class="btn btn-secondary btn-xs" onclick="viewEmp('${e.id}')">View</button>
        <button class="btn btn-ghost btn-xs" onclick="editEmp('${e.id}')">Edit</button>
        <button class="btn btn-danger btn-xs" onclick="delEmp('${e.id}')">🗑</button>
      </div></td>
    </tr>`).join('');
}

function viewEmp(id){
  const e=EMPS.find(x=>x.id===id);if(!e)return;
  const attR=ATT.find(a=>a.emp===e.name);
  const lvCount=LEAVES.filter(l=>l.emp===e.name&&l.status==='approved').length;
  document.getElementById('mViewEmpContent').innerHTML=`
    <div style="background:linear-gradient(135deg,${e.color}14,${e.color}07);border:1px solid ${e.color}28;border-radius:14px;padding:18px;margin-bottom:18px;display:flex;align-items:center;gap:16px;">
      <div class="av av-xl" style="background:${e.color};color:#fff;box-shadow:0 6px 20px ${e.color}38">${initials(e.name)}</div>
      <div style="flex:1">
        <div style="font-family:'Playfair Display',serif;font-size:20px;font-weight:700">${e.name}</div>
        <div class="tmuted tsm">${e.desig} · ${e.dept}</div>
        <div class="txxs tmuted mt8">${e.id}</div>
      </div>
      <span class="badge ${e.status==='Active'?'bp':'ba'}" style="font-size:12px;padding:5px 13px">${e.status}</span>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:16px;">
      ${[['📧 Email',e.email],['📱 Phone',e.phone],['🔄 Shift',e.shift],['📅 Joined',fmtDate(e.join)],['💰 Monthly',`<span class="tgrn fw7">₹${e.salary.toLocaleString()}</span>`],['🌴 Leaves Taken',`${lvCount} approved`]].map(([l,v])=>`
      <div style="padding:11px;background:var(--sur2);border-radius:10px;border:1px solid var(--bdr);">
        <div class="txxs tmuted" style="margin-bottom:3px;">${l}</div>
        <div class="fw6 tsm">${v}</div>
      </div>`).join('')}
    </div>
    <div style="padding:12px;background:${attR?.status==='present'?'var(--grnl)':attR?.status==='absent'?'var(--redl)':attR?.status==='late'?'var(--ambl)':'var(--viol)'};border-radius:10px;margin-bottom:16px;border:1px solid ${attR?.status==='present'?'rgba(5,150,105,.15)':attR?.status==='absent'?'rgba(225,29,72,.15)':'rgba(0,0,0,.06)'}">
      <div class="txxs tmuted fw7" style="margin-bottom:6px;text-transform:uppercase;letter-spacing:.8px;">Today's Attendance</div>
      <div class="flex aic gap12 flex-wrap"><span class="badge ${statusBadge(attR?.status||'absent')}">${attR?.status||'absent'}</span><span class="tsm">In: <b>${attR?.in||'—'}</b></span><span class="tsm">Out: <b>${attR?.out||'—'}</b></span><span class="tsm">Hours: <b>${attR?.hours||'—'}</b></span></div>
    </div>
    <div class="flex gap8">
      <button class="btn btn-primary" style="flex:1" onclick="editEmp('${e.id}');closeM('mViewEmp')">✏️ Edit Employee</button>
      <button class="btn btn-secondary" style="flex:1" onclick="closeM('mViewEmp')">Close</button>
    </div>`;
  openM('mViewEmp');
}

function prepAddEmp(){
  editingEmpId=null;
  document.getElementById('mAddEmpTitle').textContent='Add New Employee';
  document.getElementById('mAddEmpBtn').textContent='Add Employee';
  ['eFirst','eLast','eEmail','eId','eDesig','eSalary','ePhone','eJoin'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});
  document.getElementById('eDept').value='Engineering';
  document.getElementById('eShift').value='Morning';
  document.getElementById('eStatus').value='Active';
  document.getElementById('eJoin').value=todayStr();
  ['eFirstErr','eLastErr','eEmailErr','eIdErr','eSalaryErr'].forEach(id=>document.getElementById(id).textContent='');
}

function editEmp(id){
  const e=EMPS.find(x=>x.id===id);if(!e)return;
  editingEmpId=id;
  document.getElementById('mAddEmpTitle').textContent='Edit Employee';
  document.getElementById('mAddEmpBtn').textContent='Save Changes';
  document.getElementById('eFirst').value=e.name.split(' ')[0];
  document.getElementById('eLast').value=e.name.split(' ').slice(1).join(' ');
  document.getElementById('eEmail').value=e.email;
  document.getElementById('eId').value=e.id;
  document.getElementById('eDept').value=e.dept;
  document.getElementById('eShift').value=e.shift;
  document.getElementById('eDesig').value=e.desig;
  document.getElementById('eSalary').value=e.salary;
  document.getElementById('eJoin').value=e.join;
  document.getElementById('ePhone').value=e.phone;
  document.getElementById('eStatus').value=e.status;
  openM('mAddEmp');
}

function delEmp(id){
  const e=EMPS.find(x=>x.id===id);if(!e)return;
  if(!confirm(`Delete ${e.name}? This action cannot be undone.`))return;
  EMPS=EMPS.filter(x=>x.id!==id);
  ATT=ATT.filter(x=>x.emp!==e.name);
  renderEmps();renderEmpList();renderDashboard();
  toast(`${e.name} has been removed`,'warning');
}

function submitEmployee(){
  const first=document.getElementById('eFirst').value.trim();
  const last=document.getElementById('eLast').value.trim();
  const email=document.getElementById('eEmail').value.trim();
  const eid=document.getElementById('eId').value.trim();
  const sal=parseInt(document.getElementById('eSalary').value)||0;
  let valid=true;
  const setErr=(id,msg)=>{document.getElementById(id).textContent=msg;valid=!msg;};
  setErr('eFirstErr',first?'':'First name is required');
  setErr('eLastErr',last?'':'Last name is required');
  setErr('eEmailErr',email&&/\S+@\S+\.\S+/.test(email)?'':'Valid email required');
  setErr('eIdErr',eid?'':'Employee ID required');
  setErr('eSalaryErr',sal>0?'':'Valid salary required');
  if(!valid){toast('Please fix the errors','error');return;}
  if(!editingEmpId&&EMPS.find(x=>x.id===eid)){document.getElementById('eIdErr').textContent='ID already exists';toast('Employee ID already exists','error');return;}
  const clrs=['#6366f1','#ec4899','#f59e0b','#059669','#3b82f6','#7c3aed','#ea580c','#0d9488','#db2777','#0284c7','#84cc16','#8b5cf6'];
  if(editingEmpId){
    const e=EMPS.find(x=>x.id===editingEmpId);if(!e)return;
    const oldName=e.name;
    e.name=`${first} ${last}`;e.email=email;e.dept=document.getElementById('eDept').value;
    e.shift=document.getElementById('eShift').value;e.desig=document.getElementById('eDesig').value||'Employee';
    e.salary=sal;e.join=document.getElementById('eJoin').value;e.phone=document.getElementById('ePhone').value;
    e.status=document.getElementById('eStatus').value;
    ATT.filter(a=>a.emp===oldName).forEach(a=>{a.emp=e.name;a.dept=e.dept;a.shift=e.shift;});
    toast(`${e.name} updated successfully!`,'success');
  }else{
    const newE={
      id:eid,name:`${first} ${last}`,email,
      dept:document.getElementById('eDept').value,shift:document.getElementById('eShift').value,
      desig:document.getElementById('eDesig').value||'Employee',salary:sal,
      join:document.getElementById('eJoin').value||todayStr(),
      phone:document.getElementById('ePhone').value||'—',
      color:clrs[EMPS.length%clrs.length],status:document.getElementById('eStatus').value
    };
    EMPS.push(newE);
    ATT.push({emp:newE.name,dept:newE.dept,shift:newE.shift,in:'—',out:'—',hours:'—',status:'absent',note:''});
    toast(`${newE.name} added successfully!`,'success');
  }
  closeM('mAddEmp');
  renderEmps();renderEmpList();renderDashboard();
}

function exportEmpCSV(){
  exportCSV(filteredEmps().map(e=>({ID:e.id,Name:e.name,Email:e.email,Phone:e.phone,Department:e.dept,Designation:e.desig,Shift:e.shift,Salary:e.salary,JoinDate:e.join,Status:e.status})),'employees',['ID','Name','Email','Phone','Department','Designation','Shift','Salary','Join Date','Status']);
}

// ══════════════════════════════════════════════════
// SHIFTS
// ══════════════════════════════════════════════════
function renderShiftCards(){
  document.getElementById('shiftCards').innerHTML=SHIFTS.map((s)=>{
    const cnt=EMPS.filter(e=>e.shift===s.name.split(' ')[0]).length;
    return `<div class="card" style="border-top:4px solid ${s.color};">
      <div class="flex jb aic mb16">
        <div style="width:40px;height:40px;border-radius:12px;background:${s.color}18;display:flex;align-items:center;justify-content:center;font-size:18px;">🔄</div>
        <span class="badge bb">${s.type}</span>
      </div>
      <div style="font-family:'Playfair Display',serif;font-size:16px;font-weight:700;margin-bottom:4px;">${s.name}</div>
      <div class="txs tmuted mb16">${s.desc||''}</div>
      <div style="display:flex;flex-direction:column;gap:7px;font-size:12.5px;margin-bottom:13px;">
        <div class="flex jb"><span class="tmuted">Start Time</span><span class="fw6" style="color:${s.color}">${s.start}</span></div>
        <div class="flex jb"><span class="tmuted">End Time</span><span class="fw6" style="color:${s.color}">${s.end}</span></div>
        <div class="flex jb"><span class="tmuted">Employees</span><span style="font-family:'Playfair Display',serif;font-weight:800;font-size:22px;color:${s.color}">${cnt}</span></div>
      </div>
      <div class="pbar mb16"><div class="pfill" style="width:${EMPS.length?Math.round(cnt/EMPS.length*100):0}%;background:${s.color}"></div></div>
      <div class="flex gap6">
        <button class="btn btn-secondary btn-xs" style="flex:1" onclick="editShift(${s.id})">✏️ Edit</button>
        <button class="btn btn-ghost btn-xs" style="flex:1;color:${s.color}" onclick="quickAssignShift(${s.id})">👥 Assign</button>
        <button class="btn btn-danger btn-xs" onclick="deleteShift(${s.id})">🗑</button>
      </div>
    </div>`;}).join('');
}

function prepAddShift(){
  editShiftId=null;
  document.getElementById('mShiftTitle').textContent='Create Shift';
  document.getElementById('mShiftBtn').textContent='Create Shift';
  document.getElementById('shName').value='';
  document.getElementById('shStart').value='09:00';
  document.getElementById('shEnd').value='18:00';
  document.getElementById('shType').value='Day';
  document.getElementById('shColor').value='#4f46e5';
  document.getElementById('shDesc').value='';
}

function editShift(id){
  const s=SHIFTS.find(x=>x.id===id);if(!s)return;
  editShiftId=id;
  document.getElementById('mShiftTitle').textContent='Edit Shift';
  document.getElementById('mShiftBtn').textContent='Save Changes';
  document.getElementById('shName').value=s.name;
  document.getElementById('shStart').value=s.startRaw;
  document.getElementById('shEnd').value=s.endRaw;
  document.getElementById('shType').value=s.type;
  document.getElementById('shColor').value=s.color;
  document.getElementById('shDesc').value=s.desc||'';
  openM('mAddShift');
}

function deleteShift(id){
  const s=SHIFTS.find(x=>x.id===id);if(!s)return;
  if(!confirm(`Delete "${s.name}"? Employees on this shift will need reassignment.`))return;
  SHIFTS=SHIFTS.filter(x=>x.id!==id);
  renderShiftCards();renderShiftAssign();
  toast(`${s.name} deleted`,'warning');
}

function quickAssignShift(id){
  const s=SHIFTS.find(x=>x.id===id);if(!s)return;
  document.getElementById('asShiftSelect').value=s.name;
  openM('mAssignShift');
}

function doAddShift(){
  const name=document.getElementById('shName').value.trim();
  if(!name){document.getElementById('shNameErr').textContent='Shift name is required';toast('Please enter shift name','error');return;}
  document.getElementById('shNameErr').textContent='';
  const start=document.getElementById('shStart').value,end=document.getElementById('shEnd').value;
  const type=document.getElementById('shType').value,color=document.getElementById('shColor').value;
  const desc=document.getElementById('shDesc').value;
  if(editShiftId){
    const s=SHIFTS.find(x=>x.id===editShiftId);if(!s)return;
    s.name=name;s.start=fmt12(start);s.end=fmt12(end);s.type=type;s.color=color;s.startRaw=start;s.endRaw=end;s.desc=desc;
    toast(`${name} updated!`,'success');
  }else{
    const newId=Math.max(0,...SHIFTS.map(s=>s.id))+1;
    SHIFTS.push({id:newId,name,start:fmt12(start),end:fmt12(end),type,color,startRaw:start,endRaw:end,desc});
    const sel=document.getElementById('asShiftSelect');
    if(sel) sel.innerHTML=SHIFTS.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
    toast(`${name} created!`,'success');
  }
  closeM('mAddShift');
  renderShiftCards();renderShiftAssign();
}

function renderShiftAssign(){
  const q=(document.getElementById('shiftEmpQ')?.value||'').toLowerCase();
  const emps=EMPS.filter(e=>!q||e.name.toLowerCase().includes(q)||e.dept.toLowerCase().includes(q));
  document.getElementById('shiftAssignTbl').innerHTML=emps.map(e=>{
    const sd=SHIFTS.find(s=>s.name.toLowerCase().startsWith(e.shift.toLowerCase()));
    return `<tr>
      <td><div class="flex aic gap8">${avEl(e.name,'sm')}<span class="fw6">${e.name}</span></div></td>
      <td class="tmuted">${e.dept}</td>
      <td><span class="spill">${e.shift}</span></td>
      <td style="color:var(--grn);font-weight:500">${sd?.start||'—'}</td>
      <td class="tmuted">${sd?.end||'—'}</td>
      <td><button class="btn btn-secondary btn-xs" onclick="singleAssign('${e.name}')">🔄 Reassign</button></td>
    </tr>`;}).join('');
}

function singleAssign(name){
  const sel=document.getElementById('asEmpSelect');
  if(!sel)return;
  openM('mAssignShift');
  setTimeout(()=>{
    for(let o of sel.options) o.selected=(o.value===name);
  },100);
}

function prepAssignModal(){
  const empSel=document.getElementById('asEmpSelect');
  empSel.innerHTML=EMPS.map(e=>`<option value="${e.name}">${e.name} (${e.dept})</option>`).join('');
  const shiftSel=document.getElementById('asShiftSelect');
  shiftSel.innerHTML=SHIFTS.map(s=>`<option value="${s.name}">${s.name}</option>`).join('');
  document.getElementById('asDate').value=todayStr();
}

function doAssignShift(){
  const sel=document.getElementById('asEmpSelect');
  const selected=[...sel.selectedOptions].map(o=>o.value);
  if(!selected.length){toast('Please select at least one employee','error');return;}
  const shiftName=document.getElementById('asShiftSelect').value;
  selected.forEach(name=>{
    const e=EMPS.find(x=>x.name===name);
    if(e)e.shift=shiftName;
  });
  closeM('mAssignShift');renderShiftCards();renderShiftAssign();
  toast(`${selected.length} employee(s) assigned to ${shiftName}`,'success');
}

// ══════════════════════════════════════════════════
// LEAVES
// ══════════════════════════════════════════════════
function renderLeaveStats(){
  const t=LEAVES.length,p=LEAVES.filter(l=>l.status==='pending').length;
  const ap=LEAVES.filter(l=>l.status==='approved').length,rj=LEAVES.filter(l=>l.status==='rejected').length;
  document.getElementById('lv-total').textContent=t;
  document.getElementById('lv-pending').textContent=p;
  document.getElementById('lv-approved').textContent=ap;
  document.getElementById('lv-rejected').textContent=rj;
  const b=document.getElementById('leaveBadge');
  if(b){b.textContent=p;b.style.display=p>0?'':'none';}
}

function renderLeaves(filter='all'){
  lvFilter=filter;
  const q=(document.getElementById('lvQ')?.value||'').toLowerCase();
  let data=filter==='all'?LEAVES:LEAVES.filter(l=>l.status===filter);
  if(q) data=data.filter(l=>l.emp.toLowerCase().includes(q)||l.type.toLowerCase().includes(q));
  document.getElementById('leaveTbl').innerHTML=data.length?data.map(l=>`
    <tr>
      <td><div class="flex aic gap8">${avEl(l.emp,'sm')}<span class="fw6">${l.emp}</span></div></td>
      <td><span class="badge bv">${l.type}</span></td>
      <td>${fmtDate(l.from)}</td><td>${fmtDate(l.to)}</td>
      <td class="fw7">${l.days}</td>
      <td class="tmuted" style="max-width:140px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap" title="${l.reason}">${l.reason}</td>
      <td><span class="badge ${l.status==='approved'?'bp':l.status==='rejected'?'ba':'bl'}">${l.status}</span></td>
      <td><div class="flex gap4">
        ${l.status==='pending'?`<button class="btn btn-success btn-xs" onclick="actLeave(${l.id},'approved')">✓ Approve</button><button class="btn btn-danger btn-xs" onclick="actLeave(${l.id},'rejected')">✗ Reject</button>`:''}
        <button class="btn btn-secondary btn-xs" onclick="deleteLeave(${l.id})">🗑</button>
      </div></td>
    </tr>`).join(''):`<tr><td colspan="8"><div class="empty"><div class="ei">🌴</div><p>No ${filter==='all'?'':filter} leave requests</p></div></td></tr>`;
}

function actLeave(id,status){
  const l=LEAVES.find(x=>x.id===id);if(!l)return;
  l.status=status;
  if(status==='approved'){
    const attR=ATT.find(a=>a.emp===l.emp);
    if(attR&&l.from===todayStr()){attR.status='leave';attR.in='—';attR.out='—';attR.hours='—';attR.note=`${l.type} - approved`;}
  }
  renderLeaves(lvFilter);renderLeaveStats();renderDashboard();
  toast(`Leave ${status} for ${l.emp}`,status==='approved'?'success':'error');
}

function deleteLeave(id){
  if(!confirm('Delete this leave request?'))return;
  LEAVES.splice(LEAVES.findIndex(x=>x.id===id),1);
  renderLeaves(lvFilter);renderLeaveStats();
  toast('Leave request deleted','warning');
}

function switchLeaveTab(f,el){
  document.querySelectorAll('#lvTabRow .tab').forEach(t=>t.classList.remove('active'));
  el.classList.add('active');renderLeaves(f);
}

function prepLeaveSelects(){
  document.getElementById('lvEmp').innerHTML=EMPS.map(e=>`<option value="${e.name}">${e.name} (${e.dept})</option>`).join('');
  document.getElementById('lvFrom').value=todayStr();
  document.getElementById('lvTo').value=todayStr();
}

function prepLeaveModal(){
  prepLeaveSelects();
  document.getElementById('lvReason').value='';
  document.getElementById('lvDaysPreview').textContent='';
  ['lvFromErr','lvToErr','lvReasonErr'].forEach(id=>document.getElementById(id).textContent='');
}

function doApplyLeave(){
  const emp=document.getElementById('lvEmp').value;
  const type=document.getElementById('lvType').value;
  const from=document.getElementById('lvFrom').value;
  const to=document.getElementById('lvTo').value;
  const reason=document.getElementById('lvReason').value.trim();
  let valid=true;
  const setE=(id,msg)=>{document.getElementById(id).textContent=msg;if(msg)valid=false;};
  setE('lvFromErr',from?'':'From date required');
  setE('lvToErr',to?'':'To date required');
  setE('lvReasonErr',reason?'':'Reason is required');
  if(from&&to&&new Date(to)<new Date(from)){setE('lvToErr','To date must be after From date');}
  if(!valid){toast('Please fix the errors','error');return;}
  const days=Math.max(0.5,Math.ceil((new Date(to)-new Date(from))/(864e5))+1);
  LEAVES.push({id:Math.max(0,...LEAVES.map(l=>l.id))+1,emp,type,from,to,days,reason,status:'pending'});
  closeM('mApplyLeave');
  renderLeaves(lvFilter);renderLeaveStats();
  toast(`Leave request submitted for ${emp}`,'info');
}

function exportLeavesCSV(){
  const data=lvFilter==='all'?LEAVES:LEAVES.filter(l=>l.status===lvFilter);
  exportCSV(data.map(l=>({Employee:l.emp,Type:l.type,From:l.from,To:l.to,Days:l.days,Reason:l.reason,Status:l.status})),'leaves',['Employee','Type','From','To','Days','Reason','Status']);
}

// ══════════════════════════════════════════════════
// PAYROLL
// ══════════════════════════════════════════════════
function renderSalary(){
  const deptF=document.getElementById('payDeptF')?.value||'';
  const emps=deptF?EMPS.filter(e=>e.dept===deptF):EMPS;
  salCache=emps.map(e=>{
    const seed=e.id.charCodeAt(3);
    const present=20+(seed%6);
    const deduct=Math.round(e.salary/26*(26-present));
    const ot=(seed%4)*500;
    const net=e.salary-deduct+ot;
    // Use persistent store; initialise once from seed if not yet set
    if(!(e.id in SAL_PROCESSED)) SAL_PROCESSED[e.id]=false;
    const processed=SAL_PROCESSED[e.id];
    return{e,present,deduct,ot,net,processed};
  });
  const totalNet=salCache.reduce((s,r)=>s+r.net,0);
  const totalDeduct=salCache.reduce((s,r)=>s+r.deduct,0);
  const done=salCache.filter(r=>r.processed).length;
  document.getElementById('pay-total').textContent=`₹${(totalNet/1e5).toFixed(2)}L`;
  document.getElementById('pay-done').textContent=done;
  document.getElementById('pay-donePct').textContent=`${Math.round(done/salCache.length*100)}% processed`;
  document.getElementById('pay-pend').textContent=salCache.length-done;
  document.getElementById('pay-deduct').textContent=`₹${totalDeduct.toLocaleString()}`;
  document.getElementById('salTbl').innerHTML=salCache.map(r=>`
    <tr id="salrow-${r.e.id}" style="transition:background .3s;">
      <td><div class="flex aic gap8">${avEl(r.e.name,'sm')}<div><div class="fw6">${r.e.name}</div><div class="txxs tmuted">${r.e.desig||r.e.dept}</div></div></div></td>
      <td class="tmuted">${r.e.dept}</td>
      <td>₹${r.e.salary.toLocaleString()}</td>
      <td style="color:var(--grn);font-weight:700">${r.present}/26</td>
      <td style="color:var(--red);font-weight:600">₹${r.deduct.toLocaleString()}</td>
      <td style="color:var(--grn);font-weight:600">₹${r.ot.toLocaleString()}</td>
      <td style="font-family:'Playfair Display',serif;font-weight:800;font-size:16px;color:var(--txt)">₹${r.net.toLocaleString()}</td>
      <td id="salstatus-${r.e.id}">
        ${r.processed
          ? `<span class="badge bp" style="font-size:12px;padding:5px 12px;">✅ Processed</span>`
          : `<span class="badge bl" style="font-size:12px;padding:5px 12px;">⏳ Pending</span>`}
      </td>
      <td><div class="flex gap6">
        ${!r.processed
          ? `<button class="btn btn-success btn-xs" id="salprocessbtn-${r.e.id}" onclick="processSal('${r.e.id}')" style="min-width:72px;">⚡ Process</button>`
          : `<button class="btn btn-secondary btn-xs" disabled style="min-width:72px;opacity:.5;">✓ Done</button>`
        }
        <button class="btn btn-primary btn-xs" onclick="viewPayslip('${r.e.id}')" title="View & Download Payslip" style="min-width:32px;">📄</button>
      </div></td>
    </tr>`).join('');
}

function processSal(id){
  const r=salCache.find(x=>x.e.id===id);
  if(!r)return;
  // Animate button loading
  const btn=document.getElementById('salprocessbtn-'+id);
  if(btn){btn.textContent='⏳ Processing...';btn.disabled=true;}
  setTimeout(()=>{
    r.processed=true;
    SAL_PROCESSED[id]=true; // persist across re-renders
    // Update status badge in-place (no full re-render flicker)
    const statusCell=document.getElementById('salstatus-'+id);
    if(statusCell) statusCell.innerHTML='<span class="badge bp" style="font-size:12px;padding:5px 12px;">✅ Processed</span>';
    const actionCell=btn?.parentElement;
    if(actionCell) actionCell.innerHTML='<button class="btn btn-secondary btn-xs" disabled style="min-width:72px;opacity:.5;">✓ Done</button><button class="btn btn-primary btn-xs" onclick="viewPayslip(\''+id+'\')">📄</button>';
    // Flash row green
    const row=document.getElementById('salrow-'+id);
    if(row){row.style.background='rgba(5,150,105,.06)';setTimeout(()=>{row.style.background='';},1800);}
    // Update stats
    const done=salCache.filter(x=>x.processed).length;
    if(document.getElementById('pay-done')) document.getElementById('pay-done').textContent=done;
    if(document.getElementById('pay-pend')) document.getElementById('pay-pend').textContent=salCache.length-done;
    if(document.getElementById('pay-donePct')) document.getElementById('pay-donePct').textContent=Math.round(done/salCache.length*100)+'% processed';
    toast('✅ '+r.e.name+' salary processed — ₹'+r.net.toLocaleString(),'success',3500);
  },600);
}
function processAllSalary(){
  const pending=salCache.filter(r=>!r.processed);
  if(!pending.length){toast('All salaries already processed','info');return;}
  if(!confirm('Process '+pending.length+' pending salaries? This will mark them all as done.'))return;
  let i=0;
  const btn=document.querySelector('.btn[onclick="processAllSalary()"]');
  if(btn){btn.textContent='⏳ Processing...';btn.disabled=true;}
  const interval=setInterval(()=>{
    if(i>=pending.length){
      clearInterval(interval);
      renderSalary();
      toast('🎉 All '+pending.length+' salaries processed successfully!','success',4000);
      return;
    }
    pending[i].processed=true;
    SAL_PROCESSED[pending[i].e.id]=true;
    const row=document.getElementById('salrow-'+pending[i].e.id);
    if(row){row.style.background='rgba(5,150,105,.06)';}
    const statusCell=document.getElementById('salstatus-'+pending[i].e.id);
    if(statusCell) statusCell.innerHTML='<span class="badge bp" style="font-size:12px;padding:5px 12px;">✅ Processed</span>';
    i++;
  },120);
}

function viewPayslip(id){
  const r=salCache.find(x=>x.e.id===id);
  if(!r){toast('Please open Payroll page first','error');return;}
  const month=document.getElementById('payMonthSel')?.value||'March 2026';
  const company=document.getElementById('sCompany')?.value||'Acme Corp Pvt. Ltd.';
  const genDate=new Date().toLocaleDateString('en-IN',{day:'2-digit',month:'long',year:'numeric'});
  const pfAmt=Math.round(r.e.salary*.12);
  const ptAmt=200;
  const totalDeduct=r.deduct+pfAmt+ptAmt;
  const gross=r.e.salary+r.ot;
  const basicSal=Math.round(r.e.salary*.6);
  const hra=Math.round(r.e.salary*.2);
  const allow=r.e.salary-basicSal-hra;

  const html=`
    <div id="payslipPrintArea" style="font-family:'Plus Jakarta Sans',sans-serif;background:#fff;color:#1a1714;max-width:680px;margin:0 auto;">
      <!-- Header -->
      <div style="background:linear-gradient(135deg,#4f46e5,#7c3aed);padding:24px 28px;color:#fff;border-radius:12px 12px 0 0;">
        <div style="display:flex;justify-content:space-between;align-items:flex-start;">
          <div>
            <div style="font-family:'Playfair Display',serif;font-size:22px;font-weight:800;letter-spacing:2px;">PAYSLIP</div>
            <div style="opacity:.75;font-size:12px;margin-top:3px;">${month}</div>
          </div>
          <div style="text-align:right;">
            <div style="font-weight:700;font-size:15px;">${company}</div>
            <div style="opacity:.7;font-size:11px;margin-top:2px;">Pune, Maharashtra · India</div>
            <div style="opacity:.6;font-size:10.5px;margin-top:1px;">PAN: AAACA1234C · TAN: PNET05432C</div>
          </div>
        </div>
      </div>

      <!-- Employee Info -->
      <div style="background:#f5f3ef;padding:18px 28px;border-bottom:1px solid #e8e3db;">
        <div style="display:flex;align-items:center;gap:16px;">
          <div style="width:52px;height:52px;border-radius:50%;background:${r.e.color};color:#fff;display:flex;align-items:center;justify-content:center;font-weight:800;font-size:18px;flex-shrink:0;">${initials(r.e.name)}</div>
          <div style="flex:1;">
            <div style="font-weight:700;font-size:16px;margin-bottom:2px;">${r.e.name}</div>
            <div style="color:#8a8078;font-size:12px;">${r.e.desig||'—'} · ${r.e.dept} · ${r.e.id}</div>
          </div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:11.5px;">
            <div style="padding:8px 12px;background:#fff;border-radius:8px;border:1px solid #e8e3db;"><div style="color:#8a8078;font-size:10px;text-transform:uppercase;letter-spacing:.5px;margin-bottom:2px;">Shift</div><div style="font-weight:600;">${r.e.shift}</div></div>
            <div style="padding:8px 12px;background:#fff;border-radius:8px;border:1px solid #e8e3db;"><div style="color:#8a8078;font-size:10px;text-transform:uppercase;letter-spacing:.5px;margin-bottom:2px;">Working Days</div><div style="font-weight:600;">${r.present}/26</div></div>
            <div style="padding:8px 12px;background:#fff;border-radius:8px;border:1px solid #e8e3db;"><div style="color:#8a8078;font-size:10px;text-transform:uppercase;letter-spacing:.5px;margin-bottom:2px;">Pay Period</div><div style="font-weight:600;">${month}</div></div>
            <div style="padding:8px 12px;background:#fff;border-radius:8px;border:1px solid #e8e3db;"><div style="color:#8a8078;font-size:10px;text-transform:uppercase;letter-spacing:.5px;margin-bottom:2px;">Generated</div><div style="font-weight:600;">${genDate}</div></div>
          </div>
        </div>
      </div>

      <!-- Earnings & Deductions -->
      <div style="display:grid;grid-template-columns:1fr 1fr;border-bottom:1px solid #e8e3db;">
        <!-- Earnings -->
        <div style="padding:20px 24px;border-right:1px solid #e8e3db;">
          <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;color:#8a8078;margin-bottom:14px;">💰 Earnings</div>
          ${[
            ['Basic Salary', basicSal],
            ['House Rent Allowance (HRA)', hra],
            ['Special Allowances', allow],
          ].map(([l,v])=>`<div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #f0ede8;font-size:13px;"><span style="color:#4a4540;">${l}</span><span style="font-weight:600;">₹${v.toLocaleString()}</span></div>`).join('')}
          ${r.ot>0?`<div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #f0ede8;font-size:13px;"><span style="color:#059669;font-weight:600;">Overtime Bonus</span><span style="font-weight:600;color:#059669;">₹${r.ot.toLocaleString()}</span></div>`:''}
          <div style="display:flex;justify-content:space-between;padding:10px 0 0;font-size:14px;font-weight:700;color:#1a1714;"><span>Gross Earnings</span><span>₹${gross.toLocaleString()}</span></div>
        </div>
        <!-- Deductions -->
        <div style="padding:20px 24px;">
          <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;color:#8a8078;margin-bottom:14px;">📉 Deductions</div>
          ${[
            ['Absence / LOP Deduction', r.deduct],
            ['Provident Fund (12%)', pfAmt],
            ['Professional Tax', ptAmt],
          ].map(([l,v])=>`<div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #f0ede8;font-size:13px;"><span style="color:#4a4540;">${l}</span><span style="font-weight:600;color:#e11d48;">₹${v.toLocaleString()}</span></div>`).join('')}
          <div style="display:flex;justify-content:space-between;padding:10px 0 0;font-size:14px;font-weight:700;color:#e11d48;"><span>Total Deductions</span><span>₹${totalDeduct.toLocaleString()}</span></div>
        </div>
      </div>

      <!-- Net Pay -->
      <div style="padding:20px 28px;background:linear-gradient(135deg,#d1fae5,#ecfdf5);display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid #e8e3db;">
        <div>
          <div style="font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.8px;color:#059669;margin-bottom:4px;">Net Pay</div>
          <div style="font-family:'Playfair Display',serif;font-size:32px;font-weight:800;color:#059669;letter-spacing:-1px;">₹${r.net.toLocaleString()}</div>
          <div style="font-size:12px;color:#4a4540;margin-top:4px;">Gross ₹${gross.toLocaleString()} − Deductions ₹${totalDeduct.toLocaleString()}</div>
        </div>
        <div style="text-align:right;">
          <div style="display:inline-flex;align-items:center;gap:6px;padding:8px 16px;background:rgba(5,150,105,.12);border:1px solid rgba(5,150,105,.25);border-radius:999px;">
            <span style="color:#059669;font-size:13px;font-weight:700;">${r.processed?'✅ Salary Processed':'⏳ Pending'}</span>
          </div>
        </div>
      </div>

      <!-- Footer -->
      <div style="padding:16px 28px;background:#f5f3ef;border-radius:0 0 12px 12px;display:flex;justify-content:space-between;align-items:center;">
        <div style="font-size:11px;color:#8a8078;">This is a computer-generated payslip. No signature required.</div>
        <div style="font-size:11px;color:#8a8078;">Generated on ${genDate} · AttendFlow v2.0</div>
      </div>
    </div>`;

  document.getElementById('mPayslipContent').innerHTML=html;
  // Store current payslip data for download
  window._currentPayslipData={name:r.e.name,month,id,html};
  openM('mPayslip');
}

function exportSalaryCSV(){
  exportCSV(salCache.map(r=>({Employee:r.e.name,Department:r.e.dept,'Base Salary':r.e.salary,'Present Days':r.present,Deductions:r.deduct,'OT Bonus':r.ot,'Net Salary':r.net,Status:r.processed?'Processed':'Pending'})),'payroll',['Employee','Department','Base Salary','Present Days','Deductions','OT Bonus','Net Salary','Status']);
}

// ══════════════════════════════════════════════════
// REPORTS
// ══════════════════════════════════════════════════
function genReport(name){
  toast(`Generating ${name} Report...`,'info');
  setTimeout(()=>{
    toast(`${name} Report ready! ✅`,'success');
    document.getElementById('mReportTitle').textContent=`${name} Report — March 2026`;
    document.getElementById('mReportContent').innerHTML=`
      <div class="info-box info mb16">📊 ${name} report generated for March 2026. Below is a summary.</div>
      <div class="stats-grid" style="grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:16px;">
        <div class="stat-card sc-grn" style="padding:14px;"><div class="stat-label">Total Records</div><div class="stat-val v-grn" style="font-size:24px;">${name==='Attendance'?ATT.length:name==='Payroll'?EMPS.length:LEAVES.length}</div></div>
        <div class="stat-card sc-pri" style="padding:14px;"><div class="stat-label">${name==='Payroll'?'Processed':'Approved'}</div><div class="stat-val v-pri" style="font-size:24px;">${name==='Payroll'?salCache.filter(r=>r.processed).length:LEAVES.filter(l=>l.status==='approved').length}</div></div>
        <div class="stat-card sc-amb" style="padding:14px;"><div class="stat-label">${name==='Payroll'?'Pending':'Pending'}</div><div class="stat-val v-amb" style="font-size:24px;">${name==='Payroll'?salCache.filter(r=>!r.processed).length:LEAVES.filter(l=>l.status==='pending').length}</div></div>
      </div>
      <p class="tsm tmuted">Click "Export PDF" to download the full report.</p>`;
    openM('mReportDetail');
  },1200);
}

function renderReports(){
  const deptF=document.getElementById('rptDeptF')?.value||'';
  const emps=deptF?EMPS.filter(e=>e.dept===deptF):EMPS;
  document.getElementById('rptTbl').innerHTML=emps.map(e=>{
    const seed=e.id.charCodeAt(3);
    const p=18+(seed%8),a=26-p-(seed%3),late=seed%4,ot=seed%12,lv=26-p-a,pct=Math.round(p/26*100);
    return `<tr>
      <td><div class="flex aic gap8">${avEl(e.name,'sm')}<span class="fw6">${e.name}</span></div></td>
      <td class="tmuted">${e.dept}</td>
      <td style="color:var(--grn);font-weight:700">${p}</td>
      <td style="color:var(--red)">${a}</td>
      <td style="color:var(--amb)">${late}</td>
      <td class="tmuted">${ot}h</td>
      <td style="color:var(--vio)">${lv}</td>
      <td>
        <div class="flex aic gap8">
          <div class="pbar" style="flex:1;"><div class="pfill" style="width:${pct}%;background:${pct>85?'var(--grn)':pct>70?'var(--pri)':'var(--red)'}"></div></div>
          <span class="fw7 tsm" style="min-width:36px">${pct}%</span>
        </div>
      </td>
    </tr>`;}).join('');
  buildRptCharts();
}

function buildRptCharts(){
  const depts={};EMPS.forEach(e=>{depts[e.dept]=(depts[e.dept]||0)+1;});
  const dc=document.getElementById('rptDeptChart').getContext('2d');
  if(rptDeptChartObj) rptDeptChartObj.destroy();
  rptDeptChartObj=new Chart(dc,{type:'doughnut',data:{labels:Object.keys(depts),datasets:[{data:Object.values(depts),backgroundColor:['rgba(79,70,229,.78)','rgba(225,29,72,.75)','rgba(217,119,6,.75)','rgba(5,150,105,.75)','rgba(124,58,237,.75)'],borderWidth:3,borderColor:'#fff',hoverBorderWidth:3}]},options:{responsive:true,maintainAspectRatio:false,cutout:'58%',plugins:{legend:{position:'right',labels:{color:'#4a4540',font:{size:11,family:"'Plus Jakarta Sans'"},boxWidth:11,padding:10}},tooltip:{backgroundColor:'#fff',titleColor:'#1a1714',bodyColor:'#4a4540',borderColor:'#e8e3db',borderWidth:1,cornerRadius:8,padding:10}}}});
  const months=['Oct','Nov','Dec','Jan','Feb','Mar'];
  const presentTrend=[38,41,39,44,42,45];
  const absentTrend=[12,9,11,8,10,7];
  const tc=document.getElementById('rptTrendChart').getContext('2d');
  if(rptTrendChartObj) rptTrendChartObj.destroy();
  rptTrendChartObj=new Chart(tc,{type:'line',data:{labels:months,datasets:[{label:'Present',data:presentTrend,borderColor:'var(--pri)',backgroundColor:'rgba(79,70,229,.08)',fill:true,tension:.4,pointBackgroundColor:'var(--pri)',pointRadius:4,borderWidth:2},{label:'Absent',data:absentTrend,borderColor:'var(--red)',backgroundColor:'rgba(225,29,72,.06)',fill:true,tension:.4,pointBackgroundColor:'var(--red)',pointRadius:4,borderWidth:2}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:'#4a4540',font:{size:11},boxWidth:12}},tooltip:{backgroundColor:'#fff',titleColor:'#1a1714',bodyColor:'#4a4540',borderColor:'#e8e3db',borderWidth:1,cornerRadius:8,padding:10}},scales:{x:{ticks:{color:'#8a8078',font:{size:11}},grid:{color:'rgba(0,0,0,.04)'}},y:{ticks:{color:'#8a8078',font:{size:11}},grid:{color:'rgba(0,0,0,.05)'},beginAtZero:true}}}});
}

function exportReportCSV(){
  const deptF=document.getElementById('rptDeptF')?.value||'';
  const emps=deptF?EMPS.filter(e=>e.dept===deptF):EMPS;
  exportCSV(emps.map(e=>{const seed=e.id.charCodeAt(3);const p=18+(seed%8),a=26-p-(seed%3),late=seed%4,ot=seed%12,lv=26-p-a,pct=Math.round(p/26*100);return{Employee:e.name,Department:e.dept,Present:p,Absent:a,Late:late,'OT Hours':ot+'h',Leave:lv,'Attendance%':pct+'%'};}),`attendance_report_${document.getElementById('rptMonth').value.replace(' ','_')}`,['Employee','Department','Present','Absent','Late','OT Hours','Leave','Attendance%']);
}

// ══════════════════════════════════════════════════
// SETTINGS
// ══════════════════════════════════════════════════
function switchSettTab(section,el){
  document.querySelectorAll('#settTabRow .tab').forEach(t=>t.classList.remove('active'));
  el.classList.add('active');
  ['company','notifications','security','data'].forEach(s=>{
    document.getElementById(`settSection-${s}`).style.display=s===section?'block':'none';
  });
}

function saveCompanySettings(){
  const name=document.getElementById('sCompany').value.trim();
  if(!name){toast('Company name is required','error');return;}
  toast(`Company settings saved for "${name}" ✅`,'success');
}

function saveAttendancePolicy(){
  const hrs=parseInt(document.getElementById('sWorkHrs').value);
  if(!hrs||hrs<1||hrs>24){toast('Work hours must be between 1 and 24','error');return;}
  toast('Attendance policy saved! ✅','success');
}

function saveNotifSettings(){
  toast('Notification preferences saved! 🔔','success');
}

function saveProfile(){
  const name=document.getElementById('sAdminName').value.trim();
  const email=document.getElementById('sAdminEmail').value.trim();
  if(!name){toast('Name is required','error');return;}
  if(!email||!/\S+@\S+\.\S+/.test(email)){toast('Valid email is required','error');return;}
  toast('Profile updated! ✅','success');
}

function saveProfileModal(){
  const name=document.getElementById('pName').value.trim();
  if(!name){toast('Name is required','error');return;}
  document.getElementById('topUserName').textContent=name;
  document.getElementById('topUserAv').textContent=initials(name);
  document.getElementById('profileAvatar').textContent=initials(name);
  document.getElementById('profileName').textContent=name;
  closeM('mProfile');
  toast('Profile updated! ✅','success');
}

function changePassword(){
  const p1=document.getElementById('sPwd1').value;
  const p2=document.getElementById('sPwd2').value;
  const p3=document.getElementById('sPwd3').value;
  if(!p1){toast('Current password is required','error');return;}
  if(!p2||p2.length<8){toast('New password must be at least 8 characters','error');return;}
  if(p2!==p3){toast('Passwords do not match','error');return;}
  ['sPwd1','sPwd2','sPwd3'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('pwdStrength').textContent='';
  toast('Password changed successfully! 🔑','success');
}

document.getElementById('sPwd2')?.addEventListener('input',function(){
  const el=document.getElementById('pwdStrength');
  const v=this.value;
  if(!v){el.textContent='';return;}
  if(v.length<6){el.textContent='Weak password';el.style.color='var(--red)';}
  else if(v.length<10){el.textContent='Medium strength';el.style.color='var(--amb)';}
  else{el.textContent='Strong password ✓';el.style.color='var(--grn)';}
});

// ══════════════════════════════════════════════════
// INIT (called by auth system after login)
// ══════════════════════════════════════════════════
function initApp() {
  renderDashboard();
  document.getElementById('attDateF').value=todayStr();
  document.getElementById('maDate').value=todayStr();
  setEmpView('grid');
  document.getElementById('maEmp').innerHTML=EMPS.map(e=>`<option value="${e.name}">${e.name} (${e.dept})</option>`).join('');
}
</script>

<!-- ══ PUNCH CONFIRM OVERLAY ══ -->
<div class="punch-confirm-overlay" id="punchConfirmOverlay">
  <div class="punch-confirm-box">
    <div class="punch-confirm-avatar" id="pcAvatar"></div>
    <div class="punch-confirm-name" id="pcName"></div>
    <div class="txs tmuted" id="pcRole"></div>
    <div style="display:flex;align-items:center;justify-content:center;gap:8px;margin:10px 0 0;">
      <span id="pcStatusDot" class="status-dot"></span>
      <span class="tsm fw6" id="pcAction"></span>
    </div>
    <div class="punch-confirm-time" id="pcTime"></div>
    <div class="txs tmuted" id="pcDate"></div>
    <div id="pcDuration" style="margin-top:8px;font-size:13px;color:var(--grn);font-weight:600;min-height:20px;"></div>
    <div class="punch-confirm-actions">
      <button class="btn btn-secondary" style="flex:1;font-size:13px;" onclick="cancelPunchConfirm()">Cancel</button>
      <button class="btn" id="pcConfirmBtn" style="flex:2;font-size:14px;" onclick="confirmEmployeePunch()">Confirm</button>
    </div>
  </div>
</div>

</div><!-- /appWrapper -->

<script>
// ══════════════════════════════════════════════════
// AUTH SYSTEM
// ══════════════════════════════════════════════════

// User accounts database
const AUTH_USERS = [
  {
    username: 'admin',
    password: 'Admin@123',
    name: 'Admin User',
    role: 'admin',
    roleLabel: 'Super Admin',
    dept: 'Management',
    avatar: 'AD',
    color: '#4f46e5',
    permissions: ['dashboard','attendance','punch','timesheet','employees','shifts','leaves','salary','reports','settings'],
    canEdit: true,
    canDelete: true,
    canApprove: true,
    canViewPayroll: true,
  },
  {
    username: 'hrmanager',
    password: 'HR@2026',
    name: 'Ravi Kumar',
    role: 'hr',
    roleLabel: 'HR Manager',
    dept: 'HR',
    avatar: 'RK',
    color: '#059669',
    permissions: ['dashboard','attendance','punch','timesheet','employees','shifts','leaves','reports'],
    canEdit: true,
    canDelete: false,
    canApprove: true,
    canViewPayroll: false,
  },
  {
    username: 'manager',
    password: 'Mgr@2026',
    name: 'Kiran Rao',
    role: 'manager',
    roleLabel: 'Team Manager',
    dept: 'Engineering',
    avatar: 'KR',
    color: '#d97706',
    permissions: ['dashboard','attendance','punch','timesheet','employees','leaves','reports'],
    canEdit: false,
    canDelete: false,
    canApprove: true,
    canViewPayroll: false,
  },
  {
    username: 'employee',
    password: 'Emp@2026',
    name: 'Priya Patel',
    role: 'employee',
    roleLabel: 'Sales Executive',
    dept: 'Sales',
    avatar: 'PP',
    color: '#ec4899',
    permissions: ['dashboard','punch','timesheet','leaves'],
    canEdit: false,
    canDelete: false,
    canApprove: false,
    canViewPayroll: false,
  },
];

let currentUser = null;
let sessionTimer = null;
const SESSION_DURATION = 8 * 60 * 60 * 1000; // 8 hours

// ── CHECK SAVED SESSION ──
function initAuth() {
  const saved = localStorage.getItem('attendflow_session');
  if (saved) {
    try {
      const sess = JSON.parse(saved);
      if (sess.expiry > Date.now()) {
        currentUser = AUTH_USERS.find(u => u.username === sess.username);
        if (currentUser) {
          showApp();
          return;
        }
      }
    } catch(e) {}
    localStorage.removeItem('attendflow_session');
  }
  document.getElementById('loginUser').focus();
}

// ── DO LOGIN ──
function doLogin() {
  const username = document.getElementById('loginUser').value.trim();
  const password = document.getElementById('loginPass').value;
  const remember = document.getElementById('rememberMe').checked;
  const btn = document.getElementById('loginBtn');
  const errEl = document.getElementById('loginError');

  // Clear previous errors
  document.getElementById('loginUser').classList.remove('error');
  document.getElementById('loginPass').classList.remove('error');
  errEl.classList.remove('show');

  if (!username) {
    showLoginError('Please enter your username');
    document.getElementById('loginUser').classList.add('error');
    document.getElementById('loginUser').focus();
    return;
  }
  if (!password) {
    showLoginError('Please enter your password');
    document.getElementById('loginPass').classList.add('error');
    document.getElementById('loginPass').focus();
    return;
  }

  // Show loading state
  btn.disabled = true;
  btn.innerHTML = '<span class="login-spinner"></span> Signing in...';

  // Simulate auth delay (realistic feel)
  setTimeout(() => {
    const user = AUTH_USERS.find(u => u.username === username && u.password === password);

    if (!user) {
      btn.disabled = false;
      btn.innerHTML = 'Sign In';
      document.getElementById('loginUser').classList.add('error');
      document.getElementById('loginPass').classList.add('error');
      // Shake effect
      const form = document.querySelector('.login-right');
      form.style.animation = 'none';
      form.offsetHeight;
      form.style.animation = 'shake .4s ease';
      setTimeout(() => form.style.animation = '', 500);
      // Show specific error
      const userExists = AUTH_USERS.find(u => u.username === username);
      showLoginError(userExists ? 'Incorrect password. Please try again.' : 'Username not found. Check your credentials.');
      document.getElementById('loginPass').value = '';
      document.getElementById('loginPass').focus();
      return;
    }

    // Save session
    currentUser = user;
    if (remember) {
      localStorage.setItem('attendflow_session', JSON.stringify({
        username: user.username,
        expiry: Date.now() + SESSION_DURATION
      }));
    }

    // Success animation
    btn.innerHTML = '✅ Welcome, ' + user.name.split(' ')[0] + '!';
    btn.style.background = 'linear-gradient(135deg,#059669,#10b981)';

    setTimeout(() => showApp(), 700);
  }, 900);
}

function showLoginError(msg) {
  const el = document.getElementById('loginError');
  document.getElementById('loginErrorMsg').textContent = msg;
  el.classList.add('show');
}

// ── SHOW APP AFTER LOGIN ──
function showApp() {
  // Hide login, show app
  const loginScreen = document.getElementById('loginScreen');
  loginScreen.style.transition = 'opacity .4s ease, transform .4s ease';
  loginScreen.style.opacity = '0';
  loginScreen.style.transform = 'scale(1.02)';

  setTimeout(() => {
    loginScreen.style.display = 'none';
    document.getElementById('appWrapper').style.display = 'block';
    applyRoleRestrictions();
    updateTopbarUser();
    startSessionTimer();
    // Re-init app with user context
    initApp();
    document.getElementById('heroGreeting').textContent = 
      (new Date().getHours() < 12 ? 'Good morning' : new Date().getHours() < 17 ? 'Good afternoon' : 'Good evening') + 
      ', ' + currentUser.name.split(' ')[0] + '! ' + (new Date().getHours() < 12 ? '☀️' : new Date().getHours() < 17 ? '👋' : '🌙');
  }, 420);
}

// ── APPLY ROLE-BASED RESTRICTIONS ──
function applyRoleRestrictions() {
  if (!currentUser) return;
  const allowed = currentUser.permissions;

  // Hide nav items user can't access
  document.querySelectorAll('.nav-item[data-page]').forEach(item => {
    const page = item.dataset.page;
    if (!allowed.includes(page)) {
      item.style.opacity = '.35';
      item.style.pointerEvents = 'none';
      item.title = 'Access restricted — ' + currentUser.roleLabel + ' role';
    } else {
      item.style.opacity = '';
      item.style.pointerEvents = '';
    }
  });

  // Hide Add Employee button for non-admins / non-HR
  const addEmpBtn = document.querySelector('.topbar-r .btn-primary');
  if (addEmpBtn) {
    addEmpBtn.style.display = (currentUser.role === 'admin' || currentUser.role === 'hr') ? '' : 'none';
  }

  // If employee role, navigate to punch clock as default
  if (currentUser.role === 'employee') {
    setTimeout(() => goPage('punch'), 100);
  }
}

// ── UPDATE TOPBAR WITH LOGGED-IN USER ──
function updateTopbarUser() {
  if (!currentUser) return;

  // Update sidebar user
  const uav = document.getElementById('topUserAv');
  const uname = document.getElementById('topUserName');
  if (uav) { uav.textContent = currentUser.avatar; uav.style.background = `linear-gradient(135deg,${currentUser.color},${currentUser.color}cc)`; }
  if (uname) uname.textContent = currentUser.name;
  const urole = uav?.closest('.sidebar-user')?.querySelector('.urole, [style*="color:var(--txt3)"]');

  // Add session bar to topbar
  const topbarR = document.querySelector('.topbar-r');
  if (topbarR) {
    // Remove existing if any
    const existing = document.getElementById('sessionBar');
    if (existing) existing.remove();

    const sessionBar = document.createElement('div');
    sessionBar.id = 'sessionBar';
    sessionBar.style.cssText = 'display:flex;align-items:center;gap:10px;';
    sessionBar.innerHTML = `
      <div class="session-user" onclick="openM('mProfile')" title="My Profile">
        <div class="session-uav" style="background:${currentUser.color}">${currentUser.avatar}</div>
        <span style="font-size:12.5px;font-weight:600;color:var(--txt2)">${currentUser.name.split(' ')[0]}</span>
        <span class="role-badge rb-${currentUser.role}">${currentUser.roleLabel}</span>
      </div>
      <button class="logout-btn" onclick="doLogout()" title="Sign Out">↩ Logout</button>
    `;
    // Insert before last button
    topbarR.insertBefore(sessionBar, topbarR.lastElementChild);
  }

  // Update profile modal if exists
  const pName = document.getElementById('pName');
  const pEmail = document.getElementById('pEmail');
  if (pName) pName.value = currentUser.name;
  if (pEmail) pEmail.value = currentUser.username + '@acmecorp.com';
}

// ── LOGOUT ──
function doLogout() {
  if (!confirm('Are you sure you want to sign out?')) return;
  logout();
}

function logout() {
  // Stop session timer
  if (sessionTimer) clearInterval(sessionTimer);
  // Clear session
  localStorage.removeItem('attendflow_session');
  currentUser = null;
  // Stop punch timer if running
  if (typeof punchTimer !== 'undefined' && punchTimer) { clearInterval(punchTimer); }
  // Reset login form
  document.getElementById('loginUser').value = '';
  document.getElementById('loginPass').value = '';
  document.getElementById('loginPass').type = 'password';
  document.getElementById('pwToggleBtn').textContent = '👁';
  document.getElementById('loginError').classList.remove('show');
  const btn = document.getElementById('loginBtn');
  btn.disabled = false;
  btn.innerHTML = 'Sign In';
  btn.style.background = '';
  // Show login, hide app
  const appWrapper = document.getElementById('appWrapper');
  appWrapper.style.transition = 'opacity .3s ease';
  appWrapper.style.opacity = '0';
  setTimeout(() => {
    appWrapper.style.display = 'none';
    appWrapper.style.opacity = '';
    const loginScreen = document.getElementById('loginScreen');
    loginScreen.style.display = 'flex';
    loginScreen.style.opacity = '0';
    loginScreen.style.transform = 'scale(.98)';
    setTimeout(() => {
      loginScreen.style.transition = 'opacity .35s ease, transform .35s ease';
      loginScreen.style.opacity = '1';
      loginScreen.style.transform = 'none';
      document.getElementById('loginUser').focus();
    }, 50);
  }, 320);
}

// ── SESSION TIMER ──
function startSessionTimer() {
  if (sessionTimer) clearInterval(sessionTimer);
  let remaining = SESSION_DURATION;
  sessionTimer = setInterval(() => {
    remaining -= 60000;
    if (remaining <= 15 * 60 * 1000 && remaining > 14 * 60 * 1000) {
      // Warn at 15 min
      showAppToast('⏰ Session expires in 15 minutes. Save your work!', 'warning', 6000);
    }
    if (remaining <= 0) {
      clearInterval(sessionTimer);
      showAppToast('🔐 Session expired. Please sign in again.', 'error', 4000);
      setTimeout(logout, 4500);
    }
  }, 60000);
}

function showAppToast(msg, type, dur) {
  if (typeof toast === 'function') toast(msg, type, dur);
}

// ── HELPERS ──
function fillDemo(u, p) {
  document.getElementById('loginUser').value = u;
  document.getElementById('loginPass').value = p;
  document.getElementById('loginUser').classList.remove('error');
  document.getElementById('loginPass').classList.remove('error');
  document.getElementById('loginError').classList.remove('show');
  document.getElementById('loginPass').focus();
}

function togglePwVis() {
  const inp = document.getElementById('loginPass');
  const btn = document.getElementById('pwToggleBtn');
  if (inp.type === 'password') { inp.type = 'text'; btn.textContent = '🙈'; }
  else { inp.type = 'password'; btn.textContent = '👁'; }
}

function showForgotPw() {
  alert('🔐 Password Reset\n\nDemo credentials:\n\n• admin / Admin@123 (Super Admin)\n• hrmanager / HR@2026 (HR Manager)\n• manager / Mgr@2026 (Manager)\n• employee / Emp@2026 (Employee)\n\nContact your system administrator to reset your password.');
}

// ── ROLE PERMISSION GUARD ──
function canDo(action) {
  if (!currentUser) return false;
  switch(action) {
    case 'edit': return currentUser.canEdit;
    case 'delete': return currentUser.canDelete;
    case 'approve': return currentUser.canApprove;
    case 'payroll': return currentUser.canViewPayroll;
    case 'addEmp': return currentUser.role === 'admin' || currentUser.role === 'hr';
    default: return true;
  }
}

// Override goPage to enforce role restrictions
const _origGoPage = typeof goPage === 'function' ? goPage : null;
if (_origGoPage) {
  window._goPageOrig = _origGoPage;
  window.goPage = function(p) {
    if (!currentUser) return;
    if (!currentUser.permissions.includes(p)) {
      if (typeof toast === 'function') toast('🔒 Access denied — insufficient permissions for your role', 'error');
      return;
    }
    _origGoPage(p);
  };
}

// Add shake animation
const shakeStyle = document.createElement('style');
shakeStyle.textContent = '@keyframes shake{0%,100%{transform:none}15%{transform:translateX(-6px)}30%{transform:translateX(5px)}45%{transform:translateX(-4px)}60%{transform:translateX(3px)}75%{transform:translateX(-2px)}}';
document.head.appendChild(shakeStyle);


// ══════════════════════════════════════════════════
// PAYSLIP PDF DOWNLOAD & PRINT
// ══════════════════════════════════════════════════
function downloadPayslipPDF() {
  const data = window._currentPayslipData;
  if (!data) { toast('No payslip loaded', 'error'); return; }

  const btn = document.querySelector('#mPayslip .btn-primary');
  if (btn) { btn.textContent = '⏳ Preparing...'; btn.disabled = true; }

  // Build a full standalone HTML page for printing/saving
  const printHTML = `<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Payslip — ${data.name} — ${data.month}</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body { font-family: 'Plus Jakarta Sans', sans-serif; background: #f5f3ef; padding: 30px; }
  @page { size: A4; margin: 15mm; }
  @media print {
    body { background: #fff; padding: 0; }
    .no-print { display: none !important; }
    * { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
  }
  .no-print { text-align: center; padding: 16px; margin-bottom: 16px; }
  .no-print button { padding: 10px 24px; margin: 0 6px; border: none; border-radius: 8px; font-size: 14px; font-weight: 600; cursor: pointer; }
  .btn-print { background: #4f46e5; color: #fff; }
  .btn-close { background: #e8e3db; color: #1a1714; }
</style>
</head>
<body>
<div class="no-print">
  <button class="btn-print" onclick="window.print()">🖨️ Print / Save as PDF</button>
  <button class="btn-close" onclick="window.close()">✕ Close</button>
  <p style="margin-top:8px;font-size:12px;color:#8a8078;">Use browser Print → Save as PDF to download</p>
</div>
${data.html}
</body>
</html>`;

  // Open in new window for print/save
  const win = window.open('', '_blank', 'width=800,height=900,scrollbars=yes');
  if (!win) {
    toast('⚠️ Pop-up blocked! Please allow pop-ups for this site and try again.', 'warning', 5000);
    if (btn) { btn.textContent = '⬇ Download PDF'; btn.disabled = false; }
    return;
  }
  win.document.write(printHTML);
  win.document.close();

  setTimeout(() => {
    if (btn) { btn.textContent = '⬇ Download PDF'; btn.disabled = false; }
    toast('✅ Payslip opened in new window — use Print → Save as PDF', 'success', 4000);
  }, 500);
}

function printPayslip() {
  const data = window._currentPayslipData;
  if (!data) { toast('No payslip loaded', 'error'); return; }

  // Fill the hidden print wrapper and trigger browser print
  const wrap = document.getElementById('payslipPrintWrap');
  if (!wrap) { toast('Print not available', 'error'); return; }
  wrap.innerHTML = data.html;
  wrap.style.display = 'block';

  // Short delay to ensure render, then print
  setTimeout(() => {
    window.print();
    // After print dialog closes, hide the wrapper
    setTimeout(() => {
      wrap.style.display = 'none';
      wrap.innerHTML = '';
    }, 1000);
  }, 300);

  toast('🖨️ Print dialog opened', 'info', 2000);
}

// ── START ──
initAuth();
</script>

</body>
</html>